# E2 -- M2: reconcile + entities + disposition queue + ladder calibration
Acceptance: RFC section 17 M2 AC: an injected contradicting-balance fixture produces a correct A/B disposition item and, on accept, a correct supersession line in the shard plus an updated fence head visible in `git diff`; entity merge round-trips; a two-client dispose race resolves to one durable verdict; the ladder calibration report exists and the shipped defaults match it.
fidelity: executable

Design anchors: RFC 7.2a, 7.7, 8.2 (semantics), 10.2, 10.3, 10.4, 10.5, 12 (packing function), 16. Decisions: docs/adr/004 (pending records become dirty_edit items), 006 (`serenity cron`, x/term TUI), 007 (constants).

### Wave 2a (deps on E0 + E1 waves 1a-1b; 6 agents)

- [ ] T2.1 DISPOSITION store `internal/disposition`: items, append-only history, idempotency, already_disposed  Owner: pool  Est: 90m  verifies: [UC-012, UC-026]  deps: [T0.10, T0.4]  acc: [dispose retried with the same idempotency_key returns the original result; a second dispose of a disposed item returns already_disposed with the recorded verdict; reject without a note errors; .serenity/pending/*.json records import as dirty_edit items and are deleted]
  - Item kinds: reconcile, precept_draft, effect, distill, tombstone, dirty_edit. States: pending, deferred, parked, disposed. History rows are never updated.
- [ ] T2.10 Ladder policy object `internal/ladder`: config parse with mandatory correlation guards, cell state, promotion, demotion, auto-action logging  Owner: pool  Est: 90m  verifies: [UC-019]  deps: [T0.10]  acc: [config missing min_span_days or min_distinct_sources fails to parse; synthetic history meeting counts but spanning 3 days does not promote; one human reversal of an auto action demotes the cell to trust 0; a never_automate cell refuses promotion; every auto action writes an auto disposition and is sampled at sample_rate]
- [ ] T2.12 Decay + weekly sweep `internal/reconcile/decay.go` (read-time decay, alias candidates, low-confidence to distill)  Owner: pool  Est: 60m  verifies: [UC-044]  deps: [T1.11]  acc: [aged fixture flips ranking order while every claim's State is unchanged; a sub-threshold claim appears as a distill item; decay never writes a confidence value back to a file]
- [ ] T2.16 Voice-note connector (transcription via router local-cheap tier)  Owner: pool  Est: 75m  verifies: [UC-009]  deps: [T1.1, T1.2, T1.7]  acc: [fixture audio refs with a fake transcriber produce sources of kind voice with transcript bytes and the audio sha in meta; double ingest yields zero duplicates]
- [ ] T2.19 `serenity cron <job>` runner with injected clock (sweep, consolidate, decay, slo) + launchd/systemd unit docs  Owner: pool  Est: 60m  verifies: [UC-044, UC-018]  deps: [T0.3]  acc: [each job is a function callable from tests with a fake clock; `serenity cron sweep` on a fixture exits 0 and is idempotent on a second run; docs/operator/scheduling.md ships both unit files]
- [ ] T2.20 Distill queue + `serenity capture <text|audio>` staging  Owner: pool  Est: 60m  verifies: [UC-034]  deps: [T2.1]  acc: [capture returns a staged item id; dispositions claim-batch/precept-draft/note/trash each route the payload correctly (precept-draft creates a disposition item, never a .dira entry)]

### Wave 2b (6 agents)

- [ ] T2.2 Reconcile engine `internal/reconcile`: (subject, predicate) + neighbor candidates, six-verdict temporal enum, A/B items, trust 0  Owner: pool  Est: 90m  verifies: [UC-014]  deps: [T1.9, T1.10, T2.1]  acc: [contradicting-balance fixture -> exactly one A/B item with both provenances; overlapping-window fixture -> window-close proposal, not refutation; project-scoped fixture -> scoped verdict; no claim state changes without a dispose (asserted by dump diff)]
- [ ] T2.6 Expiry sweeper: pending > 14d (per kind) -> deferred; 3 cycles -> parked; resurface once on new evidence  Owner: pool  Est: 60m  verifies: [UC-013]  deps: [T2.1, T2.19]  acc: [fake-clock test walks an item through deferred to parked; a new claim on the same (subject, predicate) resurfaces it exactly once; nothing transitions parked -> disposed automatically; parked items are excluded from depth and age metrics]
- [ ] T2.8 Two-client dispose race property test  Owner: pool  Est: 45m  verifies: [UC-026]  deps: [T2.1]  acc: [go test -race with two goroutine clients over 100 iterations: exactly one wins per item and the loser receives already_disposed carrying the winner's verdict]
- [ ] T2.11 Ladder calibration sweep `internal/ladder/calibrate.go` + `evals/calibration/`  Owner: pool  Est: 90m  verifies: [UC-019]  deps: [T2.10, T1.14]  acc: [evals/calibration/report.json holds the sweep grid over min_dispositions x min_accept x sample_rate including cells with < 50 dispositions, the chosen defaults, and per-cell rationale; a test asserts config.Default ladder values equal the report's chosen values]
- [ ] T2.13 Entity resolution `internal/entities`: alias match, embedding similarity within type, undoable merge event, split, ambiguous -> disposition  Owner: pool  Est: 90m  verifies: [UC-017]  deps: [T2.1, T1.10, T0.3]  acc: [merge two fixture entities re-points every claim and renders one page; undo restores the prior state byte-identically; an ambiguous pair yields a disposition item, not a merge; split partitions claims into two pages]
- [ ] T2.15 Queue SLOs (`internal/queue/slo.go`) into `serenity status`  Owner: pool  Est: 45m  verifies: [UC-004, UC-013]  deps: [T2.1, T2.6, T1.17]  acc: [60 pending items trip the depth alert; 40 items older than 3 days trip the age alert; parked items count in neither; status renders depth, p50 age, time-to-dispose, abandonment]

### Wave 2c (6 agents)

- [ ] T2.3 Supersession write path on accept: fence strikethrough + pointer, shard superseding line + regenerated head  Owner: pool  Est: 75m  verifies: [UC-015]  deps: [T2.2, T0.3]  acc: [integration test in a temp git repo: accept the A/B item, `git diff` shows one appended shard line with supersedes=<old id> and the fence head row updated; old id unchanged; re-render byte-identical]
- [ ] T2.4 Fence/shard divergence handling: detect hand-edited shard head, disposition item, accept appends human-provenance line  Owner: pool  Est: 60m  verifies: [UC-016]  deps: [T2.1, T2.3]  acc: [edit a shard-head fence row, run extract -> a dirty_edit item; accept -> shard gains a line with actor human:<id> and the head regenerates from the shard; the M0 disagreement fixture extended: the human edit survives wipe-and-rebuild via the shard]
- [ ] T2.5 CLI inbox: J/K/space, verdicts, grouped items, `--bulk-defer <filter>`, per-family pause, `--parked`  Owner: pool  Est: 90m  verifies: [UC-012, UC-013]  deps: [T2.1, T2.6]  acc: [scripted-TTY test drives J/K/space and records one disposition per group member; --bulk-defer family=has_balance defers exactly the matching items; --parked lists parked items only]
- [ ] T2.7 `edit_accept` through the deterministic writer with human-tier provenance  Owner: pool  Est: 45m  verifies: [UC-012]  deps: [T2.1, T2.3]  acc: [edited object lands in the fence or shard with Provenance.Actor = human:<id>; the disposition row references the new claim id; re-render byte-identical]
- [ ] T2.9 Compaction gated by an approved disposition item + shard rollover at configured size  Owner: pool  Est: 45m  verifies: [UC-033]  deps: [T0.9, T2.1]  acc: [compact without an accepted compact item refuses; with it, the archive shard exists and sync dumps byte-identically; a shard crossing rollover size opens <family>.N.jsonl and ResolveHeads spans both]
- [ ] T2.14 Consolidate `internal/consolidate`: summary fences with freshness banners, shard-head refresh, re-embed changed chunks  Owner: pool  Est: 75m  verifies: [UC-018]  deps: [T2.3, T0.3]  acc: [consolidate twice on the fixture brain is byte-identical; a hand-edited summary fence is overwritten (DERIVED) while a hand-edited fence-tier claims row is preserved; shard heads equal ResolveHeads]

### Wave 2d (4 agents)

- [ ] T2.17 Briefing scaffold `internal/briefing`: five fixed sections, 800-word cap, drop-not-truncate, packing function with a token-estimator parameter  Owner: pool  Est: 75m  verifies: [UC-018]  deps: [T2.15, T2.14]  acc: [golden render on the fixture brain; a section over budget is dropped whole with omitted: N; the packer is a pure function reused unchanged by E4's brief]
- [ ] T2.18 Reconcile eval: verdict confusion matrix + contradiction-detection recall in evals/report.json  Owner: pool  Est: 60m  verifies: [UC-045, UC-014]  deps: [T1.13, T2.2, T2.12]  acc: [report has a reconcile section with per-verdict P/R/F1; a deliberately missed temporal fixture appears as a false negative]
- [ ] T2.21 Tombstone cascade: source tombstone -> retraction proposals -> accept rewrites fences/shards and rebuilds  Owner: pool  Est: 75m  verifies: [UC-035]  deps: [T1.2, T2.1, T2.3]  acc: [tombstoning a fixture source creates one retraction item per sole-provenance claim; accept marks them retracted in files, rebuild drops them from the index, and multi-provenance claims are demoted not retracted]
- [ ] T2.22 M2 exit: run the RFC AC checklist end to end on a laptop and record it  Owner: pool  Est: 45m  verifies: [UC-014, UC-015, UC-017, UC-026, UC-019]  deps: [T2.3, T2.4, T2.5, T2.8, T2.11, T2.13]  acc: [docs/evals/m2-report.md records each of the five M2 ACs with the command run and observed output]
