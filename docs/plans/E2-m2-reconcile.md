# E2 -- M2: reconcile + entities + disposition queue + ladder calibration
Acceptance: RFC section 17 M2 AC: an injected contradicting-balance fixture produces a correct A/B disposition item and, on accept, a correct supersession line in the shard plus an updated fence head visible in `git diff`; entity merge round-trips; a two-client dispose race resolves to one durable verdict; the ladder calibration report exists and the shipped defaults match it.
fidelity: executable

Design anchors: RFC 7.2a, 7.7, 8.2 (semantics), 10.2, 10.3, 10.4, 10.5, 12 (packing function), 16. Decisions: docs/adr/004 (pending records become dirty_edit items), 006 (`serenity cron`, x/term TUI), 007 (constants).

### Wave 2a (deps on E0 + E1 waves 1a-1b, gated behind M1 exit T1.23 per build-in-M-order -- ADR 011; 6 agents)

- [x] T2.1 DISPOSITION store `internal/disposition`: items, append-only history, idempotency, already_disposed  Owner: pool  Est: 90m  verifies: [UC-012, UC-026]  deps: [T0.10, T0.4]  acc: [dispose retried with the same idempotency_key returns the original result; a second dispose of a disposed item returns already_disposed with the recorded verdict; reject without a note errors; .serenity/pending/*.json records import as dirty_edit items and are deleted] (merged PR #76, 2026-09-07, rebase merge commit 203dd7775a61f12b99052796d73204106002b5b3; checkbox correction 2026-09-07 -- the code shipped and T2.6/T2.8/T2.10/T2.12/T2.16/T2.20 all already merged depending on and using this package, but this task's own mark-done step was never done. Verified before flipping, not re-implemented: `internal/index/disposition.go` claims the `disposition_items`/`disposition_history` runtime table shells T0.10 seeded; `internal/disposition.Store.Dispose` enforces reject-requires-note, idempotency-key replay, and already_disposed-on-second-dispose in that order (each covered by its own test: `TestRejectWithoutNoteErrors`, `TestDisposeRetriedWithSameIdempotencyKeyReturnsOriginalResult`, `TestSecondDisposeOfDisposedItemReturnsAlreadyDisposed`); `Store.ImportPending` sweeps `.serenity/pending/*.json` into `dirty_edit` items and deletes the source files (`TestImportPendingCreatesDirtyEditItemsAndDeletesFiles`). Spot-checked all four tests green (`go test -run ... ./internal/disposition/...`, 0.35s) rather than trusting the checkbox gap as evidence of anything wrong with the code itself.)
  - Item kinds: reconcile, precept_draft, effect, distill, tombstone, dirty_edit. States: pending, deferred, parked, disposed. History rows are never updated.
- [x] T2.10 Ladder policy object `internal/ladder`: config parse with mandatory correlation guards, cell state, promotion, demotion, auto-action logging  Owner: pool  Est: 90m  verifies: [UC-019]  deps: [T0.10]  acc: [config missing min_span_days or min_distinct_sources fails to parse; synthetic history meeting counts but spanning 3 days does not promote; one human reversal of an auto action demotes the cell to trust 0; a never_automate cell refuses promotion; every auto action writes an auto disposition and is sampled at sample_rate] (merged PR #74, 2026-09-06, rebase merge commit b321122e44bb4a95b2bcc09a81b5021bf025a809; `Config`/`ParseConfig`/`Engine` -- `Evaluate`/`Promote`/`Demote`/`LogAutoAction`; decoupled from `internal/disposition` (T2.1, a concurrent sibling task, not a dep of this one) via a package-local `Recorder` interface; `config.Default()` seeds RFC 10.3's priors as `Config.Ladder`, the seam T2.11's calibration sweep asserts against)
- [x] T2.12 Decay + weekly sweep `internal/reconcile/decay.go` (read-time decay, alias candidates, low-confidence to distill)  Owner: pool  Est: 60m  verifies: [UC-044]  deps: [T1.11]  acc: [aged fixture flips ranking order while every claim's State is unchanged; a sub-threshold claim appears as a distill item; decay never writes a confidence value back to a file] (merged PR #78, 2026-09-06, rebase merge commit 1b13ba04d5227529ff828497118b295ccd786b73; `DecayedConfidence`/`Rank`/`DistillCandidates`/`AliasCandidates`/`Sweep`, first file in the new `internal/reconcile` package -- T2.2's reconcile engine lands alongside it; alias-candidate detection is a lexical heuristic (edit-distance-1, token-subset), deliberately simpler than T2.13's later embedding-similarity merge)
- [x] T2.16 Voice-note connector (transcription via router local-cheap tier)  Owner: pool  Est: 75m  verifies: [UC-009]  deps: [T1.1, T1.2, T1.7]  acc: [fixture audio refs with a fake transcriber produce sources of kind voice with transcript bytes and the audio sha in meta; double ingest yields zero duplicates] (merged PR #77, 2026-09-07, rebase merge commit `5d568af2`; new `internal/connector/voice`: `Connector` polls a directory tree for audio files (per-path sha256/size/mtime cursor, unchanged files never re-transcribed), `RouterTranscriber` calls `router.Router.Complete` on `TaskClassTranscription` (local-cheap tier); `ToSource` stores the transcript text as `Bytes` with the audio's own sha256 in `Meta["audio_sha256"]`, so two audio files transcribing to identical text collapse onto one `Source` via normal content-address dedup. Scope disclosed in the PR: wiring `connectors.voice` into `buildConnectors`/`serenity.yml` (a `models.transcription` pin + `BuildTranscriptionRouter`) is a follow-up, not required by this acc line)
- [x] T2.19 (2026-09-06/07, PR #75) `serenity cron <job>` runner with injected clock (sweep, consolidate, decay, slo) + launchd/systemd unit docs  Owner: pool  Est: 60m  verifies: [UC-044, UC-018]  deps: [T0.3]  acc: [each job is a function callable from tests with a fake clock; `serenity cron sweep` on a fixture exits 0 and is idempotent on a second run; docs/operator/scheduling.md ships both unit files] -- done: `internal/cron` (Job registry: sweep/consolidate/decay/slo, injected Clock interface) + `serenity cron <job>` CLI + docs/operator/scheduling.md (launchd plist + systemd service/timer templates for all four jobs). Scoping disclosed in the PR: T2.6/T2.12/T2.14/T2.15 (the tasks that own real sweep/decay/consolidate/SLO semantics) are not landed yet, so all four job bodies are placeholders that only record their own run (`.serenity/cron/<job>.json`) -- T2.19's own acc line only requires the runner scaffold, the CLI surface, and idempotent run-record semantics, not the scheduled work itself (ADR 006 confirms T2.6 depends on T2.19, not the reverse). Whoever ships those tasks replaces the matching function body in place.
- [x] T2.20 Distill queue + `serenity capture <text|audio>` staging  Owner: pool  Est: 60m  verifies: [UC-034]  deps: [T2.1]  acc: [capture returns a staged item id; dispositions claim-batch/precept-draft/note/trash each route the payload correctly (precept-draft creates a disposition item, never a .dira entry)] (merged PR #82, 2026-09-07, rebase merge commit `1e45843d`; `disposition.Capture` stages a `KindDistill` item from text and/or an audio ref, `Store.RouteDistill` disposes it along `claim-batch`/`precept-draft`/`note`/`trash`, stamping the chosen route on `Item.Route`; `precept-draft` creates a new `precept_draft` disposition item carrying the same payload -- never a `.dira` entry; CLI wired as `serenity capture [text] [--audio ref] [--hint text]`. Recovered from a prior pool session killed mid-incident (shared-machine load spike) after its worktree was found clean/complete/pushed with no PR -- re-verified the diff against this acc line, re-ran the full validation ladder before shipping)

### Wave 2b (6 agents)

- [x] T2.2 Reconcile engine `internal/reconcile`: (subject, predicate) + neighbor candidates, six-verdict temporal enum, A/B items, trust 0  Owner: pool  Est: 90m  verifies: [UC-014]  deps: [T1.9, T1.10, T2.1]  acc: [contradicting-balance fixture -> exactly one A/B item with both provenances; overlapping-window fixture -> window-close proposal, not refutation; project-scoped fixture -> scoped verdict; no claim state changes without a dispose (asserted by dump diff)] (merged PR #94, 2026-09-07, rebase merge commit 8d26a86c5fb40a4fcb4f78b4dd8f35bc142c5aa1; new `internal/reconcile/reconcile.go` alongside `decay.go`'s existing weekly-sweep pieces -- `Verdict` (six members adapted from gbrain's own six-verdict contradiction-probe type, `dndungu/gbrain` `eval-contradictions/types.ts`, to RFC §10.2's routing vocabulary: agree/neutral_additive/conflict/window_close/scoped/precept_conflict-reserved-undetected), `Candidates` (exact (subject,predicate) match only -- embedding-similarity neighbors deferred to T2.13, no per-claim embedding index exists yet), `Detect` (pure, priority-resolved across multiple candidates: conflict > window_close > scoped > agree > neutral_additive), `Engine.Process` (stages exactly one `KindReconcile` disposition item via T2.1's Store for conflict/window_close, both provenances embedded via the full A/B claims, nothing staged otherwise). Scope qualifiers read as a disclosed lexical heuristic off Object text ("for project X"/"(project: X)") -- no structured scope field exists in `domain.Claim` or the persisted claims table; `ValidFrom`/`ValidTo` get their first real temporal meaning here (internal/ingest's own comment named this as E2's job). Disclosed out of scope: `agree`'s "weighted confidence bump" write-through, `precept_conflict` detection (needs a precept-matching lookup outside this task's deps), wiring to a live trigger (claims.created event or cron) -- this task ships the same shape decay.go's Sweep already established, a pure/injectable-clock function a future task wires up. Real red->green spot-check: disabled the temporal-window branch, confirmed the overlapping-window test failed with the exact expected mismatch, restored.)
- [x] T2.6 Expiry sweeper: pending > 14d (per kind) -> deferred; 3 cycles -> parked; resurface once on new evidence  Owner: pool  Est: 60m  verifies: [UC-013]  deps: [T2.1, T2.19]  acc: [fake-clock test walks an item through deferred to parked; a new claim on the same (subject, predicate) resurfaces it exactly once; nothing transitions parked -> disposed automatically; parked items are excluded from depth and age metrics] (merged PR #85, 2026-09-07, rebase merge commit `0a726d42`; `disposition.Sweep` advances pending/deferred items past their kind's threshold one cycle, reusing `Item.DeferCount`, parking at `MaxDeferCycles` (3); `Store.Resurface` moves a parked item back to pending exactly once, structurally enforced -- correlating "new evidence" with a parked item's (subject, predicate) is left to T2.2/T2.13, which own that domain knowledge; `PendingDepth`/`OldestPendingAge` exclude parked items, the minimal primitive T2.15 is expected to build on; wires `internal/cron`'s `Sweep` placeholder (T2.19) to the real logic)
- [x] T2.8 Two-client dispose race property test  Owner: pool  Est: 45m  verifies: [UC-026]  deps: [T2.1]  acc: [go test -race with two goroutine clients over 100 iterations: exactly one wins per item and the loser receives already_disposed carrying the winner's verdict] (merged PR #84, 2026-09-07; found and fixed a real gap while implementing this: Store.Dispose's read-check-write had no synchronization and the SQLite backend gives none either, so two concurrent Dispose calls on the same item could both succeed instead of exactly one winning -- added a sync.Mutex on Store guarding Dispose's critical section; TestTwoClientDisposeRaceExactlyOneWins spot-checked red->green by reverting the mutex and reproducing the exact bug (winners=2, losers=0) before restoring it)
- [x] T2.11 Ladder calibration sweep `internal/ladder/calibrate.go` + `evals/calibration/`  Owner: pool  Est: 90m  verifies: [UC-019]  deps: [T2.10, T1.14]  acc: [evals/calibration/report.json holds the sweep grid over min_dispositions x min_accept x sample_rate including cells with < 50 dispositions, the chosen defaults, and per-cell rationale; a test asserts config.Default ladder values equal the report's chosen values] (merged PR #91, 2026-09-07, rebase merge commit 07eb095b9df314e21a5188037bfcebd28b421bcd; `Grid`/`DefaultGrid()` (min_dispositions 15..100 including three values under 50), `Scenario`/`DefaultScenarios()` (six disclosed synthetic cells with a stated Why each), `Sweep()`/`Calibrate()` -- 180 grid candidates swept, 40 satisfy every scenario, and the RFC 0001 §10.3 prior (50, 0.98, 0.05) is itself among them, so `ladder.DefaultConfig()` is unchanged; `evals/calibration/gen_calibration.go` (`//go:build ignore` generator) produced the committed `evals/calibration/report.json`; `TestDefaultConfigMatchesCommittedCalibrationReport` asserts `DefaultConfig().Default` equals the report's `Chosen` field-for-field. Disclosed scope: this sweep holds `CorrelationGuards` and the `NeverAutomate` seed list fixed as inputs -- neither is recalibrated here, matching the acc line's three named dimensions; the synthetic scenario battery is this session's own disclosed evidence, not real production disposition history (E2 has not shipped), which is why `Calibrate` prefers keeping the prior over loosening it)
- [x] T2.13 Entity resolution `internal/entities`: alias match, embedding similarity within type, undoable merge event, split, ambiguous -> disposition  Owner: pool  Est: 90m  verifies: [UC-017]  deps: [T2.1, T1.10, T0.3]  acc: [merge two fixture entities re-points every claim and renders one page; undo restores the prior state byte-identically; an ambiguous pair yields a disposition item, not a merge; split partitions claims into two pages] (PR #110; new `internal/entities`, mirroring `internal/reconcile`'s Detect/Engine.Process split between pure classification and disposition staging -- `Evaluate` runs RFC §10.5's two-stage pipeline (exact/alias match, then embedding similarity within type via T1.10's `embed.Embedder`) and returns a `Decision`; `Engine.Resolve` dispatches AutoMerge to `Merge`, Ambiguous to a new `disposition.KindEntityMerge` item via `StageAmbiguous`, NoMatch to nothing. `Merge` re-points every fence-tier claim from the absorbed entity onto the survivor's page, unions aliases (absorbed slug + its own aliases become aliases of the survivor), removes the absorbed page, and persists an undoable `MergeEvent` (pre-merge byte snapshots of both pages) under `.serenity/entities/merges/<id>.json`; `Undo` restores both pages byte-identically and refuses a second undo. `Split` partitions a page's claims by id into two pages, the RFC's stated inverse-ish primitive (no undo required by the RFC for split; reversing one is calling `Merge` on the result, proven by `TestSplitThenMergeRoundTrips`). Disclosed scope: `Merge` refuses (`ErrShardTierUnsupported`) rather than silently orphaning data when the absorbed entity has any shard-tier claim family (`ss.Families`) -- RFC §10.5 says nothing about shard tiers, and §7.2a ties that tier to a different axis (high-volume machine observations, not identity); `Split` carries no such check since a fence-tier split never touches the surviving `original` entity's own shard claims. `MergeEvent`'s undo bookkeeping lives under gitignored `.serenity/` (T0.4/T2.19's own precedent for runtime state), not git-tracked -- the actual audit trail is the git commit `writer.Flush` produces over the merge's content changes (RFC §7 preamble: "audit history is git log"), automatically satisfied since every content write routes through `writer.Fence`/the same `writer.Queue`. A real red->green spot-check surfaced a genuine design finding, not just a bug: the fence claims table has no subject column (`store.ParseEntityBytes` always re-derives `SubjectSlug` from the page's own frontmatter slug), so a moved claim's in-memory `SubjectSlug` reassignment is provably inert against the current on-disk format -- confirmed by disabling it and observing `RenderEntity`'s output was byte-for-byte unchanged; kept as defensive/forward-compatible code, documented precisely in `merge.go` rather than left to look load-bearing. The spot-check that does matter (disabling the actual claim-append to the survivor's page) failed exactly as expected. Validation: `go build ./...`, `go vet ./...`, `gofmt -l .` clean, `golangci-lint run --timeout=10m ./...` (whole repo, no path args) 0 issues, `go test -race -timeout 600s ./...` green repo-wide including `internal/gate`'s file-first CI gate (no direct `WriteEntity`/`Append` caller added outside `internal/writer`/allowlist). 27 new tests across `internal/entities`, all green under `-race`.)
- [x] T2.15 Queue SLOs (`internal/queue/slo.go`) into `serenity status`  Owner: pool  Est: 45m  verifies: [UC-004, UC-013]  deps: [T2.1, T2.6, T1.17]  acc: [60 pending items trip the depth alert; 40 items older than 3 days trip the age alert; parked items count in neither; status renders depth, p50 age, time-to-dispose, abandonment] (PR #122, 2026-09-07; new `internal/queue` package, `Compute(ctx, *disposition.Store, Config, now) (Snapshot, error)`, against RFC 0001 §7's own numbers -- "alert in the briefing when p50 age > 3 days or depth > 50" -- `DefaultDepthThreshold=50`, `DefaultAgeThreshold=3*24h`, both overridable via `Config` with the same "zero means use the default" convention `ShardStore.RolloverBytes` (T2.9) already established. Depth reuses `disposition.Store.PendingDepth` directly rather than re-deriving the pending/deferred-vs-parked exclusion rule, per that method's own doc comment naming T2.15 as its expected caller. `Snapshot` carries four metrics: `Depth`/`DepthAlert` (depth > 50), `P50Age`/`AgeAlert` (median age across the same pending+deferred population, > 3 days), `TimeToDispose` (median CreatedAt->DisposedAt across every disposed item -- RFC 0001 §7's own metric name; p50 chosen for consistency with P50Age and because a single slow outlier shouldn't skew a mean, disclosed as this task's own statistic choice since the RFC names the metric but not a formula), and `Abandonment` (parked / (parked+disposed) -- also an RFC-named metric with no given formula; read as "the fraction of items that left the queue via parking rather than a human's actual verdict," disclosed as this task's own reading). Every metric with no underlying population (fresh brain, nothing pending, nothing ever disposed) reports its own `*OK=false` rather than a bare zero that would misreport "no data yet" as "healthy" -- `serenity status`'s new `queue` line renders `n/a` for each in that case, matching `printRebuildTiming`'s existing "never" convention. Wired into `internal/cli/status.go`'s `runStatus` as a new `queue` output line between `spend` and `rebuild`, opening the same `*index.SQLite` (`providers.OpenIndex`) and disposition store every other CLI verb already uses -- no bypass fixture. Two real red->green spot-checks: forced `DepthAlert` to always-false -- `TestQueueSLODepthAlertTripsAt60PendingItems` failed exactly as expected, restored, reconfirmed green; made the parked-item branch also append into the pending-age list (simulating the exclusion rule regressing) -- `TestQueueSLOParkedItemsCountInNeither` failed exactly as expected (`P50Age = 4380h0m0s, want ~0`), restored, reconfirmed green. Six new tests: five in `internal/queue/slo_test.go` (the acc line's own depth-alert, age-alert, and parked-exclusion clauses verbatim, plus a combined time-to-dispose/abandonment fixture and an empty-store "reports no data, not zero" case) and one CLI-level test in `internal/cli/status_test.go` (`TestStatusRendersQueueSLOs`, proving `runStatus` is actually wired to `queue.Compute` end to end, not just that the package's own math is correct) -- plus the two existing golden/fresh-brain status tests updated for the new `queue` line. Validation: `gofmt -l .`, `go build ./...`, `go vet ./...` clean; `go test -race -timeout 600s ./...` green repo-wide (including `internal/gate`'s file-first CI gate); `golangci-lint run --timeout=10m ./...` 0 issues; fleet build lease held for the full ladder and released immediately after. Disclosed, not built here: no `serenity.yml` config surface for overriding the depth/age thresholds yet (mirrors `disposition.Thresholds`' own disclosed gap); the briefing itself (T2.17, still open, blocked on this task and T2.14) is what RFC 0001 §7 actually means by "alert in the briefing" -- this task only gets the SLOs computed and onto `status`, per its own acc line's literal scope.)

### Wave 2c (6 agents)

- [x] T2.3 Supersession write path on accept: fence strikethrough + pointer, shard superseding line + regenerated head  Owner: pool  Est: 75m  verifies: [UC-015]  deps: [T2.2, T0.3]  acc: [integration test in a temp git repo: accept the A/B item, `git diff` shows one appended shard line with supersedes=<old id> and the fence head row updated; old id unchanged; re-render byte-identical]  Shipped 2026-09-07 (PR #97): `internal/supersede.Writer.Apply` -- fence-tier strikethrough+pointer via the existing `store.FenceWriter.RenderEntity` encoding, shard-tier one appended line (`Supersedes=<old id>`) plus a regenerated head row scoped to the one touched `(subject, family)` via `store.ShardStore.ResolveHeads`. Both paths route through `writer.Fence`/`writer.Shard`. Acc-line integration test run against a real temp git repo, green.
- [x] T2.4 Fence/shard divergence handling: detect hand-edited shard head, disposition item, accept appends human-provenance line  Owner: pool  Est: 60m  verifies: [UC-016]  deps: [T2.1, T2.3]  acc: [edit a shard-head fence row, run extract -> a dirty_edit item; accept -> shard gains a line with actor human:<id> and the head regenerates from the shard; the M0 disagreement fixture extended: the human edit survives wipe-and-rebuild via the shard] (merged PR #108, 2026-09-07, rebase merge commit 9e21d059f4d0e067b8353690479bfbd763f1b553. Detection and staging were already built (T0.4's dirty-tree guard, T2.1's Store.ImportPending) -- this task is the accept side ADR 004 named but deferred: `internal/supersede.Writer.ApplyDisposedDirtyEdit` parses a disposed dirty_edit item's PendingRecord.Human, and for each shard-tier row whose object diverges from what its (subject, family) shard currently resolves to, appends it to the shard as a new line (Provenance.Actor = the disposing human) via the existing tier-dispatching Apply (T2.3/T2.7), then regenerates the fence head. Real bug found and fixed while implementing this, not synthetic: the human's raw hand edit was never committed, so the immediately-following fence-head regeneration re-tripped the same dirty-tree guard it had just been staged from -- fixed by adding `internal/writer.CommitPath(root, path, message)`, a narrow git-add+commit for one already-on-disk path distinct from Flush's queue-tracked batch commit, called first to formalize the accepted edit as its own commit. Acc line's "M0 disagreement fixture extended" clause verified directly: internal/index/rebuild_test.go's own scaffoldBrain shape (a shard-tier family whose fence head diverges from the shard) is extended with the accept step it doesn't itself cover, then .serenity/ is wiped and rebuilt purely from repo bytes, proving the accepted edit is durable in the shard -- the real distinction from scaffoldBrain's own never-accepted stale edit, which TestShardAuthorityOverFenceHead proves the rebuild must instead overrule. Real red->green spot-check: disabled the CommitPath call, confirmed the exact ErrDirtyTree-wrapped failure the missing commit produces, restored, reconfirmed green. Validation: gofmt/build/vet clean, go test -race -count=1 on internal/supersede, internal/writer, internal/gate (T0.2's file-first AST allowlist -- CommitPath is a git shell-out, not a flagged write-method call, and stays inside internal/writer regardless), internal/index, internal/disposition all green; golangci-lint on the two touched packages 0 issues. Disclosed, not built here: no CLI wiring (serenity inbox's space/accept key does not yet call this for a dirty_edit row, mirroring T2.3's own original package-only scope); two divergence shapes -- a family with no existing shard head, or more than one concurrent head -- are left unhandled, neither exercised by the acc line.)
- [x] T2.5 CLI inbox: J/K/space, verdicts, grouped items, `--bulk-defer <filter>`, per-family pause, `--parked`  Owner: pool  Est: 90m  verifies: [UC-012, UC-013]  deps: [T2.1, T2.6]  acc: [scripted-TTY test drives J/K/space and records one disposition per group member; --bulk-defer family=has_balance defers exactly the matching items; --parked lists parked items only]  Shipped 2026-09-07 (PR #101): `serenity inbox` (`internal/cli/inbox.go`) -- j/k/space interactive loop (space=accept, d=defer, r=reject) over pending/deferred items, grouped rows disposed member-by-member; `--bulk-defer family=<value>` and `--parked` as non-interactive one-shot modes. Both acc-line scenarios covered by dedicated tests against a real disposition store.
- [x] T2.7 `edit_accept` through the deterministic writer with human-tier provenance  Owner: pool  Est: 45m  verifies: [UC-012]  deps: [T2.1, T2.3]  acc: [edited object lands in the fence or shard with Provenance.Actor = human:<id>; the disposition row references the new claim id; re-render byte-identical] (merged PR #103, 2026-09-07, rebase merge commit 06d6e4398a14a5d7641c300689f680a0313faaa4. `internal/supersede.Writer.ApplyDisposedReconcile` write-throughs a disposed KindReconcile item's accept/edit_accept verdict via the existing tier-dispatching Apply (T2.3), setting Provenance.Actor from the disposing item's Actor and unconditionally recomputing the claim id from the final object; `disposition.Item.AppliedClaimID` + `Store.RecordResultClaimID` let the disposition row reference the id the write actually produced. The CLI inbox's new 'e' key (`internal/cli/inbox.go`) wires this to the interactive loop, single ungrouped reconcile rows only. Acc line genuinely verified: `TestApplyDisposedReconcileShardTierEditAcceptProvenanceAndClaimID` checks Provenance.Actor=human:<id> on disk (shard-tier is the only tier that persists Provenance at all -- see T2.3's own fence-format finding), `TestApplyDisposedReconcileFenceTierEditAccept` checks the edited object lands on the fence page, both check re-render byte-identical and AppliedClaimID; `TestInboxInteractiveEKeyEditAcceptWritesThroughToBrainRepo` drives the 'e' key end to end. Real red->green spot-check: disabled the Actor assignment + id recompute, confirmed the expected Provenance.Actor mismatch failure, restored, reconfirmed green. Disclosed, not closed here: space/accept still never writes to the brain repo -- only 'e' does; internal/supersede.Apply had no real caller before this task, and wiring space too would break T2.5's already-merged fixture-based tests and is scope beyond this task's own acc line.)
- [x] T2.9 Compaction gated by an approved disposition item + shard rollover at configured size  Owner: pool  Est: 45m  verifies: [UC-033]  deps: [T0.9, T2.1]  acc: [compact without an accepted compact item refuses; with it, the archive shard exists and sync dumps byte-identically; a shard crossing rollover size opens <family>.N.jsonl and ResolveHeads spans both] (PR #120, 2026-09-07; replaces T0.9's original `--confirm` gate with a real `disposition.KindCompact` item (RFC §7.7: "an explicit, disposition-approved `serenity compact` run"): `serenity compact --propose` stages a new pending KindCompact item (payload `internal/cli.CompactPayload`, empty today -- one compaction scope, every family across every entity, same as T0.9's original sweep) and prints its id; a human reviews/accepts it via the existing `serenity inbox`, exactly like any other disposition item; `serenity compact --item <id>` then requires that item to exist, be `KindCompact`, and be disposed with verdict accept or edit_accept, else refuses naming why -- `serenity compact` with neither flag refuses outright, naming `--item`. Nothing in this codebase proposes a KindCompact item automatically (unlike KindReconcile/KindEntityMerge, both classifier verdicts on new evidence) -- compaction is periodic maintenance a human initiates, so `--propose` is this task's own CLI addition, disclosed as a scope decision beyond the acc line's literal wording but necessary to close the loop the RFC's "disposition-approved" language implies. Shard rollover (`internal/store/shard.go`): `ShardStore.RolloverBytes` (zero -> `DefaultRolloverBytes`, 8 MiB, mirroring the existing `Vocabulary` field's own "zero/nil means use the seeded default" convention) -- `Append` now resolves the family's current segment (`segments()`: the base `<family>.jsonl` first if present, then numbered `<family>.N.jsonl` in ascending order) and opens a new one, numbered one higher than the highest existing segment, once the current segment's on-disk size has already reached the threshold; `Lines`/`ResolveHeads`/the id-collision registry all transparently span every segment (`PathFor` keeps returning only the base path, unchanged for every other caller -- rollover is purely an Append-time storage-layout detail). `Compact` now spans every segment too, consolidates every kept (live-head) line back onto the single base file, and removes the other now-fully-redistributed segments, so a family always holds at most one live segment again after a compact pass. Disclosed, considered and NOT done: making Compact also invalidate a long-lived `*ShardStore`'s in-memory id registry cache for the family it rewrites -- reasoned through explicitly and reverted after an initial draft, since Compact only relocates a claim's line across files, never mutates its content, so an already-cached id->claim entry (including one for a claim about to be archived) stays a byte-accurate historical record after the rewrite; invalidating it would only make that instance's future collision detection *weaker* (matching, not improving on, a fresh instance's own pre-existing gap: neither `segments()` nor `readShardFile` ever scan `<family>.archive.jsonl`, so an id archived before a `ShardStore` was constructed is already invisible to it regardless of caching) -- not a regression this task introduces, and not required by this task's acc line to close. Owner: pool. Validation: `go build ./...`, `go vet ./...`, `gofmt -l .` clean, `golangci-lint run --timeout=10m ./...` (whole repo, no path args) 0 issues, `go test -race -timeout 600s ./...` green repo-wide (including `internal/gate`'s file-first CI gate) via the fleet build lease (claimed and released). Two real red->green spot-checks: (1) disabled the rollover size comparison in `writeTargetLocked` (forcing every append to stay on the current segment regardless of size) -- both new rollover tests failed exactly as expected (`expected rollover to open ... has_balance.1.jsonl: no such file or directory`), restored, reconfirmed green; (2) disabled the CLI's accepted-item state/verdict check in `runCompactCmd` -- `TestCompactCLIItemRefusesUnacceptedItem` failed exactly as expected (a pending, never-disposed item was silently accepted, `compact complete: 0 line(s) archived`), restored, reconfirmed green. Two genuine test-fixture bugs caught and fixed while writing the new rollover tests (not code bugs): `Append` takes a claim by value and derives its id into a local copy, never mutating the caller's variable (the same convention `TestShardAppendDerivesID` already establishes) -- an early draft of the rollover tests compared segment file contents against `c1.ID`/`c2.ID` after `Append` returned and always got empty strings; fixed by setting ids explicitly on the fixtures instead of relying on post-call mutation. Separately, a collision-detection test used two fixed source-ref strings at `DerivedID` width 8 (32 bits), which essentially never collide by chance -- fixed by reusing `TestShardAppendIDCollision`'s own deterministic brute-force search for two refs that actually collide at a narrow width, rather than asserting on an untested assumption. 8 new tests: 5 in `internal/store` (rollover opens a numbered segment and `ResolveHeads` spans both; a third rollover opens `.2.jsonl` in ascending order with cross-segment read order preserved; the id registry still catches a collision across segments; `Compact` consolidates rolled-over segments back to one live file and removes the rest, with a fresh-store rebuild proving the invariant; the real default threshold is not pathologically triggered by ordinary small-scale use), 4 in `internal/cli` (no `--item` refuses and names it; `--propose` stages a real pending `KindCompact` item; `--item` naming a real-but-unaccepted item refuses; the full propose -> accept -> `--item` -> archive-exists/live-heads-only/sync-byte-identical flow, replacing T0.9's original `--confirm` variant of the same three assertions) plus a `CompactPayload` wire-shape pin.)
- [x] T2.14 Consolidate `internal/consolidate`: summary fences with freshness banners, shard-head refresh, re-embed changed chunks  Owner: pool  Est: 75m  verifies: [UC-018]  deps: [T2.3, T0.3]  acc: [consolidate twice on the fixture brain is byte-identical; a hand-edited summary fence is overwritten (DERIVED) while a hand-edited fence-tier claims row is preserved; shard heads equal ResolveHeads] (merged PR #107, 2026-09-07; b11678e: real cron consolidation, preserved human prose and canonical claims, shard-head refresh, changed-only embeddings; nine CI checks green; operator contract in docs/operator/consolidation.md)

### Wave 2d (4 agents)

- [x] T2.17 Briefing scaffold `internal/briefing`: five fixed sections, 800-word cap, drop-not-truncate, packing function with a token-estimator parameter  Owner: pool  Est: 75m  verifies: [UC-018]  deps: [T2.15, T2.14]  acc: [golden render on the fixture brain; a section over budget is dropped whole with omitted: N; the packer is a pure function reused unchanged by E4's brief] (PR #137, 2026-09-07) -- new `internal/briefing` package. `Pack(sections []Section, budget int, estimate Estimator) Briefing` is the pure packing function RFC 0001 §7 (this task's own fixed 800-word daily briefing) and §12 (DIRECTION v1's `brief`, T4.6) both need: sections are evaluated strictly in priority order, each included whole when its own items' total `Estimator` cost fits the budget still remaining after every earlier included section, otherwise dropped whole -- "a section over budget is dropped whole with omitted: N" (this task's own acc line) -- with dropping one section never starving a later, lower-priority one of the budget it would have consumed. `Estimator` is a plain `func(string) int`; `WordEstimator` (word count, RFC 0001 §7's own unit) is Compose's default -- a future T4.6 caller is expected to pass a real token-count `Estimator` against its own caller-supplied token budget instead, with `Pack` itself unchanged either way, satisfying "the packer is a pure function reused unchanged by E4's brief" literally: `Pack` has no dependency on any domain package, only `[]Section` and an `Estimator`.

  `Compose(ctx, *index.SQLite, Config, now) (Briefing, error)` builds the five fixed sections' real content (RFC 0001 §7: "fixed sections *Blocked / Needs you / Moved forward / Watched / Drift*") from the same single `*index.SQLite` handle `serenity status`'s `runStatus` already opens: Blocked sources `queue.Compute`'s `Snapshot` (T2.15, a literal dep, whose own doc comment already named this package as the SLOs' second renderer) -- one line per tripped alert (depth or p50 age), empty when neither trips; Needs you sources `disposition.Store.List` filtered to `StatePending`/`StateDeferred` (excluding parked, matching `PendingDepth`'s own exclusion rule); Moved forward sources the same `List` filtered to `StateDisposed` items disposed within a configurable lookback (default 24h, this task's own disclosed reading -- RFC 0001 §7 gives no explicit window, and no "last briefing rendered at" checkpoint exists yet to read a real one from); Watched sources `spend.ProjectMonth` over `eng.SpendRows` (T4.10, already shipped, RFC 0001 §7: "projected-spend shown in ... the briefing before the ceiling trips") -- **the first live wiring of T4.10's own disclosed gap** ("the briefing's Watched section is untouched since internal/briefing (T2.17) has not shipped yet"); Drift is left empty, disclosed, since T3.9 (orphan detector, RFC 0001 §10.4) has not shipped in this codebase yet -- the same "one section not wired because its own upstream hasn't shipped" shape Watched itself carried until this task closed it. `Render(Briefing) string` formats the packed result as `## <name>` headings (always all five, in RFC order, regardless of which sections were dropped) with `- ` item lines, `(nothing)` for an included-but-empty section, or `(omitted: N)` for a dropped one.

  T2.14 (consolidate)'s role here is disclosed as a pipeline-ordering dependency, not a literal content source: RFC 0001 §7's own nightly sequence runs consolidate immediately before "render the daily briefing," establishing that the brain Compose renders over has fresh entity summary fences and shard heads -- `consolidate.Result` itself (two bare counts, Pages/Embedded) has no per-item detail a section line could render, so this task's own golden test builds its fixture the same way `internal/cli/status_test.go` already does (`git init` + `providers.OpenIndex` + seeded disposition/spend rows) rather than also driving a full `consolidate.Run` pass -- disclosed, not exercised in this package's own tests.

  Genuine red->green spot-check: temporarily forced `Pack`'s own fit check to always-true (`if true` in place of `if cost <= remaining`), confirmed all three whole-section-drop tests failed with the exact expected mismatches (a 3-item section kept its items instead of dropping to `nil`; a 5-item section's `Omitted` read `0` instead of `5`; five pending items survived a 3-word budget instead of being dropped whole), restored byte-identical, reconfirmed green.

  Ten new tests: six pure `Pack`/`Render`/`WordEstimator` table/case tests in `internal/briefing/briefing_test.go` (every-section-fits, whole-section-drop-on-over-budget, dropping one section doesn't starve a later one, an empty section is included not dropped, `WordEstimator`'s own word-counting cases, `Render`'s "(nothing)" vs "(omitted: N)" distinction) plus four `Compose` integration tests in `internal/briefing/compose_test.go` against a real `providers.OpenIndex`-backed fixture brain (a full golden byte-for-byte render exercising all five sections including the Moved-forward lookback's inclusion/exclusion boundary and Watched's real spend-projection math; Blocked's queue-alert wiring under a real 60-item depth trip; a fresh brain still renders all five section headings, none dropped; a tiny word budget drops Needs You whole through the real `Compose` path, not just `Pack` in isolation).

  Disclosed, not built here: no `serenity brief` CLI verb yet (RFC 0001 §13.1 names `brief` as a CLI verb, but this task's own acc line scopes to the package -- "golden render on the fixture brain" -- not a CLI surface; the same "package-complete, CLI wiring left for later" precedent T4.3/T4.10/T4.12/T2.3/T2.4/T2.7/T2.21 all already established); no `serenity.yml` config surface for `WordBudget`/`MovedForwardLookback`, same disclosed gap `queue.Config`/`spend.Config` already carry. Validation: `gofmt -l .`, `go build ./...`, `go vet ./...` clean; `go test -race -timeout 600s ./...` green repo-wide; `golangci-lint run --timeout=10m ./...` 0 issues; fleet build lease held for the full ladder and released immediately after. Staging/production: N/A (new library package, no deployed service consumes it yet).
- [x] T2.18 Reconcile eval: verdict confusion matrix + contradiction-detection recall in evals/report.json  Owner: pool  Est: 60m  verifies: [UC-045, UC-014]  deps: [T1.13, T2.2, T2.12]  acc: [report has a reconcile section with per-verdict P/R/F1; a deliberately missed temporal fixture appears as a false negative] (merged PR #123, 2026-09-07, rebase merge commit ffe8a630d02aea0c8c69624e1aa0be074731d6ca. New `internal/eval/reconcile` package scores `internal/reconcile.Detect` (T2.2) directly against a golden `(new claim, active-claim pool, expected verdict)` corpus -- no cached-predictions fixture, unlike `internal/eval/direction`'s DIRECTION section, because `Detect` is a pure deterministic function (no I/O, no mutation), so every CI run measures the exact shipped code rather than a fixture that can drift stale. `Score` returns per-verdict P/R/F1 (`Report.VerdictConfusion`), the raw expected-vs-predicted count table (`Report.Matrix`), and contradiction-detection recall (`Report.Contradiction`, `internal/eval.ContradictionRecall`) -- a row counts as a contradiction exactly when its expected verdict is `conflict` or `window_close`, the same condition `reconcile.Engine.Process` itself uses to decide whether to stage a DISPOSITION item, so the eval's notion of "contradiction" can never drift from what the real routing logic treats as needing human review. New 16-row corpus `evals/corpora/reconcile/labels/` covers all five verdicts `Detect` can return (`precept_conflict` excluded by design -- T2.2's own disclosed gap, `Detect` never returns it, so a fixture targeting it would show a fixed, misleading 0% rather than a real finding). `R-014` is the deliberately-included false negative the acc line names: it expects `window_close` but writes `valid_from` in RFC3339 (`2025-06-01T00:00:00Z`) instead of `internal/reconcile`'s own `validDateLayout` (`2006-01-02`, calendar-day only) -- `parseValidFrom`'s `time.Parse` rejects it, so `Detect` falls through to `conflict`, a real, currently-existing, previously-undiscovered bug in `internal/reconcile` (not fabricated for the test), substantial enough that it was minted as its own follow-up, T2.23. Real end-to-end run: `window_close P=1.000 R=0.750 F1=0.857 (tp=3 fp=0 fn=1)` -- the real R-014 false negative -- while `contradiction P=1.000 R=1.000 F1=1.000 (tp=8 fp=0 fn=0)` stays perfect, correctly distinguishing "flagged as needing review at all" from "flagged with the exact right verdict". Wired into `internal/eval/runner` (`Config.ReconcileCorpusDir`/`Report.Reconcile`, unconditional in every `Mode` including live, unlike Direction which errors on `ModeLive` -- proven by `TestRunReconcileWorksInLiveMode`) and `cmd/eval-runner` (`-reconcile-corpus` flag, no fixture flag needed) and `.github/workflows/ci.yml`'s `eval-cached` job. Also corrected a stale `Report.Contradiction` package-doc comment that still described contradiction detection as unimplemented even though T2.2 shipped a real detector; the placeholder itself is still correct for the *primary ava corpus specifically* (it carries no claim-pair fixtures), now explained rather than left misleading, with a pointer to `Report.Reconcile` for the real number. Real red->green spot-check: removed the `fp`/`fn` accumulation from `Score`'s confusion-counting loop, confirmed both the synthetic-fixture test and the real-committed-corpus integration test failed with the exact expected `window_close.FN = 0, want 1` mismatch, restored byte-identical, reconfirmed green. Disclosed, unrelated flake found during a full-repo `go test -race`: `internal/server`'s `TestTokenRotationInvalidatesOldToken` fails under `-race` ("server did not shut down within 2s") but passes cleanly without it (0.27s) -- confirmed `internal/server` untouched by this PR, a pre-existing race-detector-overhead timing flake against a hardcoded 2s deadline, not a regression. Validation: `gofmt -l .`, `go build ./...`, `go vet ./...` clean before and after a rebase onto a moved main (T2.15 landed mid-task); `go test -race -count=1 ./internal/eval/... ./internal/reconcile/... ./cmd/eval-runner/... ./internal/cli/...` green; `golangci-lint run` on the three touched/new packages 0 issues.)
- [x] T2.21 Tombstone cascade: source tombstone -> retraction proposals -> accept rewrites fences/shards and rebuilds  Owner: pool  Est: 75m  verifies: [UC-035]  deps: [T1.2, T2.1, T2.3]  acc: [tombstoning a fixture source creates one retraction item per sole-provenance claim; accept marks them retracted in files, rebuild drops them from the index, and multi-provenance claims are demoted not retracted] (merged PR #115, 2026-09-07, rebase merge commit b24f0d917294e31d6ad6ee0dad8c3483bdf609b8. `internal/supersede/tombstone.go`: `Writer.TombstoneCascade` walks every shard-tier claim citing a tombstoned source (T1.2's `store.SourceStore.Tombstone`) and, per claim, checks whether another still-live claim shares the same `(subject, predicate, object_key)` from a different, non-tombstoned source. No corroboration -> stages exactly one `KindTombstone` disposition item (a human must accept before anything is retracted, matching `internal/ladder`'s `never_automate: ["tombstones", ...]`); corroboration found -> demotes the claim immediately by reusing the existing `Apply` (T2.3) supersession path with a fresh, lower-confidence copy (`demoteFactor = 0.7`, a disclosed policy constant, not an RFC-specified number), no human gate, since demotion only ever lowers confidence and never removes a row. `Writer.ApplyDisposedTombstone` applies an accepted item's verdict via the new `Writer.Retract`, which appends a shard retraction line reusing the target claim's own id (the shard-tier lifecycle-tombstone convention `store.ShardStore.Append`/`store.ResolveHeadLines` already special-case) and regenerates the derived fence head -- rebuild then drops the retracted claim from the index for free, since `ResolveHeadLines` treats a retracted id as dead and `index.Rebuild` only ever indexes resolved shard heads. Acc line verified directly with two tests: one seeds a real fixture source via `SourceStore.Write`, tombstones it, confirms exactly one retraction item is staged, accepts it, and -- the same wipe-and-rebuild move T2.4's own acc-line test used -- wipes `.serenity` and rebuilds purely from committed repo bytes, confirming the retracted claim's value and id are both absent; the other seeds two independent claims corroborating the same fact from two different sources, tombstones one, and confirms the citing claim is demoted (not retracted) and no disposition item is staged at all. Real red->green spot-check: inverted the corroboration-detection condition, confirmed the multi-provenance test fails with the exact expected count mismatch, restored byte-identical, reconfirmed green. Validation: gofmt/build/vet clean before and after a rebase onto a moved main (T2.14 landed mid-task); `go test -race -count=1` on internal/supersede, internal/store, internal/disposition, internal/index, internal/cron, internal/consolidate, internal/gate (T0.2's file-first CI gate -- writes only ever go through the already-allowlisted writer.Shard/writer.Fence) all green; golangci-lint on internal/supersede 0 issues. Disclosed, not built here: fence-tier claims are out of scope (SourceStore.Tombstone's own doc names why -- fence rows render provenance down to a src cell with no sha256, so a source-sha scan can never find them); no `SourceStore.Delete` exists yet, so "tombstoning a source" here means calling Tombstone against an already-written source's sha, not removing its bytes from disk; no CLI wiring (mirrors T2.3/T2.4's own original package-only scope).)
- [x] T2.22 M2 exit: run the RFC AC checklist end to end on a laptop and record it  Owner: pool  Est: 45m  verifies: [UC-014, UC-015, UC-017, UC-026, UC-019]  deps: [T2.3, T2.4, T2.5, T2.8, T2.11, T2.13]  acc: [docs/evals/m2-report.md records each of the five M2 ACs with the command run and observed output] (PR #114, 2026-09-07; docs/evals/m2-report.md -- all five RFC §17 M2 AC clauses run for real on this laptop and recorded: AC1/UC-014 (`TestContradictingBalanceFixtureProducesExactlyOneConflictItem`, internal/reconcile, real SQLite-backed disposition store) -> exactly one KindReconcile item carrying both provenances; AC2/UC-015 (`TestApplyShardTierSupersedingLineAndRegeneratedHead`, internal/supersede, real temp git repo) -> one appended shard line with `supersedes`, regenerated fence head -- since the existing test only asserts diff content/line-count rather than printing it, a standalone (uncommitted, deleted after use) driver reran the identical fixture and captured the literal `git diff` text the RFC AC names as the observable, included verbatim in the report; AC3/UC-017 (`TestMergeRepointsEveryClaimAndRendersOnePage` + `TestUndoRestoresPriorStateByteIdentically`, internal/entities) -> merge then undo is the round trip, both real writes through internal/writer; AC4/UC-026 (`TestTwoClientDisposeRaceExactlyOneWins -race`, internal/disposition, single package so no fleet build lease needed) -> 100 iterations, two real goroutines, exactly one winner/loser each time, zero data races; AC5/UC-019 (`TestDefaultConfigMatchesCommittedCalibrationReport`, internal/ladder) -> `evals/calibration/report.json` exists (165 KB, 180-cell sweep) and `ladder.DefaultConfig().Default` equals its `Chosen` field-for-field (`{min_dispositions:50, min_accept:0.98, sample_rate:0.05}`, RFC §10.3's original prior, confirmed not changed). All five clauses clear; no remediation tasks filed, unlike M1's report (docs/evals/m1-report.md). **M2 is exit-verified.**)
- [x] T2.23 Fix `internal/reconcile.parseValidFrom` rejecting RFC3339-formatted dates, which misclassifies a genuine temporal-supersession case as a flat conflict  Owner: pool  Est: 30m  verifies: [UC-014]  deps: [T2.2]  acc: [an input with a RFC3339 `valid_from` (e.g. `2026-09-07T00:00:00Z`) parses successfully; the R-014 fixture from T2.18's reconcile eval corpus (evals/corpora/reconcile/labels/R-014.yaml), which currently scores as a false negative in `evals/report.json`'s reconcile section because this case is misclassified as `window_close` conflict instead of recognized temporal supersession, now scores correctly; T2.18's eval-runner re-run shows `window_close.FN == 0`] (PR #135, 2026-09-07) -- `parseValidFrom` now tries a small ordered list of layouts (`validDateLayouts`) instead of one hardcoded `validDateLayout`: the existing calendar-day-only `"2006-01-02"` first (every existing fixture/writer uses it), then `time.RFC3339` second, closing the exact gap T2.18 disclosed and minted this task from. No caller-visible signature change -- `parseValidFrom`'s own `(time.Time, bool)` contract, `detectPair`'s temporal branch, and every other reconcile.go code path are untouched; the fix is entirely inside the two-layout parse loop. Updated the three tests/fixtures that encoded the old buggy behavior as their expected outcome, since leaving them unchanged would have shipped this fix red: `internal/eval/reconcile/report_test.go`'s `TestScoreMisclassifiedRowAppearsAsFalseNegative` (T2.18's synthetic FN-detection-machinery test) switched its fixture from an RFC3339 `ValidFrom` -- no longer unparseable post-fix -- to a genuinely-unparseable garbage format (`"06/01/2025"`), preserving the test's actual purpose (proving `Score` surfaces a misclassified row as FN) independent of this specific bug; that same file's `TestScoreCommittedCorpusHasReconcileSectionAndRealFalseNegative` renamed to `...AndNoFalseNegatives` and its assertions flipped from `FN >= 1` to `FN == 0` -- it now guards the fix as a regression test, not a documented gap; `internal/eval/runner/runner_test.go`'s `TestRunScoresReconcileSectionAlongsidePrimaryCorpus` assertion flipped the same way. `evals/corpora/reconcile/labels/R-014.yaml`'s rationale field rewritten to describe the fix (kept in the corpus, not deleted, as a regression guard: a future change that narrows `parseValidFrom` back to one layout should reproduce this exact false negative); its checksum re-pinned via the corpus's own documented `go run evals/corpora/reconcile/gen_manifest.go` tool, not hand-edited. Three new tests directly in `internal/reconcile/reconcile_test.go`: `TestParseValidFromAcceptsRFC3339` (the acc line's own parse clause verbatim), `TestParseValidFromStillRejectsGarbage` (guards against a fix that widens acceptance to anything), `TestRFC014FixtureNowProducesWindowCloseNotConflict` (reproduces R-014's exact scenario at the `Engine.Process` level, independent of the eval corpus). Genuine red->green spot-check: reverted `validDateLayouts` to `[]string{validDateLayout}` (the pre-fix single-layout list), confirmed `TestParseValidFromAcceptsRFC3339` and `TestRFC014FixtureNowProducesWindowCloseNotConflict` failed with the exact expected mismatches (`ok = false, want true`; `Verdict = "conflict", want "window_close"`), restored, diff confirmed the restore was byte-identical to the fix, reconfirmed green. Real end-to-end CLI run (`go run ./cmd/eval-runner -mode cached -reconcile-corpus evals/corpora/reconcile`) confirms the printed summary matches design: `window_close P=1.000 R=1.000 F1=1.000 (tp=4 fp=0 fn=0)` -- R-014 now a true positive alongside R-011/R-012/R-013, exactly the acc line's `window_close.FN == 0` clause, verified in the actual printed output, not only in test assertions. Validation: `gofmt -l .`, `go build ./...`, `go vet ./...` clean; `go test -race -timeout 600s ./...` green repo-wide; `golangci-lint run --timeout=10m ./...` 0 issues; fleet build lease held for the full ladder and released immediately after. Staging/production: N/A (bug fix inside an existing pure-function package, no deployed service).
