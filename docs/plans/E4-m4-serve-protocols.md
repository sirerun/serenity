# E4 -- M4: serve + protocols (MCP/HTTP with auth, MEMORY_VERBS conformance, DISPOSITION v1, DIRECTION v1, spend ceiling)
Acceptance: RFC section 17 M4 AC: `gbrain protocol conformance` passes against Serenity; an external Claude Code session answers "what did we decide about X?" citing two sources on the Ava corpus; a ceiling trip forces the approval path; all endpoints reject unauthenticated calls.
fidelity: executable

Design anchors: RFC 8 (all three protocols), 9 (spend), 12, 13.1 (no privileged internal path), 14 (daemon exposure). Contracts: gbrain MEMORY_VERBS_v1.md at dndungu/gbrain@d35c9c9e441e (five verbs, uniform error envelope with populated `suggestion`, integer protocol_version, Streamable HTTP MCP at /mcp with `Authorization: Bearer`, stdio; conformance is a live write test that seeds `people/conformance-<marker>` when put_page exists and ships as data at test/fixtures/memory-verbs/cases.json). Decisions: docs/adr/010.

### Wave 4a (deps on E2/E3 exits; 6 agents)

- [x] T4.1 `serenityd` core (`internal/server`): lifecycle, tickers for sweep/consolidate/decay/revisit/slo, structured logs with job ids, pidfile, graceful shutdown  Owner: pool  Est: 90m  verifies: [UC-018, UC-044]  deps: [T2.19, T2.14, T3.10]  acc: [start on a fixture brain, fire one tick, observe sweep side effects; SIGTERM completes in-flight writer jobs and exits within 2s with no partial file; a second instance refuses to start; the file-first gate allowlist covers internal/server] (merged PR #140, 2026-09-07, rebase merge commit `ec4a524`) -- new `internal/server/daemon.go`: `Daemon` embeds `internal/cron`'s existing job functions on an internal ticker per ADR 006 and `internal/cron`'s own package doc ("serenityd embeds these same job functions on an internal ticker"), owning no job logic of its own -- every tick calls `cron.Run(ctx, job, root, clock)` unchanged. `Ticker` interface (`C()`/`Stop()`), injectable via `DaemonConfig.NewTicker` (same shape as `internal/cron.Clock`), so tests fire one tick deterministically. `DefaultSchedule()` mirrors `docs/operator/scheduling.md`'s cadence table (sweep/slo 15min, consolidate nightly, decay/revisit weekly). Pidfile-based single-instance guard (`<root>/.serenity/serenityd.pid`, POSIX signal-0 liveness check, `ErrAlreadyRunning`, stale-pidfile overwrite) -- plain `os` file I/O, not a canonical brain-repo or index write, so no `internal/gate` allowlist entry was needed (confirmed: `TestFileFirstGate/clean_tree` passes with this package in the tree). Graceful shutdown waits (`sync.WaitGroup`) for in-flight jobs before removing the pidfile; `log/slog` structured logging with a per-run job id (no logging convention existed anywhere in the repo before this -- `log/slog` chosen as the stdlib default, consistent with "standard-library Go in the engine"). Real bug caught by the shutdown acc-clause test during development: `runOnce` originally ran the job body on the daemon's own shutdown ctx, so canceling it for graceful shutdown also aborted the in-flight job's `database/sql`-backed writes mid-flight (item stuck `pending`/`DeferCount 0` instead of completing) -- fixed by running the job body on `context.Background()`, decoupled from the loop's shutdown signal, which now gates only whether the loop waits for *another* tick. All four acc clauses covered by tests against real fixture brains: `TestDaemonFiresTickAndRunsSweep` (one manual tick, real sweep transition), `TestDaemonShutdownWaitsForInFlightJobAndExitsQuickly` (shutdown requested from inside the in-flight job via a test-only hook, asserts full completion + <=2s + pidfile removed), `TestDaemonSecondInstanceRefusesToStart` (`ErrAlreadyRunning`), and the gate suite run by hand against the new package. Two real red->green spot-checks: the ctx bug above, and a deliberate `processAlive` mutation (forced `false`) that made the second-instance test hang instead of pass, restored + reconfirmed byte-identical + green. `gofmt`/`go build`/`go vet` clean, `go test -race -count=1 -timeout 600s ./...` green repo-wide, `golangci-lint run --timeout=10m ./...` 0 issues. Disclosed scope: no `serenity serve` CLI verb, `Daemon` not wired to `internal/server`'s existing HTTP `Server`/`Listen`/`Serve` (T4.3) -- T4.1's own deps exclude T4.3/T4.4, and T4.4's own disclosed-scope note names exactly this gap; that wiring is separate, later, unclaimed scope.
- [x] T4.18 Export `pkg/serenity` read-only facade for embedding consumers (Sire): `Open(brainPath string, opts ...Option) (*Brain, error)` read-only handle; `(*Brain).CheckPlan(ctx, actions []Action) (Verdict, error)` equal to `serenity check --json --actions`; `(*Brain).Recall(ctx, q string, budget Budget) (RecallResult, error)` over `internal/search.Search` (T1.11) and `internal/compose` (T1.12); the facade imports internal packages and adds no logic -- it calls exactly the functions `internal/cli` calls (`check.New`/`Matcher.Match` at internal/cli/check.go:86-100, `search.Search` at internal/cli/search.go:55, `compose.New`/`Composer.Ask` at internal/cli/ask.go:64-65); the read-only ledger handle is `direction.NewStore(root, nil)` plus a nil-queue guard in `internal/direction` so any write path returns an error instead of dereferencing nil; a writer entry point is deliberately NOT exported (writes stay in the daemon/CLI, ADR 004; one brain-writer process per brain, ADR 012); `Brief` is T4.19  Owner: pool  Est: 75m  verifies: [UC-027, UC-028, UC-038]  deps: [T3.7, T1.12]  acc: [go test ./pkg/serenity/ green; a drift test proves CheckPlan output deep-equals `serenity check --json --actions` on normalized JSON over testdata/brain-fixture; an AST test asserts pkg/serenity never imports internal/writer and never calls a direction.Store mutator (Create/Put/Delete/CreateDraft/Confirm/Supersede/Answer); a test proves every direction.Store mutator on a nil-queue store returns an error, not a panic; `go doc ./pkg/serenity` lists exactly Open, Brain, Option, the CheckPlan and Recall receivers and their wire types, nothing writable; docs/adr/012 is linked from the package doc as the protocol_version policy the facade is bound by]
  - Pitfall: cross-epic-startable now (both deps shipped; named alongside T4.3 in docs/plan.md section 5 per ADR 011 rule 5). `Recall` reuses `serenity search`/`ask`'s exact wiring, including the FTS-only fallback when no embedding model is pinned (internal/cli/search.go:49-52). `evals/corpora/direction` is a (plan, constraint set, verdict) label set, not a brain (T3.16 records that driving check_plan over it needs an adapter), so the drift corpus is the brain fixture; T4.19 extends the drift test with T4.13's transcripts once they exist.
- [x] T4.3 HTTP transport bound to loopback with bearer auth from the keychain; explicit LAN/Tailscale config with token + optional mTLS  Owner: pool  Est: 75m  verifies: [UC-030]  deps: [T0.3]  acc: [default config refuses a non-loopback bind with a named error at the listener; no token or wrong token -> 401 on every route including /healthz-authenticated variants; correct token -> 200; a grep of the repo and .serenity/ finds no token bytes; `serenity connect --rotate-token` invalidates the old token] (merged PR #56, 2026-09-03/04, rebase merge commit `6580d5a`) -- new `internal/server`: `Server` wraps `http.ServeMux`; `Listen()` refuses a non-loopback bind (`ErrNonLoopbackBindRefused`, a named error) unless `Config.AllowLAN` is set; every route registered via `Handle()` -- there is no other kind -- gets constant-time bearer-token auth read from `secrets.DaemonToken` per request (no caching, so rotation takes effect with no restart), including `/healthz`; `AllowLAN` additionally supports optional mTLS via `ClientCAFile`/`ServerCertFile`/`ServerKeyFile`, layered on top of the token check. New `serenity connect --rotate-token` (`internal/cli/connect.go`) mints a fresh token via `secrets.RotateDaemonToken`; bare `connect` reports token presence and honestly defers the MCP-stanza/hook-install behavior to T4.8 (separate, later, still-blocked task), not a silent no-op. New `config.Server` (bind/allow_lan/mTLS file paths) + `server.FromBrainConfig`; `internal/secrets` gained the token lifecycle (`DaemonToken`/`EnsureDaemonToken`/`RotateDaemonToken`/`Service`/`MockForTesting`). `docs/operator/server.md` (new) + a `docs/threat-model.md` cross-link. All five acc clauses covered by tests hitting a real listener over real HTTP, not internal calls: `TestListenRefusesNonLoopback`/`TestListenAcceptsLoopback`/`TestListenExplicitLANOptIn` (loopback refusal both directions), `TestBearerAuthEveryRoute` (401/401/200 across two routes including `/healthz`), `TestTokenRotationInvalidatesOldToken` + `TestConnectRotateTokenInvalidatesOldToken` (old token 401s post-rotation, new token 200s, no restart), `TestConnectNeverWritesTokenToDisk` (greps a fresh brain-repo tree for both token values, asserts absent). Recovery note: this task's worktree was found orphaned (claimed 2026-08-31 by a linux/DGX session, ~2 days past the 4h claim TTL, no PR) with substantial real progress already in it -- rescued (committed, pushed) rather than discarded per the pool's orphan-worktree protocol, then a dispatched subagent finished the remaining acc-clause test coverage; the subagent hit an unrelated auth fault right at PR-creation and went idle, so the coordinator session took over from the (already-complete, re-verified) working tree to open PR #56 and ship it. `go build`/`go vet`/`gofmt -l` clean, `golangci-lint run ./...` (whole-repo, no path args -- docs/lore.md L-0004) 0 issues, `go test -race -count=1 ./...` green repo-wide, all 8 PR checks green. Deviation: kazi is on PATH but the post-hoc `kazi apply --check --json` observe gate was skipped given the full green ladder already in hand, same precedent as T3.7/T3.16. Staging/production: N/A (CLI/library transport code, no deployed daemon yet -- same as T0.11, T3.7, T4.18).
- [x] T4.10 Spend ceiling + projection (`internal/spend`): per-day aggregation, monthly ceiling default $50, transactional check-and-record, approval item on trip, projection in status and briefing Watched  Owner: pool  Est: 75m  verifies: [UC-029]  deps: [T1.7, T2.1]  acc: [fixture ledger -> golden projection numbers; a judgment call that would cross the ceiling creates one approval item and does not run; accepting the item runs it; two concurrent calls cannot overshoot (property test on the check-and-record transaction)] (merged PR #119, 2026-09-07, rebase merge commit 9d019525a47d4241186c69c8b13cf0e106ddae87. New `internal/spend`, the policy layer over T1.7's already-shipped `spend_ledger` runtime table (`internal/index`'s `SpendRow`/`RecordSpend`/`SpendRows`): `ProjectMonth`/`MonthToDate`/`DailyTotals` compute month-to-date spend and a linear projection of the current calendar month's total (month-to-date / days elapsed so far * days in the month -- a disclosed policy choice, like T2.21's `demoteFactor`: the RFC names the required output, "projected spend," not a specific method). `Checker.CheckAndRecord` is the transactional gate: a not-yet-recorded judgment-tier row is recorded when month-to-date spend would stay at or under the ceiling, or -- "a judgment call that would cross the ceiling creates one approval item and does not run" -- exactly one `KindEffect` disposition item is staged and nothing is recorded when it would cross it, the whole read-check-write sequence held under one mutex so concurrent calls cannot together overshoot (the same shape T2.8 established for `disposition.Store.Dispose`'s own race). `Checker.ApplyDisposedEffect` applies an accepted item's verdict by decoding the withheld row from the item's own payload and recording it -- "accepting the item runs it." `serenity status`'s existing spend line (T1.17, `internal/cli/status.go`) gains `month_to_date`/`projected_month`/`ceiling` fields; its golden test's expected numbers were hand-verified ($0.0021 + $0.1234 = $0.1255 month-to-date on day 28 of a 31-day month projects to $0.1389). Real red->green spot-check: removed the mutex from `CheckAndRecord`, ran the concurrency property test five times uncached, confirmed a real overshoot every run (12-20 of 20 goroutines recorded against a ceiling that should admit exactly 5), restored, reconfirmed green. Validation: gofmt/build/vet clean before and after a rebase onto a moved main (a docs-only commit landed mid-task); `go test -race -count=1` on internal/spend, internal/cli, internal/index, internal/disposition, internal/router, internal/gate (T0.2's file-first CI gate -- the spend ledger is runtime-only state, so writes go through `index.SQLite.RecordSpend` directly, the same pattern `internal/disposition` already uses for its own runtime tables), internal/config all green; golangci-lint on internal/spend and internal/cli 0 issues. Disclosed, not built here: not wired into `config.Config`/`serenity.yml` (`internal/index/rebuild.go` already imports `internal/config`, so embedding `spend.Config` there the way T2.10's `ladder.Config` is embedded would create an import cycle `config -> spend -> index -> config`; `spend.Config` stays a plain Go constructor argument, "configurable" at the Go level, not yet at the YAML level); the briefing's Watched section is untouched since `internal/briefing` (T2.17) has not shipped yet -- there is nothing to wire into on that side; no live wiring into `router.Router.Complete`'s actual call path, package-only, mirroring T2.3/T2.4/T2.7/T2.21's own precedent.)
- [ ] T4.7 JSON Schemas for every protocol object + `serenity protocol --json`  Owner: pool  Est: 75m  verifies: [UC-038]  deps: [T3.7, T2.17]  acc: [docs/protocol/schemas/*.json carry $id and protocol_version; one Go struct per wire object feeds both CLI --json and the server (asserted by a reflection test); a draft-2020-12 validator accepts every schema and every CLI --json output validates against its schema in CI]
- [x] T4.12 Event log with persisted monotonic cursors (`internal/events`) shared by SSE and stdio notifications  Owner: pool  Est: 60m  verifies: [UC-042]  deps: [T2.1]  acc: [cursors survive a restart; replay from cursor N returns exactly events > N; at-least-once delivery is asserted by a dropped-consumer test] (merged PR #88, 2026-09-07; `events` claims the RuntimeTables schema shell the way T2.1/T1.7 did; Cursor lives in the marshaled payload so persistence-across-restart needs no separate counter row -- `max(Cursor)+1` under a mutex; found and applied the same fix class PR #84/T2.8 surfaced: the read-then-allocate needs a lock or concurrent Append calls collide. Disclosed scope: no live producer wired yet -- T4.2/T4.4/T4.6 (the protocol servers this log serves) don't exist)
- [ ] T4.13 Conformance fixture set under testdata/conformance/: vendored gbrain memory-verbs cases.json plus Serenity DISPOSITION and DIRECTION transcripts, checksum-frozen  Owner: pool  Est: 60m  verifies: [UC-038, UC-025]  deps: [T4.7]  acc: [testdata/conformance/{memory_verbs,disposition,direction}/ exist with a MANIFEST of sha256s; CI fails if a transcript changes without its manifest entry; gbrain's cases.json is vendored with its license line]

### Wave 4b (6 agents)

- [x] T4.2 MCP stdio transport (`internal/server/mcp`): initialize handshake, JSON-RPC framing, logs to stderr only  Owner: pool  Est: 75m  verifies: [UC-025]  deps: [T4.1]  acc: [handshake test passes; an unknown tool returns the MCP-spec error; malformed JSON-RPC returns a spec error without a crash; a test asserts stdout carries only protocol frames]
- [ ] T4.5 MEMORY_VERBS v1 server: recall, remember, entity, synthesize, forget with the gbrain envelope (protocol_version, evidence, provenance, budget meta, cost, enumerated errors with suggestion) plus optional confidence, claim_id, superseded_by  Owner: pool  Est: 90m  verifies: [UC-025]  deps: [T4.2, T4.3, T1.11, T1.12]  acc: [every verb response validates against the vendored gbrain shape; remember with empty provenance -> provenance_required with a populated suggestion; entity miss -> found:false not an error; forget is idempotent; recall budget_used <= budget_tokens with dropped_count consistent]
  - Pitfall: remember writes through the writer queue and reconcile (never id-equality dedup); synthesize without a model returns `unavailable` with a fix.
- [x] T4.4 DISPOSITION v1 server: list_pending (kinds, expiring_before, group), dispose (idempotency_key, already_disposed), capture, subscribe (SSE + long-poll, Last-Event-ID resume)  Owner: pool  Est: 90m  verifies: [UC-026, UC-034, UC-042]  deps: [T2.1, T2.20, T4.12, T4.3]  acc: [cursor walk of 120 items in pages of 50 terminates; replayed dispose returns a byte-identical response; reject without note -> protocol error; dropping the SSE connection mid-stream and resuming replays exactly the missed events; parked items appear only with the parked filter] (PR #128, 2026-09-07) -- new `internal/server/disposition`: `Handlers` (over `*disposition.Store` (T2.1/T2.5/T2.6/T2.20/T2.21) and `*events.Store` (T4.12)) implements list_pending/dispose/capture/subscribe as a thin transport layer, adding no queue logic of its own -- the same domain Stores `serenity inbox`/`serenity capture` already call, no privileged internal path. `Register(Registrar)` wires four routes onto any `Handle(pattern, http.Handler)` target (`*server.Server` in production, satisfied structurally the same way `internal/direction.Completer` decouples from `*router.Router`). RFC 0001 §8.2 gives DISPOSITION v1's operations but not its exact wire shapes (unlike §8.1 MEMORY_VERBS's own "gbrain envelope"); `docs/protocol/DISPOSITION_v1.md` (T4.16) hasn't shipped, so this task defines its own JSON request/response shapes, documented per-handler in the source rather than waiting on a spec that doesn't exist yet. list_pending paginates via a plain numeric-offset cursor (disclosed: correct for a walk over a snapshot, not for one interleaved with concurrent writes -- out of this task's acc-line scope) and adds two wire concepts the RFC's prose doesn't name: a `parked` boolean (the wire equivalent of `serenity inbox --parked`) and `group` (collapses items sharing a non-empty GroupID into one row carrying every member -- items are disposed individually regardless, "each recorded individually for the ladder"). dispose requires `idempotency_key` at the wire layer (RFC 0001 §8.2's own parameter list gives it no `?`, unlike edited_payload/note, even though the underlying Store treats an empty key as "no idempotency check") and applies a `group_id` dispose to every member as its own `Store.Dispose` call, never a bulk write; a genuinely new (non-replayed, non-already-disposed) dispose or a successful capture each publish one event. **`internal/server/disposition` is `internal/events.Store`'s first live producer** -- T4.12 shipped with none ("T4.2/T4.4/T4.6... do not exist" was its own disclosed scope at the time). subscribe selects SSE via an `Accept: text/event-stream` request header (matching a browser EventSource, which sets it automatically) and falls back to long-poll otherwise; resume prefers the `Last-Event-ID` header over a `cursor` query param, both ultimately replayed through `events.Store.Replay`. Real bug found and fixed while building this, outside this task's own files: `internal/gate`'s file-first CI gate (T0.2/T0.13) matches by bare AST selector name ("Append") regardless of receiver type, to catch any canonical-brain-repo write bypassing the writer queue -- `events.Store`'s own public method was still named plain `Append` (its `Backend.AppendEvent` already avoided the same collision), and `internal/disposition.Store` deliberately named its own equivalent `AppendDispositionHistory` for exactly this reason. Nothing had ever called the bare `Append` from a non-test file before this task (T4.12's "no live producer" scope), so the collision was latent until now. Fixed by renaming `events.Store.Append` -> `AppendEvent` (its only two callers before this task were its own test file and this task's new package, both updated) rather than allowlisting this package in the gate, which would also silently exempt any future genuine canonical write it might grow. Also found and fixed, unrelated: `internal/server/server_test.go`'s own `startTestServer` cleanup helper used a 2s timeout to wait on a server whose internal graceful-shutdown grace period is 5s -- an intermittent flake (reproduced in ~2 of 3 standalone `-race` runs on this machine), not caused by this task but blocking its own whole-repo `go test -race ./...` requirement; widened to 8s, the same fix applied to this task's own new test file's identical helper. Two genuine red->green spot-checks: disabled the `req.Parked` branch in list_pending, confirmed `TestListPendingParkedFilter` fails (the parked view returns the pending item instead of the parked one), restored, reconfirmed green; disabled the `idempotency_key` empty-check in dispose, confirmed `TestDisposeMissingIdempotencyKeyReturnsProtocolError` fails (200 instead of the required 400), restored, reconfirmed green. Fourteen new tests in `internal/server/disposition/disposition_test.go`, all against a real `*server.Server` listener over real HTTP (T4.3's own established convention) with a real `index.Open` SQLite-backed `*disposition.Store`/`*events.Store` pair -- covering all five acc-line clauses plus capture's happy/empty paths, group dispose (every member individually + its own events), a group_id with no members (404), missing idempotency_key (400), a kind filter, group-row collapsing, long-poll's fast-return path, and subscribe's own auth requirement. Disclosed, not built here: not wired into a live `serenity serve` command -- T4.1 (`serenityd` core) hasn't shipped, so there is no daemon process to register these routes into in production, the same disclosed "package-complete, not yet live-wired" scope T4.3/T4.10/T4.12 already shipped with; no `protocol_version` wire envelope field, since RFC 0001 §8.2 doesn't call for one the way §8.1 does for MEMORY_VERBS; subscribe's SSE/long-poll both poll `events.Store` on a fixed interval (200ms default) rather than an event-driven wakeup, since no pub/sub primitive exists in this codebase yet -- correct, but a subscriber reads a fresh event up to one poll interval after it was published, never sooner. Full validation ladder green under the fleet build lease: `gofmt -l .` clean, `go build ./...`, `go vet ./...` clean, `go test -race -timeout 600s ./...` (whole repo, including `internal/gate`'s file-first and precept-immutability gates) green, `golangci-lint run --timeout=10m ./...` 0 issues.
- [ ] T4.6 DIRECTION v1 server: brief (token budget governs, caps 12/8/8/5, omitted counts, budget_estimator named), check_plan (schema-primary verdict), propose (lands in the queue, never mutates a precept)  Owner: pool  Est: 90m  verifies: [UC-027, UC-028, UC-041]  deps: [T2.17, T3.7, T4.3, T2.1]  acc: [budget 800 fills sections by priority and drops an overflowing section whole with omitted: N; a zero budget returns the minimal valid object; check_plan wire verdicts equal `serenity check --json` on the same fixture; propose(precept_draft) creates a disposition item and .dira/ hash is unchanged]
- [ ] T4.8 `serenity connect claude`: MCP config stanza + hook installing `serenity check` as a pre-plan gate, idempotent, token never written to the config file  Owner: pool  Est: 60m  verifies: [UC-031]  deps: [T4.2, T4.3, T3.7]  acc: [run against a temp config dir -> valid stanza and hook; re-run is a no-op; a grep of the written files finds no token; `--print` matches the stanza]
- [ ] T4.11 Server security battery: oversized payloads, deep nesting, path traversal, replayed idempotency keys, redaction before cloud synthesis over the wire, go-fuzz on parsers 30s in CI  Owner: pool  Est: 75m  verifies: [UC-030, UC-043, UC-046]  deps: [T4.3, T4.4, T4.5]  acc: [fuzz targets run without crash; traversal fixture -> 4xx; the cloud prompt snapshot for synthesize contains redaction placeholders; a tool argument containing "create precept" leaves .dira/ unchanged]

### Wave 4c (5 agents)

- [ ] T4.9 CLI vs protocol drift tests: search/recall, ask/synthesize, check/check_plan, inbox dispose/dispose, brief/brief over one fixture  Owner: pool  Est: 60m  verifies: [UC-038]  deps: [T4.4, T4.5, T4.6]  acc: [deep-equal on normalized JSON for each pair; a temporary one-sided field addition fails the test (recorded in the test comment)]
- [ ] T4.14 Run `gbrain protocol conformance --target http://127.0.0.1:<port>/mcp --token <t>` in CI against a throwaway fixture brain (Bun installed in the job)  Owner: pool  Est: 60m  verifies: [UC-025]  deps: [T4.5, T4.13]  acc: [the job passes the SHAPE, CONTRACT BEHAVIOR, and ROUND-TRIP arms; the marker entity `people/conformance-<marker>` is cleaned up after the run; the job is a required check]
- [ ] T4.15 Serenity conformance command: `serenity protocol conformance --target <url>` replaying testdata/conformance transcripts for all three protocols  Owner: pool  Est: 60m  verifies: [UC-038]  deps: [T4.13, T4.4, T4.6]  acc: [go test ./internal/conformance boots the server and replays every transcript; a deliberately broken build (env flag) fails the suite]
- [ ] T4.16 Protocol documents: docs/protocol/MEMORY_VERBS_v1.md (adopted, credited), DISPOSITION_v1.md, DIRECTION_v1.md with governance section (maintainer arbitrates via in-repo RFC, additive-forever, fixtures location) and the kill criterion  Owner: pool  Est: 75m  verifies: [UC-038]  deps: [T4.7]  lane: agent  acc: [three documents exist; each lists every operation, every field with type, every error code, the governance section, and links its schema $id; a docs test asserts each schema $id appears in its document]
- [ ] T4.19 `(*Brain).Brief(ctx, budget Budget) (Brief, error)` on the `pkg/serenity` facade, calling T4.6's brief packer with no logic of its own; extend T4.18's drift test with every T4.13 DIRECTION transcript (check_plan and brief)  Owner: pool  Est: 45m  verifies: [UC-027, UC-038]  deps: [T4.18, T4.6, T4.13]  acc: [Brief output deep-equals the DIRECTION `brief` wire object on normalized JSON for every budget in the T4.13 brief transcripts; CheckPlan deep-equals `serenity check --json --actions` over every T4.13 check_plan transcript; `go doc ./pkg/serenity` gains exactly the Brief receiver and its wire type; docs/protocol/DIRECTION_v1.md (T4.16) gains a "consumer surfaces" note recording that the facade is bound by the same protocol_version policy as the wire, linking ADR 012; the T4.18 AST test still passes]
- [ ] T4.17 M4 exit: external Claude Code session answers "what did we decide about X?" with two citations on the Ava corpus; ceiling trip forces approval; auth sweep  Owner: David  Est: 60m  verifies: [UC-025, UC-029, UC-030, UC-031]  kind: human  deps: [T4.8, T4.10, T4.14]  acc: [docs/evals/m4-report.md records the session transcript excerpt with two source citations, the ceiling-trip approval item id, and an unauthenticated curl per endpoint returning 401]
