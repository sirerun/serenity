# E4 -- M4: serve + protocols (MCP/HTTP with auth, MEMORY_VERBS conformance, DISPOSITION v1, DIRECTION v1, spend ceiling)
Acceptance: RFC section 17 M4 AC: `gbrain protocol conformance` passes against Serenity; an external Claude Code session answers "what did we decide about X?" citing two sources on the Ava corpus; a ceiling trip forces the approval path; all endpoints reject unauthenticated calls.
fidelity: executable

Design anchors: RFC 8 (all three protocols), 9 (spend), 12, 13.1 (no privileged internal path), 14 (daemon exposure). Contracts: gbrain MEMORY_VERBS_v1.md at dndungu/gbrain@d35c9c9e441e (five verbs, uniform error envelope with populated `suggestion`, integer protocol_version, Streamable HTTP MCP at /mcp with `Authorization: Bearer`, stdio; conformance is a live write test that seeds `people/conformance-<marker>` when put_page exists and ships as data at test/fixtures/memory-verbs/cases.json). Decisions: docs/adr/010.

### Wave 4a (deps on E2/E3 exits; 6 agents)

- [ ] T4.1 `serenityd` core (`internal/server`): lifecycle, tickers for sweep/consolidate/decay/revisit/slo, structured logs with job ids, pidfile, graceful shutdown  Owner: pool  Est: 90m  verifies: [UC-018, UC-044]  deps: [T2.19, T2.14, T3.10]  acc: [start on a fixture brain, fire one tick, observe sweep side effects; SIGTERM completes in-flight writer jobs and exits within 2s with no partial file; a second instance refuses to start; the file-first gate allowlist covers internal/server]
- [ ] T4.18 Export `pkg/serenity` read-only facade for embedding consumers (Sire): `Open(brainPath string, opts ...Option) (*Brain, error)` read-only handle; `(*Brain).CheckPlan(ctx, actions []Action) (Verdict, error)` equal to `serenity check --json --actions`; `(*Brain).Recall(ctx, q string, budget Budget) (RecallResult, error)` over `internal/search.Search` (T1.11) and `internal/compose` (T1.12); the facade imports internal packages and adds no logic -- it calls exactly the functions `internal/cli` calls (`check.New`/`Matcher.Match` at internal/cli/check.go:86-100, `search.Search` at internal/cli/search.go:55, `compose.New`/`Composer.Ask` at internal/cli/ask.go:64-65); the read-only ledger handle is `direction.NewStore(root, nil)` plus a nil-queue guard in `internal/direction` so any write path returns an error instead of dereferencing nil; a writer entry point is deliberately NOT exported (writes stay in the daemon/CLI, ADR 004; one brain-writer process per brain, ADR 012); `Brief` is T4.19  Owner: pool  Est: 75m  verifies: [UC-027, UC-028, UC-038]  deps: [T3.7, T1.12]  acc: [go test ./pkg/serenity/ green; a drift test proves CheckPlan output deep-equals `serenity check --json --actions` on normalized JSON over testdata/brain-fixture; an AST test asserts pkg/serenity never imports internal/writer and never calls a direction.Store mutator (Create/Put/Delete/CreateDraft/Confirm/Supersede/Answer); a test proves every direction.Store mutator on a nil-queue store returns an error, not a panic; `go doc ./pkg/serenity` lists exactly Open, Brain, Option, the CheckPlan and Recall receivers and their wire types, nothing writable; docs/adr/012 is linked from the package doc as the protocol_version policy the facade is bound by]
  - Pitfall: cross-epic-startable now (both deps shipped; named alongside T4.3 in docs/plan.md section 5 per ADR 011 rule 5). `Recall` reuses `serenity search`/`ask`'s exact wiring, including the FTS-only fallback when no embedding model is pinned (internal/cli/search.go:49-52). `evals/corpora/direction` is a (plan, constraint set, verdict) label set, not a brain (T3.16 records that driving check_plan over it needs an adapter), so the drift corpus is the brain fixture; T4.19 extends the drift test with T4.13's transcripts once they exist.
- [ ] T4.3 HTTP transport bound to loopback with bearer auth from the keychain; explicit LAN/Tailscale config with token + optional mTLS  Owner: pool  Est: 75m  verifies: [UC-030]  deps: [T0.3]  acc: [default config refuses a non-loopback bind with a named error at the listener; no token or wrong token -> 401 on every route including /healthz-authenticated variants; correct token -> 200; a grep of the repo and .serenity/ finds no token bytes; `serenity connect --rotate-token` invalidates the old token]
- [ ] T4.10 Spend ceiling + projection (`internal/spend`): per-day aggregation, monthly ceiling default $50, transactional check-and-record, approval item on trip, projection in status and briefing Watched  Owner: pool  Est: 75m  verifies: [UC-029]  deps: [T1.7, T2.1]  acc: [fixture ledger -> golden projection numbers; a judgment call that would cross the ceiling creates one approval item and does not run; accepting the item runs it; two concurrent calls cannot overshoot (property test on the check-and-record transaction)]
- [ ] T4.7 JSON Schemas for every protocol object + `serenity protocol --json`  Owner: pool  Est: 75m  verifies: [UC-038]  deps: [T3.7, T2.17]  acc: [docs/protocol/schemas/*.json carry $id and protocol_version; one Go struct per wire object feeds both CLI --json and the server (asserted by a reflection test); a draft-2020-12 validator accepts every schema and every CLI --json output validates against its schema in CI]
- [ ] T4.12 Event log with persisted monotonic cursors (`internal/events`) shared by SSE and stdio notifications  Owner: pool  Est: 60m  verifies: [UC-042]  deps: [T2.1]  acc: [cursors survive a restart; replay from cursor N returns exactly events > N; at-least-once delivery is asserted by a dropped-consumer test]
- [ ] T4.13 Conformance fixture set under testdata/conformance/: vendored gbrain memory-verbs cases.json plus Serenity DISPOSITION and DIRECTION transcripts, checksum-frozen  Owner: pool  Est: 60m  verifies: [UC-038, UC-025]  deps: [T4.7]  acc: [testdata/conformance/{memory_verbs,disposition,direction}/ exist with a MANIFEST of sha256s; CI fails if a transcript changes without its manifest entry; gbrain's cases.json is vendored with its license line]

### Wave 4b (6 agents)

- [ ] T4.2 MCP stdio transport (`internal/server/mcp`): initialize handshake, JSON-RPC framing, logs to stderr only  Owner: pool  Est: 75m  verifies: [UC-025]  deps: [T4.1]  acc: [handshake test passes; an unknown tool returns the MCP-spec error; malformed JSON-RPC returns a spec error without a crash; a test asserts stdout carries only protocol frames]
- [ ] T4.5 MEMORY_VERBS v1 server: recall, remember, entity, synthesize, forget with the gbrain envelope (protocol_version, evidence, provenance, budget meta, cost, enumerated errors with suggestion) plus optional confidence, claim_id, superseded_by  Owner: pool  Est: 90m  verifies: [UC-025]  deps: [T4.2, T4.3, T1.11, T1.12]  acc: [every verb response validates against the vendored gbrain shape; remember with empty provenance -> provenance_required with a populated suggestion; entity miss -> found:false not an error; forget is idempotent; recall budget_used <= budget_tokens with dropped_count consistent]
  - Pitfall: remember writes through the writer queue and reconcile (never id-equality dedup); synthesize without a model returns `unavailable` with a fix.
- [ ] T4.4 DISPOSITION v1 server: list_pending (kinds, expiring_before, group), dispose (idempotency_key, already_disposed), capture, subscribe (SSE + long-poll, Last-Event-ID resume)  Owner: pool  Est: 90m  verifies: [UC-026, UC-034, UC-042]  deps: [T2.1, T2.20, T4.12, T4.3]  acc: [cursor walk of 120 items in pages of 50 terminates; replayed dispose returns a byte-identical response; reject without note -> protocol error; dropping the SSE connection mid-stream and resuming replays exactly the missed events; parked items appear only with the parked filter]
- [ ] T4.6 DIRECTION v1 server: brief (token budget governs, caps 12/8/8/5, omitted counts, budget_estimator named), check_plan (schema-primary verdict), propose (lands in the queue, never mutates a precept)  Owner: pool  Est: 90m  verifies: [UC-027, UC-028, UC-041]  deps: [T2.17, T3.7, T4.3, T2.1]  acc: [budget 800 fills sections by priority and drops an overflowing section whole with omitted: N; a zero budget returns the minimal valid object; check_plan wire verdicts equal `serenity check --json` on the same fixture; propose(precept_draft) creates a disposition item and .dira/ hash is unchanged]
- [ ] T4.8 `serenity connect claude`: MCP config stanza + hook installing `serenity check` as a pre-plan gate, idempotent, token never written to the config file  Owner: pool  Est: 60m  verifies: [UC-031]  deps: [T4.2, T4.3, T3.7]  acc: [run against a temp config dir -> valid stanza and hook; re-run is a no-op; a grep of the written files finds no token; `--print` matches the stanza]
- [ ] T4.11 Server security battery: oversized payloads, deep nesting, path traversal, replayed idempotency keys, redaction before cloud synthesis over the wire, go-fuzz on parsers 30s in CI  Owner: pool  Est: 75m  verifies: [UC-030, UC-043, UC-046]  deps: [T4.3, T4.4, T4.5]  acc: [fuzz targets run without crash; traversal fixture -> 4xx; the cloud prompt snapshot for synthesize contains redaction placeholders; a tool argument containing "create precept" leaves .dira/ unchanged]

### Wave 4c (5 agents)

- [ ] T4.9 CLI vs protocol drift tests: search/recall, ask/synthesize, check/check_plan, inbox dispose/dispose, brief/brief over one fixture  Owner: pool  Est: 60m  verifies: [UC-038]  deps: [T4.4, T4.5, T4.6]  acc: [deep-equal on normalized JSON for each pair; a temporary one-sided field addition fails the test (recorded in the test comment)]
- [ ] T4.14 Run `gbrain protocol conformance --target http://127.0.0.1:<port>/mcp --token <t>` in CI against a throwaway fixture brain (Bun installed in the job)  Owner: pool  Est: 60m  verifies: [UC-025]  deps: [T4.5, T4.13]  acc: [the job passes the SHAPE, CONTRACT BEHAVIOR, and ROUND-TRIP arms; the marker entity `people/conformance-<marker>` is cleaned up after the run; the job is a required check]
- [ ] T4.15 Serenity conformance command: `serenity protocol conformance --target <url>` replaying testdata/conformance transcripts for all three protocols  Owner: pool  Est: 60m  verifies: [UC-038]  deps: [T4.13, T4.4, T4.6]  acc: [go test ./internal/conformance boots the server and replays every transcript; a deliberately broken build (env flag) fails the suite]
- [ ] T4.16 Protocol documents: docs/protocol/MEMORY_VERBS_v1.md (adopted, credited), DISPOSITION_v1.md, DIRECTION_v1.md with governance section (maintainer arbitrates via in-repo RFC, additive-forever, fixtures location) and the kill criterion  Owner: pool  Est: 75m  verifies: [UC-038]  deps: [T4.7]  lane: agent  acc: [three documents exist; each lists every operation, every field with type, every error code, the governance section, and links its schema $id; a docs test asserts each schema $id appears in its document]
- [ ] T4.19 `(*Brain).Brief(ctx, budget Budget) (Brief, error)` on the `pkg/serenity` facade, calling T4.6's brief packer with no logic of its own; extend T4.18's drift test with every T4.13 DIRECTION transcript (check_plan and brief)  Owner: pool  Est: 45m  verifies: [UC-027, UC-038]  deps: [T4.18, T4.6, T4.13]  acc: [Brief output deep-equals the DIRECTION `brief` wire object on normalized JSON for every budget in the T4.13 brief transcripts; CheckPlan deep-equals `serenity check --json --actions` over every T4.13 check_plan transcript; `go doc ./pkg/serenity` gains exactly the Brief receiver and its wire type; docs/protocol/DIRECTION_v1.md (T4.16) gains a "consumer surfaces" note recording that the facade is bound by the same protocol_version policy as the wire, linking ADR 012; the T4.18 AST test still passes]
- [ ] T4.17 M4 exit: external Claude Code session answers "what did we decide about X?" with two citations on the Ava corpus; ceiling trip forces approval; auth sweep  Owner: David  Est: 60m  verifies: [UC-025, UC-029, UC-030, UC-031]  kind: human  deps: [T4.8, T4.10, T4.14]  acc: [docs/evals/m4-report.md records the session transcript excerpt with two source citations, the ceiling-trip approval item id, and an unauthenticated curl per endpoint returning 401]
