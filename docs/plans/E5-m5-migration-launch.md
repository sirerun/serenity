# E5 -- M5: migration + launch (gbrain import, docs site, BrainBench trend, adversarial gate, name decision)
Acceptance: RFC section 17 M5 AC: the public fixture gbrain brain migrates with the field-level round-trip test green; fresh-machine install-to-first-answer <= 15 minutes on an empty brain from docs alone; a >= 10K-message import completes within 4h and is resumable; BrainBench trend published; adversarial corpus gate green; the name decision is resolved and executed before the repo is public.
fidelity: executable

Design anchors: RFC 3 (governance), 7.7 (operator guidance), 14, 15, 16, 19 (OQ1). Decisions: docs/adr/009 (gbrain fence mapping), docs/adr/010 (mkdocs; name decision at M4 exit).

### Wave 5a (deps on E1 and E4 exits; 6 agents)

- [x] T5.1 gbrain importer: page frontmatter, facts fence, takes fence, timeline, links -> entity pages, claims, timeline fences, graph edges (`internal/import/gbrain`)  Owner: pool  Est: 90m  verifies: [UC-032]  deps: [T1.9, T0.3]  blocked-by: [T4.17]  acc: [the synthetic fixture under testdata/gbrain-fixture/ imports; every fence row maps to one claim with SourceRef gbrain:<slug>#<row>; strikethrough rows land as superseded (pointer to the mapped row) or retracted (forgotten reason in provenance); every semantic translation is flagged review: true] (implemented 2026-09-08) — CLI plus strict file importer, atomic exclusive page publication, supplementary canonical metadata, and synthetic fixture. Fixture import: 2 pages / 8 claims; lifecycle, privacy, original cells, aliases, frontmatter, timeline and link records survive parse/render and rebuild. Full race suite: 1,430 test/subtest cases passed across 53 packages, 4 named skips, no failures. A negative control removing review=true made the fixture assertion fail. Dedicated field-by-field reporting and checkpointed crash/resume remain T5.2/T5.3.
- [ ] T5.2 Field-level round-trip test + unmapped-field report  Owner: pool  Est: 60m  verifies: [UC-032]  deps: [T5.1]  acc: [TestGBrainFieldRoundTrip asserts per row: claim text, valid window, visibility, state, superseded pointer, source, context, who/weight equal between the gbrain row and the Serenity claim; the unmapped-field report is empty on the fixture; dropping one field in the mapper makes the test fail]
- [ ] T5.3 Resumable import: per-(page,row) checkpoint in .serenity/import/gbrain.json written after the batch is durable  Owner: pool  Est: 60m  verifies: [UC-032]  deps: [T5.1, T1.2]  acc: [SIGKILL at three injected points, then resume; final repo bytes equal an uninterrupted run; zero duplicate claims]
- [x] T5.4 Docs site (mkdocs-material): install, operator manual (config, cron/serve, scheduling units, backup and recovery, keychain, upgrades, git gc guidance), connector guide, protocol specs, RFC process, threat model by build-time include  Owner: pool  Est: 90m  verifies: [UC-039]  deps: [T0.1, T1.18, T4.16]  lane: agent  acc: [mkdocs build --strict passes in CI with a link checker; a test asserts the five required documents exist and that the threat model page is an include of docs/threat-model.md, not a copy] (merged PR #175, 2026-09-07, merge commit `470f57e`) -- new `mkdocs.yml` (mkdocs-material) with navigation over the already-shipped operator/connector/protocol/RFC content (T4.16's protocol docs, T4.15's conformance guide, existing operator manual pages), `docs/operator/threat-model.md` including the canonical `docs/threat-model.md` at build time via `pymdownx.snippets` (`--8<-- "threat-model.md"`, not a pasted copy). New `internal/docs/site_test.go` (`TestDocsSiteSources`) asserts every acc-line-required source path exists and that the threat-model page is a genuine include, not a copy -- the literal acc bar. `.github/workflows/pages.yml` now installs the pinned docs toolchain (`requirements-docs.txt`) and runs `mkdocs build --strict` (fails on any broken link/reference) before the existing static-site/link and chat checks, preserving the pre-existing branded static Pages site alongside the new mkdocs build. Local evidence re-run during mark-done: strict MkDocs build, `go test -race -count=1 ./internal/docs`, `python3 scripts/site/check.py`, and `actionlint .github/workflows/pages.yml` all pass. The deployed artifact remains the existing branded `site/` tree; MkDocs is a validated documentation build surface and does not replace that site in this task. Process note: opened and merged by the Codex harness on green CI with no mark-done follow-up (same pattern as T5.5/PR #173); this session independently re-verified the content before backfilling this checkbox and the roadmap Shipped entry, and a second, independent session (PR #176) performed the same backfill concurrently -- both landed with equivalent conclusions; this text reconciles the two. Unblocks T5.7 (fresh-machine install script) and T5.10 (BrainBench trend published), both of which depend on this docs site existing.
- [x] T5.5 Adversarial corpus release gate (`evals/adversarial`, required check on release.yml): injected instructions, contradictory and stale sources, false claims, entity collisions, poisoned documents, precept-fabrication attempts, effect-request forgery  Owner: pool  Est: 90m  verifies: [UC-046]  deps: [T1.20, T3.12, T4.11]  acc: [the gate asserts zero .dira/ mutations and zero effect items not originating from a human or an authenticated propose; a seeded vulnerable build (env flag) fails the gate; release.yml lists the gate as a required job and a CI test asserts that] (merged PR #173, 2026-09-07, merge commit `dae9172`) -- new `adversarial-gate` job in `.github/workflows/release.yml`, `needs:` of `goreleaser`, so a release tag cannot produce artifacts unless it passes: `go test -race -count=1 ./internal/gate` (T1.20's existing corpus suite -- `TestAdversarialCorpusDiraHashUnchanged` proves the "zero .dira/ mutations" clause structurally over the real corpus, `TestAdversarialCorpusFabricatedPredicatesRejected`/`TestAdversarialCorpusCamouflagedRealPredicatesAreGenuine` cover the fabrication-vs-genuine discrimination), plus a seeded-vulnerable-build negative step (`SERENITY_ADVERSARIAL_VULNERABLE=1`) that must fail. New `internal/gate/release_gate_test.go` (`TestAdversarialReleaseWorkflow`) greps the workflow file for the gate job and its two required steps, so a future edit that silently drops either one fails CI before it can ship. Disclosed process note: this PR was opened and merged by the Codex harness without a mark-done follow-up or docs/roadmap.md entry -- content independently re-verified here (green CI, matches every acc clause, no attribution issue) before backfilling this checkbox and the roadmap Shipped entry; the merge itself used a real merge commit rather than this repo's usual rebase-merge convention, a minor house-style deviation not worth unwinding (history rewrite is out of scope per this repo's own destructive-ops rules).
- [ ] T5.9 10K-message synthetic corpus generator (seeded, deterministic) for the import budget benchmark  Owner: pool  Est: 60m  verifies: [UC-007]  deps: [T1.4]  blocked-by: [T4.17]  acc: [`go run ./evals/gen/messages -n 10000 -seed 1` produces a deterministic mbox set with a checksum manifest]

### Wave 5b (6 agents)

- [ ] T5.6 Name decision executed: rename module, binary, CLI strings, protocol $ids, brew formula; `serenity` alias shim for one release  Owner: David  Est: 90m  verifies: [UC-039]  kind: human  deps: [T4.7]  acc: [docs/NAME.md records the decision and the trademark/OSS collision check; go build under the new module path; a grep test finds no stale old-name references outside docs/NAME.md and the shim]
  - The rename happens before conformance transcripts are frozen for release (they embed $ids) and before the repo goes public.
- [x] T5.7 Fresh-machine install script + CI job on a clean container with the canned Ava corpus: clone -> init -> connect one connector -> sync -> extract -> search -> ask -> inbox -> check, timed  Owner: pool  Est: 75m  verifies: [UC-039]  deps: [T1.15, T3.7, T4.8, T5.4]  acc: [scripts/install-verify.sh completes in < 15 min wall clock in CI; each step's output is asserted non-empty; the script follows only commands present in the docs site] (merged PR #177, 2026-09-07, `e8673de`) -- new `scripts/install-verify.sh` drives a real fresh clone through `init -> connect (hand-edit serenity.yml's connectors.file.path, per docs/connectors/file.md's own "no CLI command to author this yet" note, then serenity connectors status) -> sync -> extract -> search -> ask -> inbox --parked -> check --json --actions [...]` against the canned Ava corpus (T1.14), asserting non-empty output at every step plus stronger real-signal checks on sync ("N new source(s)") and search (a real hit line) so a vacuous "0 new sources"/"no results" pass can't slip through. New `install-verify` CI job runs it on a clean ubuntu-latest runner under a 15-minute `timeout-minutes` backstop (script self-enforces the same budget). Real environment gotcha found and fixed: `serenity init`'s daemon-token mint needs the OS keychain, which on headless Linux means a D-Bus session + secret-service backend absent from a bare runner -- the script self-relaunches once under `dbus-run-session` (installing `dbus-user-session`/`gnome-keyring` via apt-get if missing), worked first try in real CI. No live model credentials configured (deliberate, per the acc line's own scope): `extract`/`ask` honestly report their documented "no model pinned" skip and exit 0 -- `sync`'s FTS indexing of raw source text (independent of extraction) is what actually proves ingest -> index -> search works, verified via the stronger `search "Ava"` assertion. "Commands present in the docs site" read to include RFC 0001's own CLI verb list (docs/rfc/0001-serenity.md, linked in the mkdocs Governance nav) alongside the mkdocs operator/connector pages, since the seven pipeline verbs aren't each a dedicated tutorial page. Full ladder green: gofmt/go vet/go build clean, `go test -race -timeout 600s ./...` green repo-wide, golangci-lint 0 issues, gitleaks clean, actionlint + shellcheck clean on the new job/script; `gosec` (not part of this repo's CI ladder) run anyway, 86 pre-existing G306 findings outside touched files, none new. All 11 PR checks passed including the real Linux `install-verify` run; independently reverified `CLEAN`/`MERGEABLE` before merge.
- [ ] T5.8 Import budget benchmark: 10K messages through full ingest on cached model outputs, per-stage timing artifact, nightly regression compare  Owner: pool  Est: 75m  verifies: [UC-007]  deps: [T5.9, T1.15, T2.2]  acc: [nightly job completes the import in < 4h and uploads a per-stage timing table; a run 20% slower than the last recorded run fails the job]
- [x] T5.10 BrainBench trend published: nightly job appends to evals/brainbench-trend.json on a results branch and renders a chart into the docs site  Owner: pool  Est: 60m  verifies: [UC-045]  deps: [T1.21, T5.4]  acc: [the workflow runs on cached outputs with a hard USD cap, alerts on cache miss instead of re-billing, and the docs site shows the trend with >= 2 points] (merged PR #178, 2026-09-07, `d3c8f5d`) -- T1.21 (PR #43) built BrainBench scoring plus a per-push, gitignored trend artifact and explicitly deferred durable persistence and a docs chart to this task. New `internal/eval/brainbench/trend.go`: `LoadTrend`/`AppendRow` persist rows to a durable, never-truncated JSON array; `CheckCacheHit` refuses to publish when the vendored corpus scored zero fixtures (a cache miss) rather than write a misleading 0/0/0 point; `CheckBudget` enforces a hard USD cap -- both independently unit tested. New `evals/brainbench/publish_trend.go` nightly CLI entrypoint runs both guards, prints a `::error::` annotation and exits non-zero on either failure -- since `Evaluate` always passes a nil embedder (zero live model calls, structurally, per T1.21), refusing to publish on any guard trip is the entire "alert instead of re-billing" contract. New `.github/workflows/brainbench-trend-nightly.yml` (cron nightly + `workflow_dispatch`) bootstraps or checks out the orphan `results/brainbench-trend` branch, runs the publisher against it, and pushes only on success -- never touches `main`. New `docs/evals/brainbench-trend.md` (wired into `mkdocs.yml` nav) renders the trend as an inline SVG chart fetched client-side from the results branch's live JSON; new `internal/docs.TestBrainBenchTrendPageWired` asserts the page exists, is linked from nav, and fetches the real results-branch URL (not a placeholder or copy) -- a non-vacuous check. Real data seeded, not fabricated: `publish_trend.go` was run twice against the real vendored 141-fixture corpus and the resulting `results/brainbench-trend` branch (verified fresh: 3 commits, no shared history with `main`, exactly 2 files -- `README.md` disclaiming application code, `evals/brainbench-trend.json`) pushed directly (a data-only orphan branch, not application code, consistent with what the nightly job itself will do going forward) so the docs site shows real data (precision=0.911/recall=0.845/f1=0.877 on the 69/141-scored corpus) immediately rather than waiting on the first two nightly firings -- verified live via the raw GitHub URL before merge. Full ladder green: gofmt/go vet/go build clean, `go test -race -count=1 ./...` repo-wide, golangci-lint 0 issues, gitleaks clean, actionlint + shellcheck clean on the new workflow, `mkdocs build --strict` passes with the new nav entry; `gosec`'s new G304/G306 findings match the already-accepted pattern in this same package's `load.go`/`report.go` (local file paths from CLI flags, not attacker-controlled). All 9 PR CI checks passed; independently reverified `CLEAN`/`MERGEABLE`, read the full diff, and confirmed the seeded branch's contents directly before merge.
- [x] T5.11 `serenity report --export` and the weekly report card (claims by state, corrections per 100 extractions, ladder promotions/demotions with sampled false-acceptance, spend, repo growth, rebuild time)  Owner: pool  Est: 60m  verifies: [UC-037]  deps: [T2.10, T4.10, T2.15]  acc: [golden export on the fixture brain; every metric named in RFC section 16 appears; nothing is transmitted anywhere (no network in the code path, asserted by a no-network test)] (Merged PR #149, 2026-09-07, `f0d885f`; explicit unavailable metrics, read-only snapshot and stdout export. Full race suite and all nine CI checks pass.)
- [ ] T5.12 Large-brain manual run: import >= 10K real messages on a laptop, resumable, within 4h; record it  Owner: David  Est: 4h  verifies: [UC-007, UC-032]  kind: human  deps: [T5.8, T5.3]  acc: [docs/evals/m5-report.md records machine, elapsed time, interruption/resume evidence, and final counts]

### Wave 5c (3 agents)

- [ ] T5.13 Public fixture gbrain brain: publish testdata/gbrain-fixture as a standalone public repo with README crediting gbrain; CI clones it at a pinned sha for the round-trip test  Owner: pool  Est: 45m  verifies: [UC-032]  deps: [T5.2]  kind: any  acc: [the round-trip test runs against the pinned public fixture in CI; the fixture repo README states its provenance]
- [ ] T5.14 README + docs lead with plan-check (the wedge): first demo is a plan rejected against a precept; gbrain lineage credited; MEMORY_VERBS conformance badge  Owner: pool  Est: 45m  verifies: [UC-039]  deps: [T5.4, T5.6]  lane: agent  acc: [README first code block is `serenity check`; a docs test asserts the README links the RFC, credits gbrain, and shows the conformance badge]
- [ ] T5.20 Code-complete gate: every AC line of E0-E5 checked, evals reports present, release v1.0.0 tagged and brew formula published, repo made public  Owner: David  Est: 60m  verifies: [UC-039]  kind: human  deps: [T5.5, T5.7, T5.12, T5.13, T5.14, T4.17, T3.17, T2.22, T1.23]  acc: [gh release view v1.0.0 lists three archives; the repo visibility is public; docs/plan.md shows every E0-E5 task checked]
