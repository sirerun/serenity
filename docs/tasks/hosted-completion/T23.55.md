# T23.55 — Finish onboarding, recovery UX and multi-account browser coverage

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **experience**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.44, T23.45, T23.46, T23.47, T23.48.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-ui.
- Additional live-phase claims: none.
- Estimate: 5–8 active agent-hours; excludes external waits.
- Original requirements: T23.9, T23.10, T23.23, T23.24, T23.25, T23.31, T23.35.
- Evidence: `docs/launch/evidence/T23.55/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `internal/hosted/dashboard/**`
- `tests/hosted/**`
- `playwright.hosted.config.js`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Inspect and pin the current real Rakazo settings source before UI changes; match those field labels. Add authenticated selected-brain connection status with bounded polling; never treat historical account-wide discovery as proof of the selected client connection. Task65 validates these exact instructions live.
2. Provide actionable states for expired link, account/credential capacity, allocation pending, provider outage, deletion pending/retry, exhausted quota and restore_pending. Preserve export/delete access and zero-card Free flow.
3. Complete usage numbers, remaining values, reset semantics and upgrade/portal paths; billing-disabled mode never offers a paid checkout. Separate stock capacity from period quotas in wording.
4. Refactor browser fixture to select each login link by synthetic account, support multiple contexts, controlled provider failure, time advance and restart. Add two-account/multi-brain, rotation/back/refresh, exact usage, exhaustion, export-clone, delete/re-signup and billing fixture flows.
5. Keep mobile/desktop no-overflow, keyboard focus, accessible labels and clipboard checks. Submit package/CI changes to integrator; no live email or Stripe in fixture CI.

## Acceptance — all required

- [ ] All browser cases execute with zero skips at400px and1280px; rawtoken visible only at issuance; revoked old MCP session rejected.
- [ ] Cross-account mutations do not alter victim files/usage; every mutating route rejects missing CSRF/origin; deletion retry session cannot call other routes.
- [ ] Export bundle clones and matches facts; source counters match every rendered number; provider errors leak no private text; disabled billing has no checkout.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
export SERENITY_TEST_BINARY="${TMPDIR:-/tmp}/serenity-T23.55-$(git rev-parse --short=12 HEAD)"
GOWORK=off go build -trimpath -o "$SERENITY_TEST_BINARY" ./cmd/serenity
go test -race -count=1 ./internal/hosted/dashboard
npm run test:hosted
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
