# T23.52 — Implement verified uploads and bounded all-version retention

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **operations**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.48, T23.49.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-backup-ops.
- Additional live-phase claims: none.
- Estimate: 4–6 active agent-hours; excludes external waits.
- Original requirements: T23.25, T23.26, T23.28.
- Evidence: `docs/launch/evidence/T23.52/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `deploy/hosted/backup.sh`
- `deploy/hosted/serenity-backup.service`
- `deploy/hosted/serenity-backup.timer`
- `deploy/hosted/purge.py`
- `deploy/hosted/tests/test_backup_retention.py`
- `deploy/hosted/serenity-purge.service`
- `deploy/hosted/serenity-purge.timer`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Permit one backup process at a time; check staging headroom, restrictive permissions, service-private path visibility and cleanup only its own failed staging directory. Never remove an existing retained snapshot as a cleanup shortcut.
2. Upload manifest-verified artifacts then a completion record binding the manifest hash; download verifies completion+checksums before restore. Failed upload is not complete and emits failure/age metrics.
3. Replace reliance on current30+noncurrent30 expiration with total-age purge of all versions and delete markers for expired snapshot prefixes. Choose scheduled cutoff with margin so customer-deletion backups are purged within30days, e.g. eligible snapshot age29days plus hourly sweep; lifecycle is only a backstop. Own/installable hourly systemd purge service+timer; task57 wires installation and task64 proves it runs. A script without its schedule fails acceptance.
4. Purge dry-run lists exact stack-owned keys/versions and source timestamps; apply requires scoped prefix/age checks and recorded destructive-action authority. Fail closed on malformed timestamps, pagination errors or missing version inventory.
5. Keep deletion-journal records until all snapshots they protect are permanently purged; then apply separate documented minimal-metadata retention. Submit precise IAM/alarms/unit requirements to task54.

## Acceptance — all required

- [ ] Fixtures cover overwritten versions, delete markers, multipart remnants, pagination, incorrect prefix, clock cutoff and API denial; no eligible version silently survives a successful receipt.
- [ ] Interrupted upload rejected by downloader; completed upload restores manifest-consistent contents. Failed purge yields a failed retention status, not a30day promise.
- [ ] No production object deletion occurs in this task; live qualification is task66 with approved disposable resources.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
bash -n deploy/hosted/backup.sh
python3 -m unittest discover -s deploy/hosted/tests -p "test_backup_retention.py"
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
