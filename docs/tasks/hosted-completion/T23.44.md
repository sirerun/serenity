# T23.44 — Make quota accounting recoverable and storage admission bounded

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **runtime**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.41.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-runtime, R-hosted-meter.
- Additional live-phase claims: none.
- Estimate: 6–10 active agent-hours; excludes external waits.
- Original requirements: T23.19, T23.20, T23.23, T23.27.
- Evidence: `docs/launch/evidence/T23.44/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `internal/hosted/gateway/gateway.go`
- `internal/hosted/gateway/quota_test.go`
- `internal/hosted/meter/**`
- `internal/hosted/operation/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Implement the task41 operation ledger keyed by account+brain+canonical operation_key. Reserve writes and input units atomically; persist canonical outcome and finalize both counters atomically. Reconcile interrupted operations from canonical truth before expiring/releasing leases. For calls without operation_key, allocate and persist a distinct internal operation ID before mutation and carry it into canonical outcome evidence; only repeated explicit client keys deduplicate independent requests.
2. Treat failure after canonical commit as an unknown/durable outcome, never as an ordinary failed write that releases quota. Retries across brains/windows preserve one canonical fact and one committed charge; failed audit insertion cannot erase accounting.
3. Enforce the reviewed physical-byte admission/headroom contract before mutation, including Git history, canonical bytes, index/vector/WAL growth and concurrent brains. Never delete an acknowledged fact to recover space. If a safe upper bound cannot be established, block writes and escalate the contract; do not silently redefine published storage.
4. Keep export, deletion, billing cancellation and idempotent replay available at quota exhaustion. Test period rollover, released-row cleanup and all six published allowances. Produce a shared seam request for any core writer/index change; gate allowlist stays unchanged.
5. Implement the task41 hostedtest-only phase barriers in owned code and a test proving production builds cannot activate them; include phase names in the integration receipt for task58.

## Acceptance — all required

- [ ] 100 concurrent one-write requests with allowance60 admit exactly60; one-byte headroom cannot produce an acknowledged write above the defined ceiling.
- [ ] Crash after canonical write/flush/audit/token-finalization boundaries then retry consumes exactly one write and the agreed input units. A genuinely failed mutation consumes none. Cover unkeyed writes and identical content submitted with distinct keys.
- [ ] Concurrent account brains share caps; forgotten Git history still counts; replay at zero remaining is safe; auth/readiness checks leave customer usage unchanged.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
go test -race -count=1 ./internal/hosted/meter ./internal/hosted/operation ./internal/hosted/gateway
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
