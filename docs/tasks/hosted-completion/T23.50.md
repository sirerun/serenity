# T23.50 — Implement explicit restore reconciliation and safe reactivation

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **durability**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.47, T23.48, T23.49.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-recovery.
- Additional live-phase claims: none.
- Estimate: 5–8 active agent-hours; excludes external waits.
- Original requirements: T23.26, T23.27, T23.28.
- Evidence: `docs/launch/evidence/T23.50/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `internal/hosted/recovery/**`
- `internal/cli/hosted_recovery.go`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Implement plan/apply recovery commands using task41 schema/interface and task49 manifest. Plan is read-only and emits hashed/sanitized inventory plus exact required reconciliation watermark; apply requires the unchanged plan hash.
2. Fetch trusted post-snapshot deletion journal through a fresh high-watermark and current Stripe truth via ReconcileCustomer. Remove deleted accounts/brains before permitting sign-in; absent/stale/integrity-failed authority leaves affected accounts frozen. Verify a complete journal through a fenced activation barrier: reject missing middle/tail, concurrent append races and a returning old writer. Merely reading the latest visible sequence is insufficient.
3. Keep every pre-restore credential/session revoked even for surviving accounts. Permit only fresh magic-link login and newly issued tokens after successful reconciliation. Never expose activate-all or directSQL operator shortcuts.
4. Persist idempotent recovery phases; interrupted plan/apply resumes. Billing-disabled pilot still requires deletion reconciliation and restores only Free eligibility, never historical paid access.
5. Supply CLI registration/service wiring patch request to integrator. Journal pruning cannot remove data needed by any restorable retained snapshot.
6. Implement the task41 hostedtest-only phase barriers in owned code and a test proving production builds cannot activate them; include phase names in the integration receipt for task58.

## Acceptance — all required

- [ ] Snapshot then revoke/delete/cancel, lose live state, restore: old key denied, deleted account absent, canceled plan Free, surviving facts recoverable after new login.
- [ ] Provider/journal outage prevents affected activation; foreign account/modified plan hash rejected; second apply has no additional effect. Include a surviving Free account with canceled historical subscription/stale pending Checkout; no stale blocker may survive authoritative reconciliation.
- [ ] Recovery report records counts, journal watermark, schema, provider mode and exact unresolved accounts without secret/customer content.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
go test -race -count=1 ./internal/hosted/recovery ./internal/hosted/backup ./internal/cli
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
