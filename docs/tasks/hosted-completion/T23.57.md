# T23.57 — Wire all features and gate transactional deployment and releases

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **integrator**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.42, T23.44, T23.45, T23.46, T23.47, T23.48, T23.49, T23.50, T23.53, T23.54, T23.55.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-schema, R-hosted-assembly, R-hosted-release.
- Additional live-phase claims: none.
- Estimate: 4–7 active agent-hours; excludes external waits.
- Original requirements: T23.11, T23.12, T23.14, T23.17, T23.28, T23.37.
- Evidence: `docs/launch/evidence/T23.57/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `internal/hosted/service/**`
- `internal/cli/hosted.go`
- `internal/hosted/store/**`
- `deploy/hosted/deploy.sh`
- `deploy/hosted/serenity-hosted.service`
- `deploy/hosted/Caddyfile`
- `deploy/hosted/config.example.json`
- `.github/workflows/**`
- `package.json`
- `package-lock.json`
- `deploy/hosted/tests/test_deploy.py`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Integrator continuously merges approved feature wiring during predecessor tasks; now verify one assembled binary exercises every feature. Rebase each worker on merged dependencies before accepting its receipt.
2. Finalize config/secrets/schema contracts and CLI registrations; migration/recovery/deletion code must be invoked by real service paths, not only tests. Preserve self-hosted command behavior.
3. Deploy stages checksum-verified binary/config, validates secrets without output, takes a coordinated pre-upgrade snapshot, checks schema compatibility, and switches only with bounded readiness verification. Revert binary/config on compatible failure; refuse unsafe downgrade and use reviewed restore path instead. Install and enable both backup and purge timers; retain the previous manifest/config for supported rollback.
4. Implement three artifact states: local candidate (integration tests), restricted qualification candidate (same immutable artifact plus tasks58/59 PASS), and hosted-qualified (task70 packet). CI may build/store the first before58/59 exist; it must refuse promoting to the second/third without their exact evidence. Ordinary self-hosted releases remain possible but are not called hosted-qualified.
5. Create a checksum-pinned immutable unqualified Linux ARM candidate artifact in CI, with retention sufficient for qualification, source SHA and build metadata; task64 deploys this exact candidate only after58/59 pass. Teach deployment to consume a verified downloaded candidate archive, not only a published release tag. Task70 certifies these same bytes in the release packet. Task73 owns publishing the approved tag/assets without rebuilding; an optional pilot71 may keep the immutable candidate private. Record prior supported version/schema pair. No production deploy or public release tag in this task.

## Acceptance — all required

- [ ] Fresh and upgraded fixture service passes assembled integration suite; no unregistered/new unused feature accepted.
- [ ] Bad checksum/config/secret/readiness leaves previous service usable or fails closed without data rollback; newer schema refuses older binary.
- [ ] CI distinguishes fixture/live/missing-credential results and never turns a skipped live qualification into PASS; full final-head checks green.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
export SERENITY_TEST_BINARY="${TMPDIR:-/tmp}/serenity-T23.57-$(git rev-parse --short=12 HEAD)"
GOWORK=off go build -trimpath -o "$SERENITY_TEST_BINARY" ./cmd/serenity
make vet lint test
bash -n deploy/hosted/deploy.sh
python3 -m unittest discover -s deploy/hosted/tests -p "test_deploy.py"
npm run test:hosted
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
