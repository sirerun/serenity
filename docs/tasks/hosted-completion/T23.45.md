# T23.45 — Remove avoidable tenant blocking and qualify fair admission

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **runtime**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.44.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-runtime.
- Additional live-phase claims: none.
- Estimate: 4–6 active agent-hours; excludes external waits.
- Original requirements: T23.6, T23.7, T23.8, T23.20, T23.36.
- Evidence: `docs/launch/evidence/T23.45/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `internal/hosted/gateway/gateway.go`
- `internal/hosted/gateway/admission.go`
- `internal/hosted/gateway/admission_test.go`
- `internal/hosted/credential/**`
- `internal/hosted/pool/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Move cold runtime open/recovery outside the global pool lock with bounded per-brain opening coordination; preserve exactly one writer/runtime and total open/in-flight caps. Do not hold gateway global map lock over provider or disk work.
2. Make queued account admission context-aware and bounded before tool timeout starts; define per-account/per-brain/global semantics explicitly. Hash collisions must not cause unbounded unrelated-tenant waits.
3. Apply equivalent account admission to dashboard memory actions and authenticated MCP, without denying recovery/export/delete solely because write quota is exhausted. Bound maintenance/export work separately.
4. Exercise churn of credentials, MCP sessions, limiter keys and cold brains; expired/revoked entries relinquish capacity. Honor only trusted loopback proxy headers. Keep Scale ten-brain connectivity possible.

## Acceptance — all required

- [ ] Over-cap admission returns typed capacity within100ms in deterministic local tests; canceled queued requests do no work and release permits.
- [ ] Hot tenant with a blocked provider does not prevent an independent warm tenant from serving within its timeout; cold-open burst remains bounded.
- [ ] Authenticated account/IP 429 includes Retry-After; multiple credentials cannot multiply quotas/concurrency; spoofed client-IP cannot bypass controls; maps plateau under sustained churn.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
go test -race -count=1 ./internal/hosted/gateway ./internal/hosted/pool ./internal/hosted/credential
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
