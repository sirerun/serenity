# T23.41 — Freeze integration contracts and reconcile the merged baseline

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **integrator**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `ready` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: none.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-schema, R-hosted-assembly, R-hosted-launch-plan.
- Additional live-phase claims: none.
- Estimate: 4–6 active agent-hours; excludes external waits.
- Original requirements: T23.1, T23.2, T23.4, T23.11, T23.40.
- Evidence: `docs/launch/evidence/T23.41/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `docs/launch/hosted-completion/interfaces.md`
- `internal/hosted/store/**`
- `internal/hosted/service/service.go`
- `internal/cli/hosted.go`
- `internal/hosted/contracts/**`
- `internal/hosted/testhooks/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Read b986382 and subsequent main changes; inventory each original E23 requirement using the crosswalk. Keep existing implemented features; record divergence from old contracts (JSON config, uppercase secret filenames, opaque sessions, actual packages).
2. With chief-architect, resolve and publish the four bounded design decisions in interfaces.md: physical storage reservation/headroom, crash-safe canonical-operation accounting, independently durable deletion journal, and restore eligibility. Required invariants and preferred mechanisms are in the execution plan. No worker chooses a weaker substitute. The journal design must prove completeness through activation and fence old service writers; a sequence/checksum alone does not prove no tail was omitted.
3. Freeze callable Go interfaces and failure codes for ReconcileCustomer, CloseBillingAccount, operation outcome reconciliation, deletion journal Append/ReadThrough, backup manifest v2, restore Plan/Apply and telemetry. Specify lock order, request cancellation, accounting units and every new config field. Freeze a deterministic fault-barrier API compiled only under the hostedtest build tag, using inherited private pipes/files, never an HTTP endpoint or production environment switch. Feature owners implement checkpoints in their files before qualification begins.
4. As the sole schema owner, add forward-only migrations and helper APIs needed by tasks44/47/48/50 in reviewed increments before those dependent workers start. Migration must preserve schema1–3 data; never edit an applied migration. Do not add no-op production methods just to make interfaces compile. All applied SQL remains single-writer; reserve each migration version and its owning feature in interfaces.md. Feature workers submit schema/API requests and wait for the integrator commit instead of editing store files.
5. Publish a source-pinned interface receipt. Coordinator applies all subsequent shared-file requests and integrates each feature immediately in dependency order; task57 is the final integration/release gate, not permission to leave feature code unconnected.

## Acceptance — all required

- [ ] Architectural review names the exact revision and approves each of the four decisions; unresolved decision means dependent task is blocked, not an agent guess.
- [ ] Migrate schema1/2/3 fixture DBs twice; facts, subscriptions, tokens and usage retained; future schema refused without writes; fresh DB and upgraded DB equivalent.
- [ ] Existing hosted tests and file-first gate pass. Interface document lists owning task, exact signature, semantic contract, schema version and integration file for every shared seam.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
go test -race -count=1 ./internal/hosted/store ./internal/hosted/service ./internal/hosted/contracts ./internal/hosted/testhooks ./internal/gate
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
