# T23.42 — Qualify the OpenRouter-compatible adapter and fail closed on invalid vectors

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **embeddings**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.41.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-embeddings.
- Additional live-phase claims: none.
- Estimate: 3–5 active agent-hours; excludes external waits.
- Original requirements: T23.3, T23.11, T23.34.
- Evidence: `docs/launch/evidence/T23.42/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `internal/router/openai_embeddings.go`
- `internal/router/openai_embeddings_test.go`
- `internal/embed/**`
- `internal/hosted/embedding/**`
- `docs/launch/hosted-completion/provider-contract.md`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Use https://openrouter.ai/api/v1 and candidate perplexity/pplx-embed-v1-0.6b; EMBEDDINGS_API_KEY remains the provider-neutral secret filename. Do not change the production pin merely because a model is listed.
2. Confirm current primary documentation for endpoint, JSON numeric-vector encoding, model identity, dimension, input limit, cosine requirement, pricing, routing, retention and training use. Explicitly distinguish Perplexity direct terms from the serving provider selected by OpenRouter.
3. Test numeric INT8 values decoded into existing float storage; reject empty, nonfinite, malformed or inconsistent-dimension vectors. Persist/check a stable model+version+dimension pin; a mismatch must not silently mix spaces. Handle 401/429/5xx, deadlines, cancellation and absent usage.
4. Return sanitized typed provider errors; never return upstream body text that may echo input or a credential. Freeze provider routing to a privacy-qualified provider where supported; if the API cannot enforce required terms, fail qualification.
5. Provide integrator config/wiring request. Keep readiness probes bounded and account for their cost. Fix provider usage/cost recording only with explicit units; no silent cl100k_base=provider-token assumption. No paid fallback or background retries that bypass the configured budget.

## Acceptance — all required

- [ ] Fixture server asserts endpoint, model, auth placement, cancellation and request body; error fixtures cannot leak sentinels in response/logs.
- [ ] Pin/dimension mismatch cannot corrupt or silently reuse old index; compatibility verified with numeric INT8 and float fixtures.
- [ ] Provider contract reports privacy status as verified/blocked with citations and date; fixture pass is not live qualification.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
go test -race -count=1 ./internal/router ./internal/embed ./internal/hosted/embedding
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
