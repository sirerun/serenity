# T23.64 — Provision and privately deploy one cost-capped qualification environment

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **operations**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.54, T23.57, T23.58, T23.59, T23.61.
- Profile dependencies: {"paid": ["T23.63"]}.
- External gates: SPEND, EMBEDDINGS, MAIL, DNS, OPS.
- File resource claims: task claim only.
- Additional live-phase claims: R-hosted-environment.
- Estimate: 2–4 active agent-hours; excludes external waits.
- Original requirements: T23.13, T23.14, T23.17, T23.29.
- Evidence: `docs/launch/evidence/T23.64/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `docs/launch/evidence/T23.64/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Use reviewed exact change set/account/region, artifact SHA, selected mode and spending/rehearsal limits. Acquire R-hosted-environment; only one operator mutates the environment.
2. Create or use the explicitly authorized host and blank volume; bootstrap from scratch and deploy the candidate through SSM/private access. Output the EIP for task62 foundation DNS. Keep registration restricted to controlled synthetic identities. Do not require public DNS/TLS before task62.
3. Install working embedding/mail secrets securely, Stripe test secrets only for paid qualification profile. No live billing or public Start free activation.
4. Verify disk mount persistence, service identity/hardening, local Caddy forwarding, loopback readiness, initial backup+hourly timer, journal persistence and every alarm reaching its approved destination. Public certificate/DNS proof belongs to task62; private health is sufficient here. Verify the hourly purge timer is enabled and produces a receipt. Confirm deployed binary has no hostedtest fault hooks.
5. Capture reboot/redeploy idempotence and actual inventory/cost start time. Release operator lock and retain data safely; do not delete resources without scoped cleanup authorization.

## Acceptance — all required

- [ ] Private/SSM health reaches the exact artifact; configured providers work; unauthenticated private routes denied. Public exposure remains unqualified until task62.
- [ ] Fresh host and reboot require no undocumented manual install/mount; missing data mount prevents start.
- [ ] Injected readiness/disk/backup/purge failure produces an alert at the destination; normal status restores. All charges/resources listed against authorization.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
systemctl status serenity-hosted.service serenity-backup.timer
curl --fail --silent --show-error http://127.0.0.1:8090/readyz
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
