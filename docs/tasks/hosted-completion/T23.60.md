# T23.60 — Build the cost model and bounded capacity workload

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **operations**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `ready` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: none.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-cost.
- Additional live-phase claims: none.
- Estimate: 3–5 active agent-hours; excludes external waits.
- Original requirements: T23.3, T23.32, T23.36.
- Evidence: `docs/launch/evidence/T23.60/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `scripts/hosted/cost_model.py`
- `scripts/hosted/load.py`
- `evals/hosted-load/**`
- `docs/launch/hosted-economics.md`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Price0/10/100accounts and the original1Scale+3Builder+10Free full-limit mix. Include EC2 baseline/burst credits, root+data EBS, IPv4, KMS, secrets, hourly full snapshot amplification, S3 storage/requests, logs/metrics/alarms, transfer, email, embeddings, readiness probes and rebuilds.
2. Date/cite primary rates; distinguish assumptions from measurements. Compare minimum feasible AWS configuration to already-authorized existing capacity as a proposal; no automatic topology change, reservations, new GPU or spend.
3. Build bounded load harness with synthetic accounts, warm/cold brains, skewed tenants, retries, provider latency/failure, max facts/history and controlled synthetic billing entitlements. Never forge entitlements on public production. Freeze workload manifests before live measurement:10min warmup+30min steady+5min2x burst, total8concurrent clients, baseline4requests/s with80%recall/15%remember/5%forget,20%requests target cold brains,50%traffic one hot tenant,20–128token queries and32–512token facts. Full advertised memory/storage cardinalities for the selected profile; run quota-boundary saturation separately so expected429s cannot hide failures. Replay captures exact arrivals; repeat3times. These are reviewable qualification targets, not claims of tested performance.
4. Require explicit request/token/elapsed/cost caps before live mode; fixture mode cannot measure semantic quality. Report CPU/RSS/disk/writes, latency percentiles, provider totals and per-plan contribution.
5. Produce a conservative idle+peak monthly exposure and measurable cap recommendation. Original$60 ceiling is not a budget target or new authorization; no hidden plan-limit reductions.

## Acceptance — all required

- [ ] Cost math fixtures cover units and full-snapshot multiplication; all recurring categories have rates or explicit unknowns.
- [ ] Harness stops at caps and cannot target a production origin without an explicit environment guard; finite run, no new worker scheduler.
- [ ] Freeze with task41 reviewer before execution: CPU<70%,RSS<80%RAM,disk<80%,cold-ready<=60s,admitted recallp95<=2s/rememberp95<=3s, >=99%offered non-injected eligible steady-state requests complete, unexpected5xx<0.1%, unexpected steady admission rejection<1%, zero isolation/durability errors. Count rejections/timeouts in total outcomes, not only admitted latencies. If full-limit preparation exceeds the approved provider budget, use explicitly labeled cached/precomputed vectors for infrastructure load and a separate bounded live-provider mix; never claim a full live-provider load test ran.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
python3 -m unittest discover -s evals/hosted-load -p "test_*.py"
python3 scripts/hosted/load.py --fixtures --manifest "$QUALIFICATION_MANIFEST" --output "$EVIDENCE_DIR/load-fixture.json"
python3 scripts/hosted/cost_model.py --manifest "$QUALIFICATION_MANIFEST" --output "$EVIDENCE_DIR/cost.json"
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
