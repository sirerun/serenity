# T23.49 — Version and verify backup manifests before restoring any bytes

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **durability**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.41.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-backup.
- Additional live-phase claims: none.
- Estimate: 4–6 active agent-hours; excludes external waits.
- Original requirements: T23.24, T23.26, T23.27.
- Evidence: `docs/launch/evidence/T23.49/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `internal/hosted/backup/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Implement manifestv2 with source build/schema, UTC creation, sorted unique brain inventory, bundle/control DB SHA256+length and deletion-journal watermark; preserve explicit support/refusal policy for legacyv1 snapshots.
2. Validate every entry, checksum, expected DB inventory, bundle integrity, duplicates, symlinks and path traversal before creating a usable destination. A ready brain missing its Git repo is corruption, not an empty-brain success.
3. Create snapshots only under coordinated maintenance/ownership; consistent SQLite+canonical Git captures acknowledged writes. Offline backup must verify/flush dirty canonical state or refuse rather than silently omit it.
4. Restore into a new private staging directory, validate fully, then atomically publish to an empty destination; failures cannot leave a runnable partial restore. Continue revoking all sessions/tokens and freezing accounts.
5. Implement the task41 hostedtest-only phase barriers in owned code and a test proving production builds cannot activate them; include phase names in the integration receipt for task58.

## Acceptance — all required

- [ ] Truncated/tampered DB/bundle/manifest, duplicate/omitted/unknown brain, unsupported schema and unsafe path all fail before reactivation.
- [ ] Manifest roundtrip matches exact fact IDs and canonical bundle heads; restored raw credentials/sessions remain invalid.
- [ ] Live write during coordinated backup is either included consistently or excluded as a later operation; no acknowledged pre-snapshot write lost.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
go test -race -count=1 ./internal/hosted/backup
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
