# T23.58 — Prove crash recovery with real subprocess fault injection

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **qualification**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.44, T23.47, T23.48, T23.49, T23.50, T23.57.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-crash.
- Additional live-phase claims: none.
- Estimate: 4–7 active agent-hours; excludes external waits.
- Original requirements: T23.5, T23.19, T23.25, T23.26, T23.27.
- Evidence: `docs/launch/evidence/T23.58/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `internal/hosted/qualification/crash_test.go`
- `internal/hosted/qualification/crash_helpers_test.go`
- `scripts/hosted/crash_matrix.py`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Build a separate hostedtest-tagged CLI subprocess binary on disposable roots; use task41 private-pipe deterministic phase barriers, not sleeps. Production artifact stays untagged; no production environment switch or HTTP fault endpoint.
2. Kill after mkdir/before ready; canonical write/before index; flush/before accounting; first accounting phase; checkout provider success/before save; each deletion phase; partial upload; restore before publish/reactivation.
3. Restart and assert exact facts, counters, credentials, billing state and filesystem outcome. Exercise provider timeout and disk-full errors as well as SIGKILL.
4. Demonstrate a local negative control for each recovery family by disabling its repair hook in an isolated patch, record failure, then restore. Never commit the weakened version.

## Acceptance — all required

- [ ] Every required phase was actually reached before kill, and each expected invariant measured after restart; absent barrier is FAIL, not skip.
- [ ] No acknowledged pre-crash fact lost; exactly one billed operation on retry; no account/paid-plan/token resurrection; no partial restore becomes runnable.
- [ ] Fixture external services are labeled and each scenario emits a machine-readable pass/fail and negative-control result.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
go test -race -tags hostedtest -count=1 -timeout 600s ./internal/hosted/qualification -run Crash
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
