# T23.63 — Create isolated Stripe test products, portal and webhook configuration

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **billing**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: paid.
- Dependencies: T23.47, T23.61.
- Profile dependencies: {}.
- External gates: STRIPE_TEST.
- File resource claims: R-hosted-billing.
- Additional live-phase claims: R-hosted-environment.
- Estimate: 2–3 active agent-hours; excludes external waits.
- Original requirements: T23.16, T23.21, T23.22.
- Evidence: `docs/launch/evidence/T23.63/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `scripts/hosted/stripe_setup.py`
- `scripts/hosted/tests/test_stripe_setup.py`
- `docs/launch/evidence/T23.63/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Confirm Sire Run account identity read-only; use testmode only. Idempotently create/reuse Serenity Builder1900USD/month and Scale4900USD/month tagged plan_version1; never alter unrelated products.
2. Configure portal upgrades/proration, scheduled downgrades/cancellation to match task47. Register only required events at the approved app test webhook; save generated webhook secret through secret manager, not logs.
3. Commands default to plan/dry-run; require explicit scoped apply and idempotency tags. Test local forwarding separately from the real public endpoint.
4. Finish with test object/configuration/secret setup and local forwarding fixtures only. Task67 owns all public signed delivery after tasks62/64; do not wait for64 inside this predecessor.

## Acceptance — all required

- [ ] Running setup twice yields same owned object IDs and no duplicates; wrong account/live credential refused.
- [ ] Exact prices/events/portal settings recorded with testmode label; signing secret never appears in artifacts.
- [ ] Unit fixtures pass before external apply; setup outcome cannot be described as real revenue.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
python3 -m unittest discover -s scripts/hosted/tests -p "test_stripe_setup.py"
python3 scripts/hosted/stripe_setup.py --manifest "$QUALIFICATION_MANIFEST" --plan --output "$EVIDENCE_DIR/stripe-plan.json"
python3 scripts/hosted/stripe_setup.py --manifest "$QUALIFICATION_MANIFEST" --apply --plan "$EVIDENCE_DIR/stripe-plan.json" --output "$EVIDENCE_DIR/stripe-objects.json"
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
