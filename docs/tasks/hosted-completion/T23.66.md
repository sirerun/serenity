# T23.66 — Rehearse off-host restore, deletion retention, migration and rollback

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **qualification**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.48, T23.49, T23.50, T23.52, T23.57, T23.64.
- Profile dependencies: {"paid": ["T23.67"]}.
- External gates: SPEND, EMBEDDINGS.
- File resource claims: R-hosted-recovery-proof.
- Additional live-phase claims: R-hosted-environment.
- Estimate: 3–5 active agent-hours; excludes external waits.
- Original requirements: T23.25, T23.26, T23.27, T23.28, T23.37.
- Evidence: `docs/launch/evidence/T23.66/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `docs/launch/evidence/T23.66/**`
- `scripts/hosted/recovery_rehearsal.py`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Use separately authorized disposable target/volume and synthetic accounts. Record snapshot, acknowledge writes, revoke token/delete account/cancel plan afterward, then restore only from actual downloaded complete backup plus independent journal.
2. Run plan/apply reconciliation; prove no deleted account/token/ended paid plan returns. Verify surviving memory through newly issued credential and semantic recall.
3. Inject corrupted bundle/DB/manifest, incomplete upload, stale journal, missing provider and interrupted apply. Measure wall time and last recoverable acknowledgment; report actual RTO/RPO, not invented SLA.
4. Rehearse migration from supported old schema snapshot and compatible rollback for named immutable versions. Unsupported downgrade must refuse; do not roll back production customer data to conceal migration failure.
5. Exercise all-version purge on explicitly disposable versioned test prefixes with an injected clock; prove the30day upper-bound policy algebra and actual API behavior. Never delete actual retained customer backups for testing.

## Acceptance — all required

- [ ] Off-host recovery yields expected inventory/facts and invalid old credentials; deleted accounts remain absent despite pre-deletion snapshot.
- [ ] Fault cases never yield a usable partial/unsafe restore; successful migration/rollback preserves acknowledged data.
- [ ] RTO/RPO and purge evidence include actual artifact hashes, journal watermark, version inventory and resource cleanup/retention disposition.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
python3 scripts/hosted/recovery_rehearsal.py --live --manifest "$QUALIFICATION_MANIFEST" --output "$EVIDENCE_DIR/recovery.json"
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
