# T23.70 — Assemble the reviewed release packet and exact launch actions

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **integrator**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.56, T23.57, T23.58, T23.59, T23.65, T23.66, T23.68, T23.69.
- Profile dependencies: {"paid": ["T23.67"]}.
- External gates: none.
- File resource claims: R-hosted-launch-plan.
- Additional live-phase claims: none.
- Estimate: 2–4 active agent-hours; excludes external waits.
- Original requirements: T23.28, T23.33, T23.35, T23.36, T23.37, T23.40.
- Evidence: `docs/launch/evidence/T23.70/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `docs/launch/hosted-release.md`
- `docs/launch/hosted-runbook.md`
- `docs/launch/hosted-acceptance.md`
- `docs/launch/hosted-status.md`
- `docs/launch/hosted-implementation-handoff.md`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Aggregate source-pinned per-task receipts for the selected pilot/paid profile. Any code change since evidence invalidates affected checks; rerun before recording PASS. Task69 must supply either actual human evidence or a documented unavailable-participants exception; this is an evidence requirement even though human availability is not a hard execution dependency.
2. Include immutable candidate artifact ID/archiveSHA and proposed release tag, CI, migration/rollbackpair, provider privacy/pin/budget, actual deployment inventory, secrets rotation, backup/journal/purge, alarms/recipient, measured costs/caps/RTO/RPO and security evidence.
3. Write copy-paste deploy/rollback/restore-plan/apply/rotate/deletion-retry runbook commands verified by a different operator. Resolve every original E23 requirement via crosswalk; no vague later.
4. Prepare separate reviewed pilot activation, live-billing cutover and publicsite activation diffs plus rollback. Record task69 result or explicit unqualified exception; no hidden acceptance waiver.
5. Ask for final action only once the packet is concrete; route through standing chief/founder surfaces. Prior valid approval is reused within scope. Plan publication is not deployment authority.
6. Certify the candidate as hosted-qualified in the packet only; do not publish a tag here. Task73 is the explicit release-publication owner under PUBLIC_GO; task71 may run a private pilot using the approved immutable candidate identifier.

## Acceptance — all required

- [ ] No required row UNKNOWN/PARTIAL/NOT_RUN for selected profile; only explicit original human-walkthrough exception may remain, acknowledged with no timing claim.
- [ ] Pilot packet explicitly excludes paid readiness; paid packet includes task67. Reviewer can perform rollback from the runbook.
- [ ] Candidate source/artifact/evidence match and all final checks green. Record proposed release tag; task73 performs authorized publication of the same bytes. Receipt separates implemented, merged, deployed, live and deferred with owners/triggers.

Only permitted dependency exceptions:

- T23.69: NOT_RUN only with dated coordinator/human-unavailable disposition; no timing claims; release reviewer acknowledges exception.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
export SERENITY_TEST_BINARY="${TMPDIR:-/tmp}/serenity-T23.70-$(git rev-parse --short=12 HEAD)"
GOWORK=off go build -trimpath -o "$SERENITY_TEST_BINARY" ./cmd/serenity
python3 scripts/hosted/check_completion_plan.py
make vet lint test
npm run test:hosted
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
