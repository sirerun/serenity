# T23.68 — Measure capacity and recompute the launch bill

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **operations**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.45, T23.60, T23.64, T23.65.
- Profile dependencies: {}.
- External gates: SPEND, EMBEDDINGS.
- File resource claims: R-hosted-cost.
- Additional live-phase claims: R-hosted-environment.
- Estimate: 2–4 active agent-hours; excludes external waits.
- Original requirements: T23.3, T23.8, T23.20, T23.32, T23.36.
- Evidence: `docs/launch/evidence/T23.68/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `docs/launch/evidence/T23.68/**`
- `docs/launch/hosted-economics.md`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Acquire environment lock and use controlled synthetic load window. For paid profile run full1Scale+3Builder+10Free workload; pilot profile measures its explicitly approved cap without implying paid-tier qualification.
2. Run warm/cold/skewed/churn load and provider failure; measure actual wall time, CPU/RSS/disk, p95 latency, error/admission rates and live-provider token totals within cap.
3. Project idle, measured and worst-case monthly costs including backup-history amplification and readiness/recovery calls; reconcile provider receipts with internal accounting.
4. If targets fail, record concrete smallest correction with owner/trigger, obtain any needed architecture/plan change review, and rerun affected workload. Never silently reduce published allowances or spend past cap.

## Acceptance — all required

- [ ] Task60 thresholds met for selected profile or explicit FAIL prevents corresponding launch; zero durability/isolation errors.
- [ ] Concrete monthly maximum is within approved budget and historical$60 ceiling; unknown line item blocks cost qualification.
- [ ] Configured account/runtime caps match measured capacity, and reaching a cap fails gracefully with recovery operations available.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
python3 scripts/hosted/load.py --live --manifest "$QUALIFICATION_MANIFEST" --output "$EVIDENCE_DIR/load.json"
python3 scripts/hosted/cost_model.py --manifest "$QUALIFICATION_MANIFEST" --measurements "$EVIDENCE_DIR/load.json" --output "$EVIDENCE_DIR/cost.json"
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
