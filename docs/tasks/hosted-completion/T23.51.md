# T23.51 — Bootstrap a blank ARM host idempotently and validate topology

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **operations**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.41.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-infra.
- Additional live-phase claims: none.
- Estimate: 4–6 active agent-hours; excludes external waits.
- Original requirements: T23.13, T23.14.
- Evidence: `docs/launch/evidence/T23.51/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `deploy/hosted/bootstrap.sh`
- `deploy/hosted/deploy.py`
- `deploy/hosted/tests/test_bootstrap.py`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Keep AWS us-west-2 single ARM VM architecture; prepare scripts and fixtures without provisioning. Validate subnet belongs to VPC and AZ, region/account IDs and all stack inputs before preview/execution.
2. Install version-pinned/checksummed Caddy, Git, AWS CLI and required service dependencies on AL2023 ARM. Identify only the exact stack-owned EBS volume by ID; format only a positively identified blank new volume under recorded authorization. Never infer blankness from a missing mount.
3. Preserve an existing filesystem, mount by UUID, persist fstab, set ownership and order startup after mount/network/SSM. Refuse app startup on the root disk when the data volume is missing.
4. Use command adapters/fixtures to test block-device operations. Deliver stack UserData/DependsOn/IAM requirements to the sole stack integrator rather than edit stack.json here.

## Acceptance — all required

- [ ] Blank intended disk, retained formatted disk, wrong disk, missing disk, second run and reboot cases have explicit expected outcomes; wrong/existing disk is never formatted.
- [ ] Package/network failure leaves a stopped unhealthy service; no secret data in logs; region/VPC/AZ mismatch refuses before change-set creation.
- [ ] Fresh-host live proof remains BLOCKED until task64; local fixture success is labeled local.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
bash -n deploy/hosted/bootstrap.sh
python3 -m unittest discover -s deploy/hosted/tests -p "test_bootstrap.py"
python3 -m py_compile deploy/hosted/deploy.py
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
