# T23.54 — Integrate least-privilege infrastructure and delivered alarms

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **integrator**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.51, T23.52, T23.53, T23.60.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-infra, R-hosted-telemetry.
- Additional live-phase claims: none.
- Estimate: 3–5 active agent-hours; excludes external waits.
- Original requirements: T23.13, T23.29.
- Evidence: `docs/launch/evidence/T23.54/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `deploy/hosted/stack.json`
- `deploy/hosted/tests/test_stack.py`
- `deploy/hosted/telemetry/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Only this task edits stack.json. Apply bootstrap, backup/journal, retention and telemetry requirements as one reviewed change set; keep existing account/region, encryption, IMDSv2, inbound80/443 and SSM administration.
2. Add exact least-privilege versioned-object/journal/metrics/log permissions. Keep write/delete prefixes and roles separated where needed; no broad s3:* or accountwide secret read.
3. Wire alarm actions to an approved existing destination or explicitly priced new destination. Thresholds: readiness2 consecutive1min failures; errors>5% over5min with>=20requests; disk>=80% warning/>=90% critical; last verified backup>3h; purge failure>=1; CPU>=70% for10min; instance failure>=1. Missing health/backup/purge telemetry breaches.
4. Validate alarm math/units with task53, network/AZ/disk boot topology with task51, and full monthly line items with task60. Preview only; record change-set impact and no unexpected replacements.

## Acceptance — all required

- [ ] Template fixture checks prove every required metric has a producer, correct dimension and AlarmActions; every permission targets stack resources.
- [ ] cloudformation validate-template passes. No resource creation is claimed from syntax checks.
- [ ] Injected alarm delivery to the chosen destination is required in task64, not accepted from a green CloudWatch console state alone.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
python3 -m unittest discover -s deploy/hosted/tests -p "test_stack.py"
aws cloudformation validate-template --template-body file://deploy/hosted/stack.json
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
