# T23.65 — Prove real embeddings and the actual Rakazo customer journey

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **qualification**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.42, T23.43, T23.55, T23.62, T23.64.
- Profile dependencies: {}.
- External gates: EMBEDDINGS, MAIL.
- File resource claims: R-hosted-client-proof.
- Additional live-phase claims: R-hosted-environment.
- Estimate: 3–5 active agent-hours; excludes external waits.
- Original requirements: T23.12, T23.17, T23.31, T23.32, T23.34.
- Evidence: `docs/launch/evidence/T23.65/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `docs/launch/evidence/T23.65/**`
- `scripts/hosted/client_qualification/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Run task43 frozen corpus with the dedicated capped key and privacy-qualified routing. Save corpus/model/provider/source hashes, ranks, exact usage and failures. Stop immediately at allowance.
2. Use pinned official MCP SDK through actual Caddy, then inspect/pin a real Rakazo build and settings. Verify four-tool discovery, read-only/write modes, empty label, bot/space entity scoping and two-account separation.
3. Save a unique synthetic fact, immediate paraphrase recall, lexical-negative control, service restart recall, stable IDs/citations, dropped-response dedup and forget absence from recall/exact read/export facts.json. Git bundle history may retain the forgotten text by design: explicitly verify/document this distinction. Verify bot/space scoping and two-account separation. No direct DB insert masquerading as customer write.
4. Time ten fresh controlled-mailbox browser journeys individually, from Start free/link entry to first authenticated recall. Report failures and small-sample p95; do not invent human timings.
5. Any changed instructions become a targeted task55/56 fix followed by relevant reruns; no broad upstream Rakazo work without a concrete reproduced defect.

## Acceptance — all required

- [ ] Frozen semantic thresholds pass, zero scope leakage, no forgotten result; latency/usage reported even if outside target.
- [ ] Actual Rakazo works through public HTTPS after restart using dashboard-issued credential; clipboard/rawtoken rules preserved.
- [ ] Ten results have complete start/end/outcome records; synthetic browser runs are explicitly not human walkthroughs.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
python3 scripts/hosted/eval_embeddings.py --live --manifest "$QUALIFICATION_MANIFEST" --output "$EVIDENCE_DIR/semantic.json"
python3 scripts/hosted/client_qualification/run.py --live --manifest "$QUALIFICATION_MANIFEST" --output "$EVIDENCE_DIR/client.json"
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
