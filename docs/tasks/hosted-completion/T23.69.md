# T23.69 — Prepare and record first-time human walkthroughs

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **experience**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.55, T23.56, T23.65.
- Profile dependencies: {}.
- External gates: HUMANS.
- File resource claims: R-hosted-walkthrough.
- Additional live-phase claims: R-hosted-environment.
- Estimate: 1–3 active agent-hours; excludes external waits.
- Original requirements: T23.30, T23.31, T23.33.
- Evidence: `docs/launch/evidence/T23.69/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `docs/launch/hosted-walkthrough.md`
- `docs/launch/evidence/T23.69/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Prepare a one-page script: landing, mailbox login, token copy, Rakazo settings/write opt-in, save/paraphrase recall, export and deletion; provide approved disposable accounts and a reset procedure.
2. Recruit only through authorized coordinator/human channels; do not send unsolicited messages. Record up to5 consenting first-time participants, observed timings, confusion and failures without names/private memory.
3. Fix reproducible blocking UX problems through owning lane and rerun affected steps; preserve first-run results.
4. If no participants, record NOT_RUN and reason. Original scope permits explicitly unqualified human timing; only release reviewer/founder can accept that exception, and no speed claim may be published. Always produce a terminal receipt: PASS for completed observations or NOT_RUN with explicit unavailable-participants reason and coordinator-recorded exception request; never silently leave recruitment pending.

## Acceptance — all required

- [ ] Actual participant observations are separate from automation; no fabricated scores/timings.
- [ ] All material onboarding blockers have a fix receipt or block the selected launch; accessibility/mobile checks remain passing.
- [ ] Timing/public claims match evidence; unavailable participants are visible in final packet.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
python3 scripts/site/check.py
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
