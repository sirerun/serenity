# T23.61 — Prepare the exact credential, budget and operator prerequisite packet

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **integrator**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.42, T23.43, T23.60.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-launch-plan.
- Additional live-phase claims: none.
- Estimate: 1–3 active agent-hours; excludes external waits.
- Original requirements: T23.3, T23.11, T23.13, T23.15, T23.16, T23.38, T23.40.
- Evidence: `docs/launch/evidence/T23.61/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `docs/launch/hosted-provider-setup.md`
- `docs/launch/hosted-completion/authorizations.md`
- `scripts/hosted/provider_preflight.py`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Prepare named secret destinations, minimal permissions, exact config patch, selected provider terms, finite request allowance, forecast and rollback before requesting any missing external input.
2. OpenRouter: dedicated Serenity key at EMBEDDINGS_API_KEY, candidate model/provider pin, proposed$1 total qualification cap with no automatic reset/top-up/fallback. Confirm actual key limit and account credit status through sanitized metadata when authorized; recommendation alone is not permission.
3. Resend: scoped send key, sender domain records, optional separate domain-management access, controlled recipient ownership. Stripe: restricted test key for products/prices/customers/subscriptions/Checkout/portal/webhooks and generated signing secret. No live key yet.
4. Inventory existing AWS/foundation/notification authority read-only, and record missing permissions precisely. Prepare actual monthly deployment-cost maximum plus a separate time/cost cap and cleanup plan for rehearsal resources; do not ask user to solve implementation details.
5. Record mode as public-paid scope unchanged or optional invitation-only pilot explicitly selected. Existing approvals count; ask only for genuinely missing authority through the standing coordinator channel.

## Acceptance — all required

- [ ] Packet contains no secret values, home paths or customer data; each gate lists required evidence, responsible role and exact operation authorized.
- [ ] Missing key blocks live task only; deterministic development can continue. No provider purchases, resource creation or messages are sent by preflight.
- [ ] Preflight checks placeholder/permissions/config names without echoing values and distinguishes unavailable credential from failed provider validation.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
python3 scripts/hosted/provider_preflight.py --manifest "$QUALIFICATION_MANIFEST" --output "$EVIDENCE_DIR/preflight.json" --metadata-only
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
