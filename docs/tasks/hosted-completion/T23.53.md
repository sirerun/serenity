# T23.53 — Add bounded telemetry and prove production log redaction

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **operations**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `planned` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: T23.41, T23.42.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-telemetry.
- Additional live-phase claims: none.
- Estimate: 3–5 active agent-hours; excludes external waits.
- Original requirements: T23.11, T23.29.
- Evidence: `docs/launch/evidence/T23.53/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `internal/hosted/telemetry/**`
- `deploy/hosted/telemetry/**`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Implement task41 metrics interface: requests/errors/readiness, provision duration, recall/remember latency, quota/admission denial, pool occupancy, provider calls/tokens/cost, disk utilization and last verified backup age. Fixed environment/service/operation dimensions only.
2. Provide structured logger with recursive sensitive-field redaction and error sanitization. Capture Caddy, CLI, provider failure, mail failure, backup Git stderr and billing webhook paths; redact magic-link query strings and authorization/cookies in proxy logs.
3. Provide service/CLI/Caddy integration requirements to integrator; avoid independent edits to shared assembly. Use bounded buffering and flush behavior that cannot block requests indefinitely.
4. Specify log/metric retention, emission frequency, missing-data behavior and cost in task60 inputs. Never add tenant IDs, emails, brain IDs, facts or tokens as metric labels.

## Acceptance — all required

- [ ] Synthetic sentinel email/fact/token/session appears nowhere in production captured logs across success/failure paths; disabling one guard locally makes the test fail.
- [ ] Development login links remain loopback-only; production cannot enable them. Telemetry failures do not cause memory data loss.
- [ ] Metric JSON names/units/dimensions and sample frequencies match the frozen alarm contract; bounded buffer verified under blocked sink.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
go test -race -count=1 ./internal/hosted/telemetry
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
