# T23.43 — Build a reproducible semantic retrieval qualification corpus

> Generated from `docs/launch/hosted-completion/tasks.json`; coordinator edits the registry and runs `--render`.

Read the [execution protocol](../../launch/hosted-plan.md) and [worker prompt](../../launch/hosted-completion/worker-prompt.md) first.

- Lane: **embeddings**; executor: Claude Code Sonnet, except named human/technical gates.
- Planning state: `ready` (not proof of completion).
- Profiles: pilot, paid.
- Dependencies: none.
- Profile dependencies: {}.
- External gates: none.
- File resource claims: R-hosted-eval.
- Additional live-phase claims: none.
- Estimate: 3–5 active agent-hours; excludes external waits.
- Original requirements: T23.3, T23.34.
- Evidence: `docs/launch/evidence/T23.43/result.json` plus adjacent sanitized artifacts.

## Exclusive write scope

Claim this task and the corresponding file resources before edits. Other paths are read-only; submit a precise integration request instead of editing shared files.

- `evals/hosted/**`
- `scripts/hosted/eval_embeddings.py`
- `docs/launch/hosted-completion/embedding-eval.md`

The task also owns its evidence directory. No worker edits the task registry, aggregate status/acceptance, or another task’s receipts.

## Required steps

1. Create100 synthetic query cases:40 paraphrases,20 names/entities,20 preferences/negation/near-duplicates,10 multilingual positive cases,5 temporal positive cases and5 expected-empty forgotten/expired cases. All95 positive cases have exactly one expected factID and scored distractors. No customer data. At least20 paraphrases lack useful content-word overlap.
2. Create a CLI harness that targets the real hosted MCP endpoint and separately runs lexical-only control in a disposable brain; never add a public debug bypass. Record model pin, provider, dimensions, corpus hash, exact source SHA, usage and elapsed time.
3. Support --fixtures for harness verification and explicit --live with a manifest budget/token/call cap, no automatic alternative-model routing. Query vectors and stored vectors use cosine semantics; test vector scale invariance.
4. Freeze expected IDs and candidate quality thresholds before looking at live outcomes. Save per-query ranks and failures; never adjust corpus/threshold to turn red green.

## Acceptance — all required

- [ ] Before live evaluation, task41 reviewer freezes: Hit@5 (one relevant fact/query) >=0.90 over95 positive queries; paraphrases>=36/40; names>=19/20; preferences>=19/20; multilingual>=9/10; temporal=5/5. At least18/20 lexical-negative paraphrases hit semantically and miss in the lexical control. All5 expected-empty cases return no current fact; all cross-account/forgotten leakage=0. Expected-empty queries are excluded from Hit@5 denominator and scored separately.
- [ ] Fixed-vector or lexical-only stub fails the quality predicate; fixture mode can pass harness mechanics but never marks quality PASS.
- [ ] Budget exhausted/provider unavailable yields BLOCKED with partial results and no further calls. Threshold changes need a separate reviewed rationale, not a worker override.

## Verification commands

Run from your isolated repository root. New harness paths/flags below are implementation deliverables, not commands claimed to exist today. Resolve `$QUALIFICATION_MANIFEST`, `$EVIDENCE_DIR` and any binary path before running; record the fully resolved command. A `--help` invocation is never acceptance evidence. Follow the build-lease protocol for multi-package Go work.

```sh
python3 -m unittest discover -s evals/hosted -p "test_*.py"
python3 scripts/hosted/eval_embeddings.py --fixtures --manifest "$QUALIFICATION_MANIFEST" --output "$EVIDENCE_DIR/eval-fixture.json"
```

For an external gate, prepare/test fixtures now but execute the live command only when that gate’s evidence is recorded. Missing credentials result in BLOCKED for the live phase, not PASS or a silent skip.

## Handoff / stop conditions

Open one scoped PR with the task ID, source SHA, tests/counts, evidence level, known limits and integration requests. Hold on changed interfaces, scope expansion, unexpected cost or a trusted review hold. Fix failures in scope; otherwise record BLOCKED with the exact owner/input required. Do not merge your own PR or deploy because CI is green. The coordinator accepts only after the dependency revision, review and relevant CI are verified.
