## T23.2: Land 1dfecc8 (direction and disposition HTTP wiring) through a PR

### Objective

A reviewer sees `origin/main` contain the same diff as local commit
`1dfecc828d`: `serve --http` registers the `/direction/*` and
`/disposition/*` routes alongside `/healthz` and `/mcp`, landed through a
reviewed PR rather than a direct push. `hosted-status.md` records that the
hosted gateway (T23.7) will not register these routes.

### Scope

Files this task lands (already authored in `1dfecc828d`, on
`origin/feat/blink-still-protocol-routes-20260911`): `internal/cli/serve.go`,
`internal/cli/serve_http_test.go`, `docs/operator/mcp.md`,
`docs/operator/server.md`. This task also updates
`docs/launch/hosted-status.md`. Out of scope: authoring new behavior beyond
resolving rebase conflicts against the post-T23.1 `main`; registering
`/direction/*` or `/disposition/*` in any hosted-mode code path (none exists
yet); any change under `internal/hosted`.

### Design notes

No new APIs. Rebase `origin/feat/blink-still-protocol-routes-20260911` onto
`main` (post-T23.1 head) in a worktree, resolve any conflict from the six
merged PRs, open a PR, get CI green, rebase-merge. `internal/cli/serve.go`'s
`runServeHTTP` (see `internal/cli/serve.go:99-146`) is the function this
commit extends; verify after rebase that it still calls `srv.Handle("/mcp", httpHandler)`
and the new direction/disposition handlers alongside it, and that
`memoryTools` (`internal/cli/serve.go:178-231`) is unchanged by the rebase.

ADR 014's binding chain section is explicit that hosted mode exposes only
`recall`, `remember`, `forget`, `read_memory_fact` over MCP and does not
register the DIRECTION or DISPOSITION HTTP routes at all -- those routes
are single-user CLI/daemon surface, not part of the hosted gateway's
surface. Note this in `hosted-status.md` so a later task does not
mistakenly wire them into `internal/hosted/gateway`.

### Acceptance criteria

- `git -C <main checkout> merge-base --is-ancestor 1dfecc828d origin/main`
  exits 0, or an equivalent rebased commit carrying the same diff is on
  `origin/main`, and `go test ./internal/cli -run TestServeHTTP` passes on
  `main`.
- `hosted-status.md` states plainly that hosted mode does not register
  `/direction/*` or `/disposition/*`.
- The rebased commit changes only the four files named above (or their
  post-T23.1 rebased equivalents); no unrelated file is touched.
- Genuine red: this task lands existing, already-tested code and adds no
  new behavior of its own, so there is no new genuine-red case to author.
  The applicable check is regression, not a new red: temporarily revert
  the route registration lines in `runServeHTTP` locally, confirm
  `TestServeHTTPEndToEnd` (`internal/cli/serve_http_test.go`) fails because
  the direction/disposition endpoints 404, then restore and confirm it
  passes. Report both outputs.

### Verification

`go test -race -run TestServeHTTP ./internal/cli/...` (covers both
`TestServeHTTPRequiresExactlyOneMode` and `TestServeHTTPEndToEnd`). Before
the PR, under the build lease: `go vet ./...`, `golangci-lint run --timeout=10m`,
`go test -race -timeout 600s ./...`. Claim with
`CLAIM_REMOTE=/Users/Shared/mini-build-lease.git ~/.claude/skills/claim/scripts/claim.sh claim R-build-lease --purpose "T23.2 verify"`
and release after.

### Pointers

- Epic row: `docs/launch/hosted-plan.md` T23.2; section 2 discovery names
  the commit and its diff size (65+/23- across the three files above).
- ADR 014 "Binding chain" section: "The public gateway exposes only
  `recall`, `remember`, `forget` and `read_memory_fact` ... DISPOSITION
  routes and the DIRECTION routes are not registered in hosted mode."
- `internal/cli/serve.go:99-146` (`runServeHTTP`), `internal/cli/serve.go:178-231`
  (`memoryTools`) -- the functions this commit extends.
- `docs/launch/hosted-status.md` Pins table (local-only increment row) and
  Active ownership table.
