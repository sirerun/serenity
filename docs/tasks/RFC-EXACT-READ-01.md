# RFC-EXACT-READ-01 — supported exact live memory read

Owner: hq-rfc_exact_read (Sonnet), dispatched by HQ root under David's explicit all-RFC/headless delegation authority. Parent: PR223 feat/keyed-memory-cancellation at86b0929da3e2eef306a56887207f34c2bd3944df. Draft implementation only; human merge/release gate retained.

Problem: Ajent's canonical wiki projection needs to resolve an immutable fact ID to exact canonical content. Recall/entity search is not an exact revision/body API. Keyed remember provides immutable idempotency, not document-head CAS.

Bounded result: inspect existing supported interfaces first; if no exact live memory fact read exists, add a discoverable additive MCP extension (proposed read_memory_fact) taking an exact validated fact ID, returning that live fact's exact content/provenance/expiry metadata without search/model calls. Preserve the five pinned MEMORY_VERBS v1 tools unchanged. Read only within the server's configured brain and existing disclosure fence. Reject invalid IDs; return a uniform unavailable result for missing, private, expired, canceled or otherwise ineligible content. Reuse canonical eligibility/expiry/fence helpers, not an index-only lookup that can resurrect withdrawn data. No paths, brain selection, entity fallback, synthesis, history traversal or credential material in request/response. Bound response using existing fact limits; mark content untrusted. Do not mutate canonical state, introduce writers, require embedding, or promote evidence.

This is NOT document revision/CAS/history publication, not permission to expose past withdrawn revisions. Ajent will own authorized allowed-pointer metadata, and exact reads must recheck its current grants around RPC. Describe remaining canonical document requirements honestly.

Verification: targeted meaningful tests for exact vs fuzzy IDs, invalid IDs, missing/private/expired/canceled records, no provider call, supported memory created with operation key and cancellation before/after read; live configured writer must expose fresh state after writes/cancel. Preserve response schema compatibility and existing core tool inventory. At least one actual supported-process remember→exact-read→cancel→unavailable proof with clean source/binary pins; no private real data. Full required race/vet/lint/build only under load<=10 and successfully claimed shared R-build-lease, exact own SHA release. Use BuildOffload caches/temp. If lease unavailable, continue read/code review and bank concrete wait state, never bypass or poll forever.

Work only isolated serenity-exact-read branchfeat/exact-memory-read plus outbox/exact-read status/evidence. Do not edit parent checkout or Ajent branches. No descendants, production actions, paid provider requests, active-client install, outreach, merge or deploy. Commit/push branch and prepare draft PR stacked on PR223 after checks; no HQ commits. Root handles Ajent inbox (safe-mode worker has no MCP); disclose that in PR body. Use just-in-time Composio GitHub discovery; gh fallback if no connection. Read AGENTS, Go and honest-verdicts skills before coding. Return concise actual counts, pins, failures and limits.
