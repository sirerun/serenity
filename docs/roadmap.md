# Serenity roadmap

Living status for RFC 0001 (docs/rfc/0001-serenity.md). Plan of record: docs/plan.md (epic files under docs/plans/). Update on every merge, lane claim/finish, blocker, and decision.

## Shipped

- 2026 08 27 T0.8 controlled-vocabulary enforcement (`internal/store/vocabulary.go`: `Vocabulary map[string]bool` field on `FenceWriter`/`ShardStore`, nil falls back to `defaultVocabulary` seeded from `config.Default()`; `FenceWriter.RenderEntity` and `ShardStore.Append` reject a claim/family outside it before any disk I/O; `internal/config/config_test.go` pins the exact 13 RFC §7.2 predicates with their §7.2a tiers) -- PR #3, merge commit `d773147`. Owner: kazi lane (goal `t0-8-vocab-enforcement`, converged rung 1 claude-sonnet-5). Deviation (found in code review, fixed before shipping, not by kazi): the converged diff rejected unknown predicates with a plain `fmt.Errorf(...)` -- no `store.ErrUnknownPredicate` sentinel existed at all, which is what the task's `acc:` line literally names, and both "Rejects" tests only asserted `err != nil` (any error would have passed, not specifically the vocabulary check). Added the sentinel, wrapped both writers' rejection errors with `%w`, and rewrote the two tests to assert `errors.Is(err, ErrUnknownPredicate)` plus "no file/shard-line was written" -- pushed as a follow-up commit on the same PR branch, independently re-verified (`go build`, `go vet`, `go test -race ./...`, `golangci-lint run ./...` all green) before merge. Separately hit a CI-infra issue: after the fix push, `task/t0-8-vocab-enforcement-2` stopped registering ANY GitHub Actions run (`actions/runs?head_sha=...` returned zero entries, `mergeStateStatus` stuck `UNKNOWN`) while sibling wave-0a branches triggered normally in the same window -- two more pushes and a close/reopen didn't help. Root cause: the branch was 8 merges behind current main (T0.1/T0.2/T0.3/T0.6/T0.7/T0.9/T0.10/T0.12 had all landed since the branch was cut), which appears to have wedged GitHub's mergeability computation for `pull_request` runs. Rebasing onto current `origin/main` (real conflicts in `internal/store/shard.go`/`fence_test.go` against T0.7's id-collision registry and T0.6's merge-determinism test, resolved by keeping both sides' additions) immediately restored `mergeable: MERGEABLE` and a normal CI run. Staging: N/A, Production: N/A (CLI released via a hand-cut tag, no staging/production service -- T0.11). Closes wave 0a: 9/9 done.
- 2026 08 27 T0.9 `serenity compact` verb (`internal/cli/compact.go`: `newCompactCmd`/`runCompact`, `--confirm`-gated, RunE returns a plain error mentioning "confirm" without it -- exit 1 via `cmd/serenity/main.go`'s existing error handling; with it, walks every shard family and calls the pre-existing, pre-tested `store.ShardStore.Compact`; wired into `root.go` alongside the other subcommands. `internal/cli/compact_test.go` builds and execs the real binary -- `TestCompactCLINoConfirmExits1` (exit 1, mentions "confirm"), `TestCompactCLIConfirm` with subtests `archive-exists`/`live-heads-only`/`sync-identical` (byte-identical `sync` dump before/after, real Go string equality). `internal/store/shard_test.go`'s `TestShard10KProperty` additively extended to `Compact` the 10K-claim shard then resolve/rebuild heads from a fresh `ShardStore`. README documents `serenity compact --confirm`) -- PR #8, merge commit `da6be23`. Owner: kazi lane (goal `t0-9-compact-verb`, predicate vector: 7 capability + 4 guard + 1 process, all drafted from the acc: line and confirmed RED at t0). Deviation (disclosed in the PR): kazi's own coding-harness dispatch (`kazi apply --parallel --harness claude ...`) failed 3/3 times on a pre-existing kazi bug -- the process wedges/dies at startup with the "#1255" idle-scheduler signature (`plan`/`approve`/`--check` all unaffected; only the scheduler path hangs). Matches an already-open issue filed by David, kazi-org/kazi#1683 -- added a reproduction there (with this task's and T0.7's independent datapoints) rather than filing a duplicate. Fell back to implementing directly, using the same kazi-drafted predicate vector as the objective acceptance gate (`kazi apply --check`, which stayed reliable throughout) instead of kazi's coding harness -- the acceptance bar itself was unchanged. CI caught two real issues the direct implementation missed on the first push: two unchecked `fmt.Fprintf` returns (errcheck, fixed to match the repo's `_, _ = fmt.Fprintf(...)` convention) and the integration test execing `init` as a separate binary process, which hits the real OS keychain that CI runners don't have (`secrets.MockForTesting()` only patches the in-process package, not a child process) -- fixed by scaffolding via `runInit()` in-process instead, matching the existing `TestSyncWipeRebuildViaCLI` pattern. Staging: N/A, Production: N/A (this repo ships as a released CLI binary via a hand-cut tag, no staging/production service -- see T0.11). Closes wave 0a's last kazi-lane task; only T0.8 remains open in the wave.
- 2026 08 27 T0.10 runtime-state allowlist `internal/index` (`RuntimeTables`: 5 allowlisted future-milestone tables -- `jobs`, `disposition_items`, `disposition_history`, `spend_ledger`, `caches` -- given minimal schema shells; `ResetAll` now hard-errors if a table is ever listed in both the derived-tables set and `RuntimeTables`, guarding against a future rebuild silently wiping runtime state; `TestResetAllPreservesRuntimeStateWipesOthers` and `TestRebuildPreservesRuntimeState` seed both table classes and prove only the derived ones wipe) -- PR #9, merge commit `f690688`. Owner: kazi lane (goal `t0-10-runtime-state-allowlist`). Verifies UC-002. Unblocks nothing directly but clears wave 0a.
- 2026 08 27 T0.7 claim-id collision tripwire (`store.DerivedID` gains an explicit hex-width parameter, `DefaultIDWidth = 8`, byte-identical output at that width to the old fixed `h[:4]`; `ShardStore.Append` gains a per-file, lazily-built id registry and returns the new `store.ErrIDCollision` -- writes nothing -- when an id already names a different `(subject, predicate, objectKey, validFrom, sourceRef)` tuple in the same shard file, exempting legitimate retraction-tombstone id reuse; `TestShardAppendIDCollision` forces a real collision via a deliberately narrow width and asserts the pre-existing row is untouched) -- PR #10, merge commit `c7c3324`. Owner: kazi lane (goal `t0-7-claim-id-collision-tripwire`, all 8 authored predicates converged -- 3 capability, 4 guard, 1 process). Deviation (non-blocking): also updated `internal/writer/entrypoint.go`'s `DerivedID` call site -- unavoidable, T0.3's writer queue merged mid-task and calls `DerivedID` directly. Implements ADR 004 D2. Found and fixed a genuine 32-bit hash collision in `TestShard10KProperty`'s fixed RNG seed once the tripwire went live (expected at ~1.2% for 10K claims in one shard per the birthday bound, not a detection bug) by widening that one test's ids to 16 hex chars, per ADR 004's own migration path.
- 2026 08 27 T0.6 fence concurrent-merge determinism property test (`TestFenceConcurrentMergeDeterministic` in `internal/store/fence_test.go`, RFC 0001 §7.2, UC-016: builds two independent merge-safe edits -- append-only, the only edit classes §7.2 allows -- to copies of one fence page, unions the resulting rows in both possible concatenation orders, asserts the two raw pre-render orderings genuinely differ (non-vacuousness check) then asserts `FenceWriter.RenderEntity` produces byte-identical output for both -- proving RenderEntity's existing sort-before-render step is what makes concurrent git merges resolve deterministically) -- PR #6, merge commit `1eca9df`. Owner: kazi lane (goal `t0-6-fence-merge-determinism`). No production code touched -- test-only diff, rebased onto main after T0.12/T0.8 merged to keep it minimal. Note: plan calls this a "property test"; it's a single deliberately-constructed worked example rather than randomized-input, unlike its sibling `TestFenceRoundTripProperty` -- flagged for visibility, fully satisfies the task's `acc:` line as written.
- 2026 08 27 T0.2 file-first CI gate `internal/gate/filefirst_test.go` (go/parser + go/ast walk of `internal/`, flags `UpsertEntity`/`UpsertClaim`/`InsertChunk`/`UpsertVector` calls outside the allowlist -- `internal/index/rebuild.go` exact path, `internal/writer/` prefix; `TestFileFirstGate` subtests `clean_tree` (real tree, zero violations), `red_check_disallowed_caller` (synthetic disallowed caller, exactly one violation -- the acc line's red-check), `writer_prefix_allowed` (synthetic `internal/writer/` caller, zero violations)) -- PR #7, merge commit `5c7fd20`. Owner: kazi lane (goal `t0-2-file-first-gate`, 10/11 predicates converged: every code/behavior predicate green, only the `landed` process predicate stuck). Root cause of the stuck verdict: a predicate-authoring bug on the dispatching session's side, not the code -- the brief hardcoded `git push -u origin task/t0-2-file-first-gate`, but kazi's scheduler-owned worktree executes on its own `kazi-partition/p-<hash>` branch (confirming KAZI-EXEC.md's documented warning that `--workspace` is a source, not the execution environment), so the `HEAD == @{u}` check never had a real upstream to match. Recovered the converged commit from the pushed `kazi-partition/p-16b82378e8787989-790523c6-1039f1b4` branch, cherry-picked cleanly (no conflicts) onto a rebase against current main, and independently re-verified before shipping: `go test ./internal/gate -v` (all 3 subtests pass), `go vet ./...` clean, `go test ./... -race -timeout 120s` all green, `golangci-lint run ./...` 0 issues. See docs/lore.md L-0003. **Unblocks T0.13** (wave 0b, wiring + gate extension) alongside T0.3.
- 2026 08 27 T0.3 writer queue `internal/writer` (`Queue`: one drain goroutine, per-path sequence numbers assigned under lock so submission order == execution order; `writer.Fence`/`writer.Shard` queue-backed entry points wrapping `store.FenceWriter.WriteEntity`/`store.ShardStore.Append` -- both stay pure primitives per ADR 004; `TestQueueOrderingProperty` 8 goroutines x 200 jobs over 3 overlapping paths, race-clean, proves no interleaving + total per-file order + all 1600 jobs landed) -- PR #5, merge commit `be8d2bc`. Owner: kazi lane (goal `t0-3-writer-queue`, converged rung 1 claude-sonnet-5, first-pass rate 0.6). Independently re-verified (fresh fetch + `go test -race -v ./internal/writer/...`, `go vet`, `gofmt -l`, full suite) before merge, not just kazi's self-report. Deviation (non-blocking): `Submit`/`Fence`/`Shard` return richer tuples (`Result{Seq,Bytes,Err}` / `(path, bytes, err)`) instead of a bare `error` as scoped -- keeps the four mandated names, and the extra return values are what T0.4's dirty-tree guard will need (rendered bytes for pending records). **`internal/writer` is now available** -- unblocks wave 0b (T0.4 dirty-tree guard, T0.5 daemon commits, T0.13 wiring + gate extension).
- 2026 08 27 T0.12 golangci-lint CI gate (`.golangci.yml` v2: govet, staticcheck, errcheck, unused / gofmt, goimports; `make lint`; CI `lint` job; the 26 pre-existing errcheck findings it surfaced fixed for real, incl. two genuine bugs -- `ShardStore.Append` swallowing a flush error and `ShardStore.Compact` swallowing a tmp-file write error) -- PR #4, merge commit `2ee0a14`. Owner: kazi lane (goal `t0-12-golangci-lint-ci`, converged on claude-sonnet-5 rung 1, no escalation needed). Unblocks T0.11 (v0.1.0 release cut, David).
- 2026 08 27 T0.1 threat model doc `docs/threat-model.md` (RFC 0001 §14: five adversaries, one mermaid data-flow diagram, redaction contract, keys-in-keychain, loopback-authenticated daemon, precept-integrity invariant, right-to-forget deletion chain) plus `internal/docs/threat_model_test.go`, the file-first heading gate that keeps the doc honest -- PR #2. Owner: `/apply --pool` session af341d66 (subagent lane).
- 2026 08 27 docs/lore.md L-0001 (GOWORK=off landmine: the parent sirerun/go.work references removed ./api and ./gist, breaking `go build`/`test` for any Go command run from serenity/ or a worktree parented under sirerun/) -- PR #1. Owner: a third concurrent `/apply --pool` session on this shared checkout.
- 2026 08 27 Podcast script docs/content/podcast-serenity-and-gbrain.md (two voices, ~13 min) and its ElevenLabs Studio render (Turbo v2.5, Alice as HOST, Adam as MAINTAINER, MP3). Owner: David's planning session.
- 2026 08 27 Plan to code complete (commit 9474e5a): docs/plan.md, docs/plans/E0-E6 (108 tasks), ADRs 001-010, devlog. Owner: David's planning session.
- 2026 08 27 T0.13 wire writer queue into CLI tests + extend file-first gate (`internal/gate/filefirst_test.go`'s `writeCalls` now also tracks `WriteEntity`/`Append` -- the canonical-file write primitives -- under the same allowlist as the index writes (`internal/index/rebuild.go`, `internal/writer/`), with two new subtests (`red_check_disallowed_store_write`, `store_write_writer_prefix_allowed`); `internal/cli/cli_test.go`'s `TestSyncWipeRebuildViaCLI` and `internal/cli/compact_test.go`'s `TestCompactCLIConfirm` seed via `writer.Fence`/`writer.Shard` instead of calling the store writers directly) -- PR #11, merge commit `5b11cef` (rebase merge). Owner: kazi lane (goal `t0-13-wire-writer-queue`), all 7 predicates converged. No production code changes -- no caller outside `internal/writer` invokes the store writers yet (M1 ingest spine hasn't landed), so this closes the gate/test gap only, not a behavior change. Deviation (same landmine as T0.2, docs/lore.md L-0003): predicate evaluation ran in the scheduler's `kazi-partition/p-a75bb28e31dfd1f9-*` worktree, not `task/t0-13-wire-writer-queue` -- work was committed there, then cherry-picked cleanly onto the actual task worktree at `/Volumes/BuildOffload/sirerun-worktrees/serenity-wt-t0-13-wire-writer-queue` and independently re-verified (`go build`, `go vet`, `gofmt -l`, `go test -race -timeout 120s`, `golangci-lint run` all green) before pushing and opening the PR. CI green on all 5 checks (lint, test, 3x crossbuild) before merge. Closes wave 0b's gate/test-wiring task; T0.4 and T0.5 remain in flight in the same wave.
- 2026 08 26 M0 skeleton + substrate invariants (commit 13dc0d2): init, fence + shard writers with round-trip property tests, 10K-claim shard property test, SQLite index with wipe-and-rebuild invariant and the fence/shard disagreement fixture, sync/extract/doctor/status, keychain daemon token, CI (vet/build/test-race, cross-build), goreleaser + brew tap config (never tagged).

## In progress

- 2026 08 27 E0 wave 0a complete (9/9) -- see Shipped entries for T0.1-T0.12.
- 2026 08 27 E0 wave 0b dispatched via `/apply --pool` (this session, `/loop` tick): T0.4 (dirty-tree guard), T0.5 (daemon commits + doctor last-push age), T0.13 (wire T0.3 into existing callers + extend the CLI wipe-rebuild test through the queue) -- all three claimed (WON), all kazi-lane, converging in parallel isolated worktrees. **Disk mitigation**: `/System/Volumes/Data` still at 15Gi free / 93% used (below the 20Gi standing threshold) -- removed one stale, fully-merged worktree (`.claude/worktrees/agent-a047e4b5d66b0e442`, T0.1, 408K, no live process, its branch deleted) and routed this wave's three task worktrees to `/Volumes/BuildOffload` (1.1Ti free; the go build cache already lives there) instead of the internal disk, per the plan's own "per-task worktrees on /Volumes/BuildOffload" constraint. Dispatching without further throttling since none of the new work writes meaningfully to the internal disk. T0.13 shipped (see Shipped); T0.4 and T0.5 remain in flight.

## In flight (PRs open)

- none

## Planned

- E0 M0 residuals (threat model, file-first gate, writer queue, dirty-tree guard, daemon commits, fence merge test, id tripwire, vocabulary enforcement, compact verb, runtime allowlist, lint, v0.1.0 tag)
- E1 M1 ingest spine + honest evals (watcher, Gmail IMAP, repo crawler, chunker, router, extraction, claims write path, embeddings, hybrid search, ask, eval harness, Ava corpus, BrainBench trend)
- E2 M2 reconcile + entities + disposition queue + ladder calibration
- E3 M3 direction (dira vendored, interview, check_plan, orphan detector)
- E4 M4 serve + protocols (MCP/HTTP auth, MEMORY_VERBS conformance, DISPOSITION v1, DIRECTION v1, spend ceiling, connect claude)
- E5 M5 migration + launch (gbrain import, docs site, adversarial gate, name decision, install-time AC)
- E6 M6 hardening soak (outline; starts after code complete, ADR 002)

## Blocked

- T0.11 v0.1.0 release: needs the HOMEBREW_TAP_GITHUB_TOKEN repo secret and a tag push (founder). FOUNDER: David.

## Decisions

- 2026 08 27 ADR 001 Gmail app-password IMAP certified (David).
- 2026 08 27 ADR 002 code complete = M0-M5 ACs green; M6 is a post-code-complete soak (David).
- 2026 08 27 ADR 003 third-party deps only at connector/provider edge (fsnotify, go-imap/v2, net/http adapters).
- 2026 08 27 ADR 004 one writer queue, file-backed pending records, 8-hex ids with collision tripwire.
- 2026 08 27 ADR 005 eval labeling protocol (two model families + David adjudicates); voice notes move to M2 (David).
- 2026 08 27 ADR 006 `serenity cron` until serverd in M4; x/term inbox TUI.
- 2026 08 27 ADR 007 reconcile constants (0.85 candidates, corroboration formula, parked resurfacing, read-time decay).
- 2026 08 27 ADR 008 precepts are unmodified dira entries; applies_when in the body; Serenity owns the matcher.
- 2026 08 27 ADR 009 gbrain import reads markdown fences; field mapping fixed.
- 2026 08 27 ADR 010 check exit codes per RFC, one event log per transport, token rotation, mkdocs-material, name decision at M4 exit (David).
