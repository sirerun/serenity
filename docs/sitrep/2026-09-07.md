# Serenity sitrep — 2026-09-07 (eighth report)

Serenity is the memory/reasoning engine this session is building: a
personal "brain" that ingests a person's messages and documents, extracts
facts and commitments from them, answers questions with citations, catches
plans that conflict with a person's own stated rules before they're acted
on, and serves all of that to other tools (like Sire) over a documented
network protocol. This report answers one specific question: **what
remains before the engineering is code complete** — the plan's own name
for the point where every acceptance line across all six core work areas
is checked, a v1.0.0 release is tagged, and the repository goes public.

## What you need to do

1. **The naming question is no longer preventable — it already shipped.**
   Last report flagged that a separate, independent work session had
   opened a pull request building a full public marketing website for the
   product under the name "Serenity," while the plan's own name-decision
   task (this report will call it T5.6 once, purely so you can find it in
   the plan file — every other mention spells out what it does in full)
   remained unratified. Since that report, both that website's pull
   request (https://github.com/sirerun/serenity/pull/170) and its
   companion DNS change in the separate infrastructure repository
   (https://github.com/sirerun/foundation/pull/224) have merged, and the
   DNS record has actually gone live — this session independently
   verified it: `serenity.sire.run` resolves and answers with a real page
   over HTTPS right now. In plain terms: a branded public website, a real
   subdomain, and a visual identity are all live in production today
   under the name "Serenity," and the plan's own file that's supposed to
   record the trademark/naming-collision check and the decision itself
   (`docs/NAME.md`) still does not exist anywhere in this repository. The
   decision window this report warned about closed before you acted on
   it. What's left is a different, costlier choice: either formally ratify
   "Serenity" as the name now — write the `docs/NAME.md` file the plan's
   acceptance line requires (it needs the trademark/collision check and
   the decision record) and let the already-shipped branding stand — or
   decide the name should change anyway and accept that you'd be
   reworking a live public website, a live DNS record, and a shipped
   visual identity, not just an internal codebase. Absent a specific
   reason to change course, ratifying is the lower-cost path, since the
   live infrastructure already assumes this name; that is a
   recommendation, not a decision made for you. Budgeted at 90 minutes
   either way. If you do nothing, the repository's own plan keeps
   recording this as an open, undecided task while production
   infrastructure has already decided it by default.
2. **A verification task that was blocked last report is now fully
   unblocked, and it's the single biggest lever left in the plan.** The
   final human checkpoint for the "serve over a network protocol"
   milestone (this report will call it T4.17 once) needs you to connect a
   real Claude Code session to this engine and confirm, firsthand, that it
   answers a "what did we decide about X?" question with two real
   citations, that spending past the configured monthly ceiling forces an
   approval step instead of running automatically, and that every network
   endpoint refuses an unauthenticated request. Last report, this was
   waiting on engineering work that hadn't finished; that work (three
   separate pieces: the spend-ceiling policy, the "connect this to Claude
   Code" setup command, and the full network-conformance suite) has since
   all shipped and merged, so this is now ready to run today. It matters
   beyond its own checkbox: two further tasks in the "migration and
   launch" work area are mechanically wired (not just informally
   expected) to wait for this one specifically, and four more wait on
   those two in turn — so finishing this single 60-minute task is what
   reopens the biggest blocked cluster left in the plan. What to do:
   connect a real Claude Code session to a running Serenity instance,
   ask it a question that depends on prior context, and record the
   session transcript excerpt with its two citations, the approval item's
   ID from a deliberately-forced ceiling trip, and one unauthenticated
   `curl` per network endpoint showing it's rejected, in
   `docs/evals/m4-report.md`. Budgeted at 60 minutes. If you do nothing,
   six further tasks stay blocked behind it, not because the engineering
   isn't ready but because the confirmation step is waiting on you
   specifically — no agent or work session can substitute for your own
   firsthand check of the answer's correctness.

Nothing else needs you personally beyond these two. The manual
10,000-message import test is still waiting on engineering work that
hasn't finished (the importer that would feed it isn't built yet), so
there's nothing actionable on it today.

## Verdict

This effort is on track and still accelerating, and the biggest thing
that changed since the last report is a real, self-inflicted governance
miss: the naming decision this report has warned about for two reports
running is no longer a decision in progress — it's an outcome already
live in production, decided by default rather than by you. Engineering
progress itself is strong: six more tasks shipped since the last report,
including the last two pieces of the network-serving milestone, and the
one remaining engineering-side blocker on the biggest cluster of
unfinished work (see item 2 above) has just cleared.

## Overall completion + trend

**Overall: ~89% (112 of 126 tasks across the six core work areas, merged
and verified) — was ~84% (106 of 126) at the last report, a few hours
ago.** Six more tasks shipped in that window: two finished the "serve
over a network protocol" work area entirely except for your own final
check (a certifier proving an outside reference client can talk to this
engine correctly, and this engine's own equivalent replay tool proving
its three protocols match the recorded reference examples byte for
byte), and four landed in the "migration and launch" work area (a gate
that scans incoming content for injected instructions and fabricated
facts before a release ships, a public documentation site built from
already-written content, a script that proves a brand-new machine can
install this engine and get one cited answer within 15 minutes purely by
following the documentation, and a nightly job that now durably records
this engine's retrieval-quality benchmark score over time and charts it
on the documentation site). This count still deliberately excludes the
seventh, final work area (a post-launch hardening and load-testing
phase) — the plan's own "code complete" gate only requires the first six
work areas' acceptance lines to be checked, and the seventh doesn't even
start until after that gate passes. This is a rough measure, not weighted
by how big each task actually is, so treat it as directionally right
rather than exact.

## The four states

**Done: ~89% (112 of 126 tasks, merged and verified).**

- The first four work areas are entirely finished: the core data storage
  layer, the pipeline that turns a person's messages into structured,
  honestly-scored facts, the engine that reconciles conflicting or
  updated facts and routes disputed ones to a human review queue, and the
  "direction" system that checks a proposed plan against a person's own
  stated rules.
- The fifth work area — serving all of this over a documented network
  protocol so other tools can use it — is now 20 of its 21 tasks done,
  with only your own final check (item 2 above) left.
- The sixth work area — migrating data from the tool this engine
  supersedes and preparing for public launch — has grown to 5 of its 15
  tasks done: a weekly operator report card, the content-safety gate
  named above, the public documentation site, the fresh-machine install
  script, and the retrieval-benchmark trend publisher.

**In progress: ~1% (1 of 126 tasks) — one work session, actively
running.**

- Closing the last measurable accuracy gap in the fact-extraction
  pipeline (twelve categories of fact needed to hit a required accuracy
  bar; this report will call the task T1.29). The session working it
  (one of several independent work sessions this fleet runs in parallel,
  each locking one task to itself so no two sessions duplicate the same
  work — this one is 3 hours 57 minutes into holding that lock, close to
  this fleet's own 4-hour default staleness warning, but it is
  demonstrably still active, not stuck: in that time it has run two full
  measurement passes end to end (each about 85 minutes), found and fixed
  two separate real, reproducible bugs along the way (one a
  reasoning-timeout on one specific fact category, one a case where the
  model kept a word the correct answer always drops), and is now 15-20
  minutes into a third and hopefully final full run meant to show all
  twelve categories passing together for the first time. It's holding its
  own pull request for your review rather than merging it itself, per
  this fleet's standing rule.

**Todo — ready to pick up now: 0% (0 of 126 tasks).**

- Nothing is both unclaimed and immediately startable right now. Every
  remaining task either needs you directly or is blocked behind something
  that needs you directly — see Blocked below.

**Blocked: ~10% (13 of 126 tasks).**

- *Needs you:* the naming decision and the network-protocol final check,
  both covered in full above. Two more founder-only tasks exist further
  out in the plan — a large-scale real-data import test, and the final
  "everything is checked, tagged, and public" gate — but both are still
  blocked on other engineering finishing first, so there's nothing
  actionable on either one today.
- *Other blockers:* eleven tasks in the "migration and launch" work area
  (the importer for this engine's predecessor tool, its accuracy test,
  its resumable-restart test, a large-import performance benchmark, a
  public copy of the test data used for that importer, the README's
  first-impression rewrite, and two follow-up investigations into the
  fact-extraction confusion the in-progress task above surfaced) are held
  back by chains that trace back to the same two decisions in "Needs you"
  above — most of them ultimately wait on the network-protocol final
  check finishing, and the README rewrite specifically waits on the
  naming decision, since it can't credibly show the product's name until
  that's settled.

## Risks & watch items

- **A production website and a live DNS record now exist under a name
  the plan itself still lists as an open decision** — covered in full
  under "What you need to do." This is no longer a risk to prevent; it's
  a fact on the ground that needs a decision now.
- **The plan's own summary line for the first work area is still stale**
  (flagged in the last report too, still not fixed, still not blocking
  anything): it undercounts how many tasks that work area actually
  contains, because three tasks were added to it after that summary line
  was last written and the line itself was never refreshed. Worth a
  two-minute fix; harmless in the meantime since the underlying task list
  itself is accurate.
- **Fleet health:** the one work session dispatched today (see "In
  progress" above) is active and reporting in on schedule; no session is
  silent or stuck. Its claim on its task is close to this fleet's 4-hour
  default staleness threshold, but the session's own recent reports (two
  completed full measurement runs, two disclosed and fixed bugs, a third
  run in progress) are direct evidence it's still making real progress,
  not a stale or abandoned lock.

## Next milestone

Your own final check on the network-protocol work area (item 2 above)
is what closes that work area out completely and reopens the biggest
blocked cluster of remaining work — six further tasks either wait on it
directly or wait on tasks that do. Separately, the in-progress
fact-extraction session's third and hopefully final measurement run
landing clean is what closes out the first work area's last open item.

## Glossary for this report

- work area (also called an "epic" internally): a large body of related
  work made up of many smaller tasks — this plan has seven of them, six
  of which gate "code complete"
- work session (also called a "pool session" internally): one of several
  independent work sessions this fleet runs in parallel, each locking one
  task to itself (a "claim") so no two sessions duplicate the same work
- code complete: the plan's own term for the point where every task in
  the first six work areas is checked, a versioned release is tagged,
  and the repository is made public
- conformance: proving that Serenity's implementation of a network
  protocol behaves exactly like the reference specification says it
  should, by replaying a fixed set of recorded test cases against it and
  comparing the results byte for byte
- Sire: the separate product that is meant to embed this engine as its
  memory/reasoning layer
