## Last Updated: 2026-09-05T10:35:00Z (`/loop /apply --pool` — STOPPED, E1 wave 1e complete, frontier dry again)

## Completed This Session:
- **T1.24** OpenRouter as an explicit, selectable provider (ADR 013) — PR #63, merged. `internal/config.Models.Provider` field + `internal/providers`'s `buildChatProvider` helper, only `"openrouter"` short-circuits the pre-existing "claude"-substring inference. Real regression found and fixed along the way: `config.Default()`'s new `"openrouter"` seed would have made two existing CLI end-to-end tests hit the real OpenRouter API instead of their local fixtures — fixed by explicitly clearing `cfg.Models.Provider` in both.
- **T1.25** `serenity config set-model` CLI + `docs/providers.md` — PR #66, merged. Both mark-done docs updates (PR #64, #67) also merged, plus two dispatch-note roadmap updates (PR #61, #65) — 6 PRs total this session, all self-merged on green CI.
- **kazi's `apply` grind dispatch is confirmed broken in this environment**: both `--parallel` and serial `kazi apply` hang at startup with the idle-scheduler `#1255` signature, reproducibly, across both T1.24 and T1.25's goals. Filed as a reproduction comment + reopen on kazi-org/kazi#1683 (closed 2026-08-31, still reproducing on 1.275.1). `kazi plan`/`approve`/`apply --check` all remain reliable — only the real dispatch path hangs. Fell back to direct subagent implementation for both tasks, using the kazi-drafted predicate vector via `kazi apply --check --json` as the objective acceptance gate. This is now the established pattern for this repo (T0.4, T0.7/T0.9, `t1-7-model-router`, T1.24, T1.25 all hit it) — future sessions should skip straight to the subagent lane rather than re-diagnosing.
- **Own mistake, disclosed in full (docs/roadmap.md has the complete writeup)**: mid-diagnosis on T1.24, force-removed a kazi-owned worktree without pausing on the `git status` output already printed in the same command — it held real uncommitted grind progress, destroyed permanently (nothing recoverable, no git object existed). The next attempt reproduced equivalent work, which was committed and pushed before touching anything further, so it survived a second hang. Lesson applied for the rest of the session: always re-read `git status --short` output before any worktree-removal step, never chain print-then-remove in one breath.
- Fixed a recurring environment corruption: the primary checkout's `.git/config` had `core.bare = true` again (a repeat of the 2026-09-03/04 T4.3 incident) — reset to `false`. Recorded in `docs/lore.md` L-0010 (T1.24's PR) as a variant of the existing L-0006/L-0007 class (that one was `core.worktree`, this one `core.bare`; root cause still unconfirmed for either).

## In Progress:
- None.

## Next Up (for a future session, NOT dispatched now):
- **T1.23**: David's own M1 exit verification (real Gmail 30 days + 5 repos on a laptop, `kind: human`) — the sole remaining E1 task, and the gate for everything in E2-E5. Restart `/loop /apply --pool` once this lands; per `docs/plan.md` section 5, wave 2a offers 5 immediately-claimable tasks the moment it does.
- kazi-org/kazi#1683 remains open with this session's two reproductions attached — worth checking if it gets a real fix before assuming the subagent-fallback pattern is still needed next time.

## Known Blockers:
- T1.23 blocked on David only (needs his real Gmail account and his own repos — genuinely not delegable).
- Disk: 97Gi free / 79% used on `/System/Volumes/Data` this session — well above the 20Gi threshold, not a blocker. `/Volumes/BuildOffload` was NOT mounted this session (worktrees went to the internal disk instead, per CLAUDE.md's guidance to fall back when the offload volume is absent and disk pressure is low).

## Merge Status:
- `main` on origin at `d526126` as of this checkpoint. 6 PRs merged this session (#61-#67, minus the roadmap-note churn already folded in): #63 (T1.24), #64 (T1.24 mark-done), #65 (T1.25 dispatch note), #66 (T1.25), #67 (T1.25 mark-done), plus #61/#62 (T1.24 dispatch/fallback notes).

## Ship Status:
- E1 wave 1e: 2/2 shipped (T1.24 PR #63, T1.25 PR #66). E1 overall: 24/25, only T1.23 (human) remains.

## Claims held by this session:
- None. All released (T1.24, T1.25, and both R-plan-md locks).

## Loop status: STOPPED (frontier dry — everything left is either T1.23 itself or transitively gated behind it). No ScheduleWakeup armed. Restart with `/loop /apply --pool` once T1.23 lands.
