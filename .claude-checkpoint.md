## Last Updated: 2026-09-07T13:25:00Z (`serenity_pool-e1`, post-T4.9-merge scan)

## Completed This Session:
- Landed T4.6 (DIRECTION v1 server) earlier this session, PR #146, merge `6b84db2`. See prior checkpoint history / docs/roadmap.md Shipped for detail.
- Landed T4.9 (CLI vs protocol drift tests: search/recall, ask/synthesize, check/check_plan, inbox dispose/dispose, brief/brief over one fixture). PR #157, rebase merge commit `ef14903078ec7df5f56640ef63e692f309154d4d`. All 9 CI checks green (build/crossbuild x3/lint/test/dira-cli-conformance/dira-vendor-pin/brainbench-trend/eval-cached). Full detail in docs/roadmap.md's Shipped section and docs/plans/E4-m4-serve-protocols.md.
  - Extracted each pair's shared logic into an exported function both the CLI path and protocol handler call (`internal/cli.searchResults`/`askAnswerFor`, `internal/server/memory.PackFacts`/`FactOfCitation`, `internal/server/direction.Handlers.BuildBrief`), mirroring T4.6's own `check.ToWire` precedent -- no new user-facing CLI surface built to make the comparison possible (an earlier draft added a `serenity brief` verb; reverted after review since it would have shipped real new surface with none of the fuzz/security scrutiny T4.11 gave every other verb handler in this epic -- filed as a follow-up task instead, noted under T4.9's own line in docs/plan.md's wave-4c list).
  - T4.9 asserts CLI<->protocol-handler structural parity only, a different axis from T4.20's still-open protocol<->gbrain-pinned-contract conformance repair -- disclosed explicitly in the epic file/roadmap/PR, not conflated.
  - Found, not fixed: `internal/index.SQLite.SearchFTS` hard-errors on a bare hyphenated query token (FTS5 MATCH grammar quirk, verified directly against sqlite3's fts5 extension) instead of degrading gracefully -- affects `search`/`ask`/`recall` for any hyphenated query term in production.
  - Hit a real doc-race finishing this: T4.11's own mark-done pass (PR #155) landed on main right as I rebased/pushed, both sessions independently computing E4's rollup as "10/20" (each assuming their own task was the sole +1). Caught on rebase, corrected docs/plan.md's E4 header and docs/roadmap.md's Planned line to the true 11/20 mentioning both PRs.
  - T4.9 claim released (`c58cc413...`). Worktree (`/Volumes/BuildOffload/wt/serenity-T4.9`) and remote branch already gone by the time this session checked post-merge; no stranded branch/worktree.

## Current frontier (verified directly against docs/plans/E4-m4-serve-protocols.md's checkbox state + deps this session, not stale):
- **E0/E1(except T1.23)/E2/E3: fully shipped** (13/13, 24/25, 23/23, 17/17).
- **E4**: 11/20 as of this pass (T4.1, T4.2, T4.3, T4.4, T4.6, T4.8, T4.9, T4.10, T4.11, T4.12, T4.18 checked; T4.5 checked-but-reopened acceptance). Remaining: T4.7 and T4.20 both claimed and actively in progress (T4.7 ~2h into a stated 8h run; T4.20 ~50min in, neither stale). T4.13/T4.14/T4.15/T4.16/T4.19 all transitively blocked on T4.7 and/or T4.20 finishing. T4.17 is David's own `kind: human` M4 exit task.
- **E5**: 0/15, still milestone-gated behind M4 per ADR 011.
- **E6**: post-code-complete, not reachable yet.
- **Net: the frontier is genuinely dry for a new pool claim right now.** Everything open in E4 is either claimed (T4.7, T4.20) or transitively gated on one of those two landing.

## Known Blockers:
- T1.23 still blocked on David only.
- E4's remainder blocked on T4.7 and T4.20, both actively claimed elsewhere.

## Merge Status:
- `main` at `ef14903` (this session's PR #157 merge, includes the T4.9 code + all docs mark-done/PR-fill-in/rollup-fix commits). Locally in sync (verified via `git fetch origin main` + `git log origin/main`).

## Claims held by this session:
- None. T4.9 released after merge.

## Loop status: STOPPED (frontier dry -- every remaining E4 task is blocked on T4.7/T4.20, both already claimed and actively worked; E5 stays milestone-gated). No ScheduleWakeup armed -- per docs/plan.md section 5's idle-session rule, not idle-polling. Relaunch once T4.7 or T4.20 lands and reopens the rest of wave-4c, or once `/claim --list`/`git ls-remote origin refs/claims/*` shows one of them released.
