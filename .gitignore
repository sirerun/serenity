# Derived state — rebuildable, never canonical (RFC §7.5)
.serenity/

# Ephemeral agent outputs
.claude/
.kazi/
.mcp.json

# Release artifacts
dist/

# dira's own derived read cache, rebuilt on demand (T3.14 fixture ledger)
testdata/brain-fixture/.dira/cache/

# Generated eval report (plan T1.22) -- regenerated by cmd/eval-runner,
# never hand-edited or canonical.
/evals/report.json

# CI-generated eval run output -- regenerated fresh every run
# (evals/brainbench/gen_trend.go, T1.21) and uploaded as a workflow
# artifact, never committed. T5.10 is what persists a trend across runs.
evals/brainbench-trend.json
