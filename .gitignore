# Derived state — rebuildable, never canonical (RFC §7.5)
.serenity/

# Ephemeral agent outputs
.claude/
.kazi/
.mcp.json

# Release artifacts
dist/

# dira's own derived read cache, rebuilt on demand (T3.14 fixture ledger)
testdata/brain-fixture/.dira/cache/
