---
id: int-0002
kind: intent
title: Zero-ceremony operation — one binary, no server, no daemon
state: active
created: "2026-06-01T09:00:00Z"
tags: [fixture, enforcer, dx]
source:
  hook: manual
  tier: human
confirmed_by: human
---

Fixture copy of the real `int-0002`, reused verbatim for `internal/enforcer`
testing because `dec-0060` and `dec-0081` in this same fixture ledger both
`derives_from` it and the canonical demo (`README.md`, `dira check` block)
cites it by id in `dec-0060`'s `why_not`.

dira is invoked from hooks, several times per session, in the latency path of a
human waiting for a prompt to return. Every millisecond of cold start and every
step of setup is felt directly.

The whole tool must therefore be: one static binary, no runtime to install, no
process to keep alive, no port to own, no account to create, and no network
call on the happy path. Storage is files in the repo you already have; sync is
the git remote you already push to.

Achieved when: `brew install dira && dira init` is the entire onboarding, the
binary works with the network unplugged, and `dira brief` returns in well under
100ms on a cold cache.
