---
id: dec-0075
kind: decision
title: Compress checkpoint snapshots with zstd before writing to disk
state: staged
created: "2026-07-20T08:00:00Z"
tags: [fixture, enforcer, persistence]
alternatives:
  - option: write checkpoints uncompressed
    why_not: >
      larger on-disk footprint for a long session with many checkpoints —
      not yet confirmed as a real problem, which is exactly why this entry
      is staged rather than accepted
source:
  hook: Stop
  session: fixture-session-01
  excerpt: "let's zip the checkpoint before we write it, it's getting kind of big"
  tier: regex
---

Regex-tier guess from `dira sniff` (`dec-0003`). Unconfirmed: no human has
reviewed this, so it must never be cited by `dira check` regardless of how
closely a candidate plan matches its text. `internal/enforcer/testdata/corpus.yaml`
includes a row whose plan text matches this entry almost word for word and is
still labeled `compliant` — proving `state: staged` is not enforcement
substrate even under near-total lexical overlap.
